---
phase: 09-multisig-registry-governance-e5
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - privdId_admin/backend/package.json
  - privdId_admin/backend/services/safeService.js
  - privdId_admin/backend/services/credentialService.js
  - privdId_admin/backend/services/studentService.js
  - privdId_admin/backend/models/Student.js
autonomous: false
requirements: [GOV-02, GOV-03]
user_setup: []

must_haves:
  truths:
    - "safeService.js exposes proposeSafeTransaction(to, data), proposeRegistryWrite(fnName, args), confirmSignature, executeTransaction, and getPendingTransactions wrappers around @safe-global/protocol-kit + api-kit"
    - "proposeRegistryWrite encodes a registry call and delegates to the generic proposeSafeTransaction(to, data) primitive, so any encoded Safe call (including acceptAdmin during the 09-05 admin handoff) can be proposed without duplicating propose logic"
    - "credentialService.js's exported issueCredentialOnChain(student, dek) and revokeCredentialOnChain(rollNo) keep their names/signatures but issueCredentialOnChain pins to IPFS first then proposes (returning {cid, safeTxHash, status:'pending'}) and revokeCredentialOnChain proposes (returning {safeTxHash, status:'pending'}) instead of signing directly with PRIVATE_KEY"
    - "All 3 studentService issue call-sites (createStudent L144, updateStudentById re-issue path L298, insertBulkStudents bulk path L193) and the revokeStudent call-site (L319) record pendingRegistryAction; the issue sites store ciphertextCID; none flip the terminal issued/revoked state on propose"
    - "DEK persistence is preserved at every issue site after the propose-only refactor — createStudent's student.dek assignment, the bulk-path Student.updateOne dek payload field, and updateStudentById's reused DEK all survive unchanged"
    - "No direct unsigned ethers.Wallet write to issueCredential/revokeCredential remains in the issue/revoke flow"
  artifacts:
    - path: "privdId_admin/backend/services/safeService.js"
      provides: "Safe propose/confirm/execute/getPending wrappers + generic proposeSafeTransaction primitive"
      exports: ["proposeSafeTransaction", "proposeRegistryWrite", "confirmSignature", "executeTransaction", "getPendingTransactions"]
    - path: "privdId_admin/backend/models/Student.js"
      provides: "pendingRegistryAction subdocument field"
      contains: "pendingRegistryAction"
    - path: "privdId_admin/backend/package.json"
      provides: "Safe SDK dependencies"
      contains: "@safe-global/protocol-kit"
  key_links:
    - from: "privdId_admin/backend/services/credentialService.js"
      to: "privdId_admin/backend/services/safeService.js"
      via: "proposeRegistryWrite import + call"
      pattern: "proposeRegistryWrite"
    - from: "privdId_admin/backend/services/studentService.js"
      to: "privdId_admin/backend/models/Student.js"
      via: "student.pendingRegistryAction assignment"
      pattern: "pendingRegistryAction"
---

<objective>
Build the backend Safe propose/confirm/execute service (`safeService.js`), redirect `credentialService.js`'s on-chain issue/revoke functions through it, and add a `pendingRegistryAction` status to the Student model + `studentService.js` so issuing/revoking becomes a "proposed, awaiting signatures" 2-phase lifecycle (D-12). This is GOV-03 (safeService wraps the flow and is the path issue/revoke uses) and GOV-02's off-chain half (no single key can mutate state — all writes now go through a Safe proposal).

Purpose: Remove the last direct unsigned `ethers.Wallet`-signed registry write and route every issue/revoke through Safe 2-of-3 consensus.
Output: Installed Safe SDK deps; `safeService.js` (with a generic `proposeSafeTransaction` primitive plus the registry-specific `proposeRegistryWrite`); redirected `credentialService.js`; `pendingRegistryAction` on Student + studentService propose-only handling at all 4 call-sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-multisig-registry-governance-e5/09-CONTEXT.md
@.planning/phases/09-multisig-registry-governance-e5/09-RESEARCH.md
@.planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md

<interfaces>
Verified Safe SDK API surface (from RESEARCH.md, sourced from installed package .d.ts). Use these directly.
- protocol-kit v7 entry: Safe.init({ provider, signer?, safeAddress }) — provider is an RPC URL string OR EIP-1193 object; default export Safe.
- protocolKit.createTransaction({ transactions: [{ to, value: '0', data }] }) -> SafeTransaction
- protocolKit.getTransactionHash(safeTransaction) -> safeTxHash
- protocolKit.signTransaction(safeTransaction) -> signed SafeTransaction; .getSignature(address).data -> signature hex
- protocolKit.executeTransaction(txObject) -> { transactionResponse?.wait?.() }
- api-kit v4 entry: new SafeApiKit({ chainId: 11155111n }) (Sepolia). NO public service for local chainId 31337 — see constraints.
- apiKit.proposeTransaction({ safeAddress, safeTransactionData, safeTxHash, senderAddress, senderSignature })
- apiKit.confirmTransaction(safeTxHash, signature)
- apiKit.getTransaction(safeTxHash), apiKit.getPendingTransactions(safeAddress) -> { results: [{ confirmations?: [], isExecuted, ... }] }
- apiKit.getSafeInfo(safeAddress) -> { threshold, owners, nonce } — threshold is NOT on the tx object (RESEARCH Pitfall 3)
- ABI encode call data: new ethers.Interface(registryArtifact.abi).encodeFunctionData('issueCredential', [rollNo, cid, pubHashBytes32]) then wrap { to: REGISTRY_ADDRESS, value: '0', data } (RESEARCH Pitfall 5)
</interfaces>

<source_facts>
Verified against actual source (graphify + direct read) — DO NOT trust earlier line numbers from memory:
- credentialService.js: `anchorOnChain(rollNo, cid, merkleRoot)` is UNEXPORTED (L44), returns `{ txHash, blockNumber }`. `issueCredentialOnChain(student, dek)` is EXPORTED (L96): it encrypts, pins to IPFS to get `cid` (via pinToIPFS L18), then calls `anchorOnChain(student.rollNo, cid, student.merkleRoot)` (L100) and `return { cid, txHash, blockNumber }` (L103). `revokeCredentialOnChain(rollNo)` is EXPORTED (L58), returns `{ txHash, blockNumber }`.
- The direct unsigned write lives INSIDE `anchorOnChain` and `revokeCredentialOnChain` (the `new ethers.Wallet(...PRIVATE_KEY...)` + `registry.issueCredential(...)` / `registry.revokeCredential(...)` calls).
- studentService.js imports `{ issueCredentialOnChain, revokeCredentialOnChain, pinEnvelopeToIPFS }` at L6. There are THREE issue call-sites and ONE revoke call-site:
  1. createStudent (L120) — issue at L144: `const { cid, txHash, blockNumber } = await issueCredentialOnChain(student, dek);` then L145 `student.dek = dek.toString('base64')`, L146-148 sets student.ciphertextCID/onChainTxHash/onChainBlock, then `await student.save()` (L151). The student.dek assignment is a Mongoose instance field persisted by .save().
  2. insertBulkStudents (L175) — bulk issue at L193: same destructure, then `Student.updateOne({ _id: student._id }, { ciphertextCID: cid, dek: dek.toString('base64'), onChainTxHash: txHash, onChainBlock: blockNumber, anchorPending: false, lastAnchorError: null })` at L194-197 (also mirrors ciphertextCID/onChainTxHash/onChainBlock onto the in-memory `student`). `Student.updateOne` writes ONLY the listed fields — there is NO implicit carry-over; `dek` is written in the SAME updateOne and is silently lost if omitted.
  3. The re-issue path used by updateStudentById (studentController.js L202) — issue at L298: same destructure, L299-301 sets ciphertextCID/onChainTxHash/onChainBlock then `await student.save()`. DEK is REUSED here from the existing student.dek (selected via .select('+dek') at L224), never re-assigned and never rotated (D-04/D-05).
  4. revokeStudent (L312) — revoke at L319: `await revokeCredentialOnChain(student.rollNo);` then L325-326 flips student.revoked = true / student.revokedAt = new Date().
- Student model fields (verified): onChainTxHash (String, default null), onChainBlock (Number, default null), anchorPending (Boolean), lastAnchorError (String), revoked (Boolean, default false), revokedAt (Date, default null). dek has select:false. No pendingRegistryAction field yet (this plan adds it).
- middleware/ currently contains ONLY asyncHandler.js + errorHandler.js — there is NO auth/JWT middleware anywhere (relevant to plan 09-03, not this plan).
</source_facts>

<constraints>
- Use @safe-global/protocol-kit@^7.2.0 + @safe-global/api-kit@^4.2.0 + @safe-global/types-kit@^3.1.0 (RESEARCH Standard Stack, all [OK] in Package Legitimacy Audit). Do NOT use EthersAdapter/Safe.create()/safe-ethers-lib (v1/v2, removed — RESEARCH Pitfall 2).
- The backend never holds the 3 officials' private keys (D-01). safeService's sign-side functions must accept an externally-supplied signature/signer (MetaMask-relayed). process.env.PRIVATE_KEY stays reserved for the deployer EOA only (D-03) — never use it for official issue/revoke signing.
- Keep credentialService.js's EXPORTED function names stable. studentService.js L6 imports { issueCredentialOnChain, revokeCredentialOnChain, pinEnvelopeToIPFS } — do NOT rename these exports or studentService breaks.
- D-12 return semantics: issue/revoke now return { ..., safeTxHash, status: 'pending' } immediately after PROPOSE (do not block on execute). studentService records pendingRegistryAction and leaves issued/revoked terminal state unset until execution confirms. Execution wiring lives in plan 09-03's execute route + plan 09-04's UI; this plan only records the pending state and stops flipping terminal state on propose.
- DEK PERSISTENCE IS NON-NEGOTIABLE: the propose-only refactor MUST NOT drop DEK storage at any issue site. A null DEK after anchoring is a known prior live bug (bulk student 22BCSD01 left with null DEK, blocking credential claim). createStudent's `student.dek = dek.toString('base64')` and the bulk-path updateOne's `dek: dek.toString('base64')` field must both survive verbatim. The updateStudentById path reuses the existing DEK and must not change it.
- Reuse the existing ABI-loading pattern from credentialService.js (__filename/__dirname/registryArtifact = JSON.parse(readFileSync(join(__dirname,'../../../zk-proofs/artifacts/contracts/CredentialRegistry.sol/CredentialRegistry.json')))) — do NOT introduce a second ABI-loading mechanism (PATTERNS Shared Patterns).
- Use the [safe]-prefixed bracketed console.log convention (PATTERNS safeService section), e.g. [safe] Proposed issue for ${rollNo} | safeTxHash: ${safeTxHash}.
- chainId/txService config from env: SAFE_ADDRESS, SAFE_CHAIN_ID (default 11155111 Sepolia). Gate any local-dev raw-key signer behind an explicit USE_LOCAL_SIGNERS flag so it can never activate against Sepolia (RESEARCH Security Domain Information-Disclosure mitigation).
- proposeRegistryWrite MUST be a thin wrapper over a generic proposeSafeTransaction(to, data) primitive (not a parallel copy of the propose logic). This generic primitive is what plan 09-05's acceptAdmin-handoff script reuses to propose an arbitrary encoded Safe call — keep the propose/sign/api-kit logic in ONE place.
</constraints>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install Safe SDK packages (legitimacy gate)</name>
  <what-built>
    Install @safe-global/protocol-kit, @safe-global/api-kit, @safe-global/types-kit into privdId_admin/backend.
    All three were audited [OK] by slopcheck v0.6.1 against the npm registry (RESEARCH Package Legitimacy Audit) — official @safe-global org scope, repo github.com/safe-global/safe-core-sdk, ~3yr/~1.7yr ages.
  </what-built>
  <how-to-verify>
    1. Confirm the three packages and versions against RESEARCH.md Package Legitimacy Audit table (all [OK], Approved).
    2. Optionally spot-check npmjs.com/package/@safe-global/protocol-kit shows the official Safe Global org.
    3. Approve to run: cd privdId_admin/backend and npm install @safe-global/protocol-kit@^7.2.0 @safe-global/api-kit@^4.2.0 @safe-global/types-kit@^3.1.0
  </how-to-verify>
  <action>After approval, run npm install @safe-global/protocol-kit@^7.2.0 @safe-global/api-kit@^4.2.0 @safe-global/types-kit@^3.1.0 in privdId_admin/backend. Verify the three keys land in package.json dependencies. This is a blocking-human gate per the package-install security policy even though the audit verdict was [OK].</action>
  <verify>
    <automated>grep -c "@safe-global/protocol-kit\|@safe-global/api-kit\|@safe-global/types-kit" privdId_admin/backend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - All three @safe-global/* packages present in privdId_admin/backend/package.json dependencies
    - privdId_admin/backend/node_modules/@safe-global/protocol-kit exists
  </acceptance_criteria>
  <resume-signal>Type "approved" to install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create safeService.js (generic proposeSafeTransaction + registry wrappers) and redirect credentialService.js through it</name>
  <files>privdId_admin/backend/services/safeService.js, privdId_admin/backend/services/credentialService.js</files>
  <read_first>
    - privdId_admin/backend/services/credentialService.js (ABI-loading block + __filename/__dirname/registryArtifact L8-11; pinToIPFS L18; UNEXPORTED anchorOnChain L44-55 returning {txHash,blockNumber}; EXPORTED revokeCredentialOnChain L58-67; EXPORTED issueCredentialOnChain L96-103 which pins via pinToIPFS, calls anchorOnChain L100, returns {cid,txHash,blockNumber} L103; the [credential] console.log convention)
    - .planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md (safeService.js section + credentialService.js MODIFIED section — exact redirect shape)
    - .planning/phases/09-multisig-registry-governance-e5/09-RESEARCH.md (Pattern 2 propose->confirm->execute; Pattern 3 getPendingTransactions + getSafeInfo.threshold; Pitfall 3, 4, 5)
  </read_first>
  <action>Create privdId_admin/backend/services/safeService.js (ESM, matching backend module style). Reuse credentialService.js's exact __filename/__dirname/registryArtifact ABI-loading block. Export FIVE functions:
  (a) proposeSafeTransaction(to, data) — the GENERIC primitive: wraps { to, value: '0', data }, calls Safe.init({ provider, signer, safeAddress: process.env.SAFE_ADDRESS }), createTransaction({ transactions: [meta] }), getTransactionHash, signTransaction, then apiKit.proposeTransaction({...}), returns { safeTxHash, status: 'pending' }. The proposer signature comes from the caller (MetaMask relay) in production; provide a USE_LOCAL_SIGNERS-gated local-dev fallback that may use a raw key, never active when NODE_ENV is production.
  (b) proposeRegistryWrite(fnName, args) — a THIN wrapper: builds data via new ethers.Interface(registryArtifact.abi).encodeFunctionData(fnName, args), then returns proposeSafeTransaction(process.env.REGISTRY_ADDRESS, data). Do NOT duplicate the Safe.init/createTransaction/propose logic here — delegate to (a). (This delegation is what plan 09-05's acceptAdmin-handoff script reuses: it encodes acceptAdmin() against the registry ABI and calls proposeSafeTransaction directly.)
  (c) confirmSignature(safeTxHash, signature, signerAddress) — calls apiKit.confirmTransaction(safeTxHash, signature).
  (d) executeTransaction(safeTxHash) — calls apiKit.getTransaction(safeTxHash) then protocolKit.executeTransaction(tx) and awaits the receipt; returns the execution result including the on-chain transaction hash and block number (executePendingTx in 09-03 consumes these to set the student's terminal onChainTxHash/onChainBlock). Surface the resulting txHash and blockNumber on the returned object (e.g. { safeTxHash, txHash, blockNumber, ... }).
  (e) getPendingTransactions() — reads apiKit.getSafeInfo(SAFE_ADDRESS).threshold once plus apiKit.getPendingTransactions(SAFE_ADDRESS), returning each result decorated with signedCount = confirmations?.length ?? 0 and awaiting = threshold - signedCount (RESEARCH Pitfall 3 — threshold is on the Safe, not the tx).
  Instantiate apiKit = new SafeApiKit({ chainId: BigInt(process.env.SAFE_CHAIN_ID || 11155111) }). Use [safe]-prefixed console.logs.

  Then MODIFY credentialService.js: add import { proposeRegistryWrite } from './safeService.js'. RESTRUCTURE the EXPORTED functions themselves (not just the internal anchorOnChain helper):
  - issueCredentialOnChain(student, dek): keep its existing encrypt + pinToIPFS step so it still produces `cid` first; then compute pubHashBytes32 exactly as the current issue path does; then REPLACE the `await anchorOnChain(...)` call with `const { safeTxHash, status } = await proposeRegistryWrite('issueCredential', [student.rollNo, cid, pubHashBytes32]);` and `return { cid, safeTxHash, status };` (NOT the old {cid, txHash, blockNumber}). Remove/neutralize the direct-write body inside anchorOnChain (or stop calling anchorOnChain) so no new ethers.Wallet(...PRIVATE_KEY...) + registry.issueCredential(...) write remains reachable from the issue path.
  - revokeCredentialOnChain(rollNo): replace its direct-write body with `return await proposeRegistryWrite('revokeCredential', [rollNo]);` so it returns { safeTxHash, status }. Remove the new ethers.Wallet(...PRIVATE_KEY...) + registry.revokeCredential(...) call.
  Keep both EXPORT names and arg signatures byte-stable (studentService imports them at L6).</action>
  <verify>
    <automated>cd privdId_admin/backend && node --check services/safeService.js && node --check services/credentialService.js && grep -c "proposeSafeTransaction\|proposeRegistryWrite\|getPendingTransactions\|confirmSignature\|executeTransaction" services/safeService.js</automated>
  </verify>
  <acceptance_criteria>
    - node --check passes for both services/safeService.js and services/credentialService.js
    - safeService.js exports proposeSafeTransaction, proposeRegistryWrite, confirmSignature, executeTransaction, getPendingTransactions (grep each)
    - proposeRegistryWrite delegates to proposeSafeTransaction (grep proposeSafeTransaction inside the proposeRegistryWrite body — the Safe.init/createTransaction/apiKit.proposeTransaction logic appears ONCE, inside proposeSafeTransaction)
    - executeTransaction returns the on-chain txHash + blockNumber from the receipt (so 09-03 can persist terminal state) — grep the executeTransaction return for txHash/blockNumber (or the receipt fields they derive from)
    - safeService.js contains NO EthersAdapter / Safe.create( reference
    - credentialService.js issueCredentialOnChain still pins to IPFS to obtain cid, then calls proposeRegistryWrite('issueCredential', [...]) and returns an object containing cid AND safeTxHash AND status (grep "return" near issueCredentialOnChain shows safeTxHash + cid; NO txHash/blockNumber returned from the issue path)
    - credentialService.js revokeCredentialOnChain calls proposeRegistryWrite('revokeCredential', [rollNo]) and returns { safeTxHash, status }
    - No direct registry write remains reachable from issue/revoke: grep -v '^[[:space:]]*//' services/credentialService.js | grep -c "registry.issueCredential\|registry.revokeCredential" returns 0
    - credentialService.js still EXPORTS issueCredentialOnChain and revokeCredentialOnChain with unchanged arg names (grep the export signatures)
    - safeService.js computes awaiting from getSafeInfo threshold, not a confirmationsRequired field on the tx (grep getSafeInfo)
  </acceptance_criteria>
  <done>safeService.js wraps a generic proposeSafeTransaction primitive plus proposeRegistryWrite/confirm/execute/getPending; credentialService.js's EXPORTED issue/revoke functions are restructured to pin-then-propose with the new {cid,safeTxHash,status} / {safeTxHash,status} return shapes and no direct PRIVATE_KEY write.</done>
</task>

<task type="auto">
  <name>Task 3a: pendingRegistryAction on Student model + studentService ISSUE-path propose-only handling (all 3 issue call-sites)</name>
  <files>privdId_admin/backend/models/Student.js, privdId_admin/backend/services/studentService.js</files>
  <read_first>
    - privdId_admin/backend/models/Student.js (existing status-field conventions: enrollmentPhase L62-66, onChainTxHash/onChainBlock/anchorPending/lastAnchorError L67-82, revoked/revokedAt L83-90)
    - privdId_admin/backend/services/studentService.js (import L6; createStudent L120 issue L142-157 — note `student.dek = dek.toString('base64')` at L145 before the unconditional `await student.save()`; insertBulkStudents L175 bulk issue L190-213 — note the per-student `Student.updateOne({ _id }, { ciphertextCID, dek: dek.toString('base64'), onChainTxHash, onChainBlock, anchorPending, lastAnchorError })` at L194-197; the re-issue path used by updateStudentById at L292-303 — note DEK is REUSED from student.dek via .select('+dek'), never re-assigned)
    - .planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md (Student.js MODIFIED section + studentService.js MODIFIED section — exact field shape and adaptation)
  </read_first>
  <action>MODIFY models/Student.js: add a pendingRegistryAction field as a subdocument shaped { safeTxHash: { type: String, default: null }, type: { type: String, enum: ["issue", "revoke"], default: null } }, following the existing defaulting convention of the surrounding status fields.
  MODIFY services/studentService.js at ALL THREE issue call-sites — createStudent (L144), insertBulkStudents bulk path (L193), and the re-issue path used by updateStudentById (L298): change each destructure from `{ cid, txHash, blockNumber }` to `{ cid, safeTxHash }` (issueCredentialOnChain now returns {cid, safeTxHash, status} per Task 2), set student.ciphertextCID = cid (keep this — the CID is still produced and needed), set student.pendingRegistryAction = { safeTxHash, type: 'issue' }, and STOP setting onChainTxHash/onChainBlock and any terminal "issued" flag on propose (those are deferred to the execute-confirmation path in 09-03). Reuse the existing try/catch + AppError shape for the propose call, keeping the message intent but reflecting propose semantics.

  CRITICAL — DEK persistence MUST survive at every issue site (dropping it reproduces the known prior live bug where a bulk-issued student, e.g. 22BCSD01, had a null DEK after anchoring, which blocked credential claim):
  - createStudent (L142-157): the line `student.dek = dek.toString('base64')` (currently L145) MUST REMAIN — it is set on the Mongoose instance and persisted by the subsequent `await student.save()`. Only remove the `student.onChainTxHash = txHash` / `student.onChainBlock = blockNumber` assignments; KEEP `student.dek`, KEEP `student.ciphertextCID`, and ADD `student.pendingRegistryAction = { safeTxHash, type: 'issue' }`.
  - insertBulkStudents bulk path (L194-197): the per-student `Student.updateOne({ _id }, { ... })` payload currently writes `{ ciphertextCID, dek: dek.toString('base64'), onChainTxHash, onChainBlock, anchorPending, lastAnchorError }`. `Student.updateOne` writes ONLY the fields explicitly listed in this payload object — there is NO implicit carry-over, so any omitted field is silently NOT persisted. You MUST keep `dek: dek.toString('base64')` in the updateOne payload exactly as today, and keep `ciphertextCID`; only swap the `onChainTxHash`/`onChainBlock` fields for `pendingRegistryAction: { safeTxHash, type: 'issue' }`. Likewise preserve the companion in-memory `student.ciphertextCID` assignment after the updateOne (and drop the in-memory onChainTxHash/onChainBlock mirrors).
  - updateStudentById re-issue path (L292-303): this path REUSES the existing `student.dek` (selected via .select('+dek'), never rotated — D-04/D-05) and saves via `await student.save()`. Do NOT add, remove, or reassign any dek here — just swap onChainTxHash/onChainBlock for `student.pendingRegistryAction = { safeTxHash, type: 'issue' }`.</action>
  <verify>
    <automated>cd privdId_admin/backend && node --check models/Student.js && node --check services/studentService.js && grep -c "pendingRegistryAction" models/Student.js && grep -c "pendingRegistryAction.*issue\|type: 'issue'\|type: \"issue\"" services/studentService.js && grep -A8 "Student.updateOne" services/studentService.js | grep -c "dek:"</automated>
  </verify>
  <acceptance_criteria>
    - node --check passes for models/Student.js and services/studentService.js
    - grep -c "pendingRegistryAction" models/Student.js returns >= 1; the enum is exactly ["issue", "revoke"]
    - createStudent (around L144): destructures safeTxHash from issueCredentialOnChain, sets student.ciphertextCID and student.pendingRegistryAction = { ..., type: 'issue' }, and no longer sets onChainTxHash/onChainBlock on propose — verify by reading the createStudent issue branch
    - DEK PRESERVED in createStudent: the `student.dek = dek.toString('base64')` line (around L145) is STILL present in the createStudent issue branch after the edit — verify by reading the branch and confirming `student.dek =` remains before the `await student.save()`
    - insertBulkStudents bulk path (around L193): the per-student handling AND the Mongo updateOne write ciphertextCID + pendingRegistryAction(type:'issue') and no longer onChainTxHash/onChainBlock — verify by reading the insertBulkStudents loop
    - DEK PRESERVED in bulk path: the bulk-issue `Student.updateOne` payload STILL contains `dek: dek.toString('base64')` (NOT dropped when onChainTxHash/onChainBlock were swapped out) — automated check `grep -A8 "Student.updateOne" services/studentService.js | grep -c "dek:"` returns >= 1, AND verify by reading the bulk updateOne payload that the `dek:` key is in the SAME object as ciphertextCID and pendingRegistryAction
    - updateStudentById re-issue path (around L298): destructures safeTxHash, sets ciphertextCID + pendingRegistryAction(type:'issue'), no terminal flip, and does NOT add/remove/reassign student.dek (the reused DEK is left untouched) — verify by reading that branch
    - grep "pendingRegistryAction" services/studentService.js matches at a MINIMUM 3 distinct issue locations (createStudent, insertBulkStudents, updateStudentById re-issue path)
  </acceptance_criteria>
  <done>Student has a pendingRegistryAction field; all 3 studentService issue call-sites record pendingRegistryAction(type:'issue') + ciphertextCID, PRESERVE DEK persistence (createStudent's student.dek, the bulk updateOne dek field, and updateStudentById's reused DEK), and defer terminal issued state to execution.</done>
</task>

<task type="auto">
  <name>Task 3b: studentService REVOKE-path propose-only handling</name>
  <files>privdId_admin/backend/services/studentService.js</files>
  <read_first>
    - privdId_admin/backend/services/studentService.js (revokeStudent L312-330: try/catch around revokeCredentialOnChain at L319 then the student.revoked = true terminal flip)
    - .planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md (studentService.js MODIFIED revoke section)
  </read_first>
  <action>MODIFY services/studentService.js revokeStudent (L312-330): capture the { safeTxHash, status } now returned by revokeCredentialOnChain (Task 2) and set student.pendingRegistryAction = { safeTxHash, type: 'revoke' }. Do NOT set student.revoked = true on propose — leave the terminal revoked state (and revokedAt) untouched until the execute confirmation path (wired in 09-03) flips it. Reuse the existing try { await revokeCredentialOnChain(...) } catch (err) { console.error("[credential] ... failed for", student.rollNo, ":", err.message); throw new AppError("On-chain revocation failed: ...", ...) } shape, keeping the message intent but reflecting propose semantics.</action>
  <verify>
    <automated>cd privdId_admin/backend && node --check services/studentService.js && grep -c "type: 'revoke'\|type: \"revoke\"" services/studentService.js</automated>
  </verify>
  <acceptance_criteria>
    - node --check passes for services/studentService.js
    - revokeStudent (L312-330) sets student.pendingRegistryAction = { safeTxHash, type: 'revoke' } from revokeCredentialOnChain's return
    - revokeStudent no longer unconditionally sets student.revoked = true on propose: the student.revoked = true assignment is removed from the propose path (gated to the execute-confirmation path) — verify by reading revokeStudent
    - grep "type: 'revoke'" (or "revoke" within a pendingRegistryAction assignment) matches in revokeStudent
  </acceptance_criteria>
  <done>revokeStudent records pendingRegistryAction(type:'revoke') on propose and defers the terminal revoked flip to execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| studentService -> credentialService -> safeService | Issue/revoke origination; must propose, never directly sign with a stored key |
| safeService -> Safe Transaction Service | Off-chain signature aggregation; backend relays caller signatures |
| backend env (PRIVATE_KEY / SAFE_OWNER_*) | Deployer key only; official keys must never live here (D-01/D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | safeService signing issue/revoke with a backend-held key, bypassing 2-of-3 | mitigate | proposeSafeTransaction proposes only; official signatures are MetaMask-relayed (D-01). process.env.PRIVATE_KEY is never used for official signing. Acceptance criteria assert no direct registry.issueCredential/revokeCredential write remains. |
| T-09-06 | Information Disclosure | local-dev raw-signer fallback accidentally activating against Sepolia and leaking/abusing a key | mitigate | USE_LOCAL_SIGNERS flag gates the raw-key path; disabled when NODE_ENV=production. |
| T-09-07 | Tampering | duplicate propose on double-issue/double-revoke creating conflicting pending txs | mitigate | Safe nonce model makes identical-payload proposes idempotent (same safeTxHash); studentService should skip re-proposing when a pendingRegistryAction already exists for the student (UX guard). |
| T-09-08 | Repudiation | encoding wrong call data so a tx proposes successfully but reverts on execute (RESEARCH Pitfall 5) | mitigate | Reuse registryArtifact.abi via ethers.Interface.encodeFunctionData with the exact issueCredential/revokeCredential arg order; no hand-built call data. The shared proposeSafeTransaction primitive is the single encoding/propose path, so acceptAdmin (09-05) uses the same verified mechanism. |
| T-09-14 | Tampering / Data Integrity | propose-only refactor silently dropping DEK persistence at a bulk/single issue site, leaving a null DEK that blocks credential claim (known prior live bug, 22BCSD01) | mitigate | DEK persistence is explicitly preserved at every issue site (createStudent student.dek, bulk updateOne dek field, updateStudentById reused DEK). Task 3a acceptance criteria include a grep assertion that the bulk updateOne payload still contains `dek:` and a read-verify that createStudent's student.dek assignment survives. |
</threat_model>

<verification>
- node --check clean on safeService.js, credentialService.js, Student.js, studentService.js.
- safeService exports the 5 wrappers (incl. generic proposeSafeTransaction); proposeRegistryWrite delegates to it; no v1 Safe API; no direct registry write left in credentialService.
- All 3 issue call-sites + the revoke call-site record pendingRegistryAction; terminal state deferred to execute.
- DEK persistence survives at all issue sites: createStudent student.dek, bulk updateOne dek field, updateStudentById reused DEK.
</verification>

<success_criteria>
- GOV-03: safeService.js wraps propose/confirm/execute and is the path issue/revoke uses; a generic proposeSafeTransaction primitive enables arbitrary Safe-call proposals (reused by the 09-05 acceptAdmin handoff).
- GOV-02 (off-chain half): no single backend key can mutate registry state; all writes go through a Safe proposal.
- D-12 2-phase lifecycle recorded via pendingRegistryAction at every issue/revoke call-site, with no regression to DEK persistence.
</success_criteria>

<output>
Create `.planning/phases/09-multisig-registry-governance-e5/09-02-SUMMARY.md` when done.
</output>
</output>
</output>
</content>
</invoke>
