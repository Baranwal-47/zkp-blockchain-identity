---
phase: 09-multisig-registry-governance-e5
plan: 05
type: execute
wave: 5
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - zk-proofs/scripts/generateSafeOwners.js
  - privdId_admin/backend/.env.example
autonomous: false
requirements: [GOV-04]
user_setup:
  - service: metamask
    why: "Three official owner accounts must sign Safe transactions in the browser (D-01/D-11)"
    dashboard_config:
      - task: "Import the 3 generated throwaway owner private keys as separate MetaMask accounts (AcadAdmin, Asst. Registrar, Dean)"
        location: "MetaMask -> Import Account, once per owner key"
      - task: "Fund the deployer EOA and each owner account with Sepolia test ETH"
        location: "A Sepolia faucet"

must_haves:
  truths:
    - "3 brand-new throwaway owner keypairs are generated (not the operator's personal MetaMask accounts, not the deployer EOA)"
    - "A real Safe 2-of-3 (those 3 owners, threshold 2) is deployed on Sepolia and resolvable via the Safe Transaction Service / Etherscan"
    - "The registry admin is transferred to the Sepolia Safe via transferAdmin + acceptAdmin (the first real Safe-governed transaction)"
    - "A full propose -> 2 MetaMask signatures -> manual execute cycle for an issue (or revoke) succeeds on Sepolia through the Pending Approvals UI"
    - "Direct issue/revoke from the deployer EOA reverts after the handoff"
  artifacts:
    - path: "zk-proofs/scripts/generateSafeOwners.js"
      provides: "Generates 3 fresh keypairs and prints addresses for env + private keys for MetaMask import"
      contains: "Wallet.createRandom"
  key_links:
    - from: "Pending Approvals UI"
      to: "Sepolia CredentialRegistry via Safe"
      via: "propose/sign x2/execute"
      pattern: "execute"
---

<objective>
Stand up the real Safe 2-of-3 on Sepolia and verify the end-to-end manual-signing UX there (D-11): generate 3 fresh throwaway owner keypairs (D-02), deploy the Safe, hand the registry admin to it, and walk through a real propose -> 2 MetaMask signatures -> manual execute cycle via the Pending Approvals screen. This completes GOV-04's Sepolia requirement and proves GOV-02 against a live Safe.

Purpose: Sepolia is the only environment where the manual MetaMask multi-account signing UX actually exercises (api-kit + real Safe Transaction Service); local Hardhat (plan 09-01) covered the protocol-kit-only half.
Output: a key-generation helper script; populated Sepolia env values; a verified live multisig issue/revoke cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-multisig-registry-governance-e5/09-CONTEXT.md
@.planning/phases/09-multisig-registry-governance-e5/09-RESEARCH.md
@.planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md

<constraints>
- D-02: the 3 Safe owners are brand-new THROWAWAY keypairs — NOT the operator's personal MetaMask accounts, NOT the deployer EOA. Generate them fresh for Sepolia.
- D-03: the existing/current wallet is the DEPLOYER EOA only — it deploys the Safe and calls transferAdmin, then is never an owner and never signs issue/revoke again.
- D-11: Sepolia uses the FULL api-kit-backed propose/confirm/execute + Pending Approvals UI (unlike local Hardhat's protocol-kit-only path). Safe Transaction Service for Sepolia is live (RESEARCH Environment Availability — api.safe.global/tx-service/sep returned 200).
- D-06: execute stays a deliberate manual button click even after 2/2 signatures — verify it does NOT auto-fire.
- deploySafe.js (from 09-01) is reused for the Sepolia deploy — pass the 3 fresh owner addresses and run against the Sepolia network/RPC (SEPOLIA_RPC_URL already configured, confirmed live). Do NOT rewrite deploySafe.js's core; only its owner inputs and network differ.
- Real private keys must NEVER be committed. generateSafeOwners.js prints keys to stdout for one-time MetaMask import; they go into a gitignored .env / MetaMask, not into .env.example (which keeps placeholders only).
- The frozen circuit/verifier remain untouched.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generateSafeOwners.js — 3 fresh throwaway keypairs</name>
  <files>zk-proofs/scripts/generateSafeOwners.js</files>
  <read_first>
    - zk-proofs/scripts/deployRegistry.js (CJS script shape / console.log conventions)
    - .planning/phases/09-multisig-registry-governance-e5/09-CONTEXT.md (D-02 throwaway keypairs, D-03 deployer separation)
  </read_first>
  <action>Create zk-proofs/scripts/generateSafeOwners.js (CJS Hardhat/Node script) that generates 3 fresh keypairs via ethers Wallet.createRandom() — one per role (AcadAdmin, Asst. Registrar, Dean) — and prints, for each, the role label, the address (for SAFE_OWNER_* env + VITE_SAFE_OWNER_* frontend env), and the private key (for one-time MetaMask import). Print a clear warning that these are throwaway Sepolia-only keys, must NOT be committed, and that the deployer EOA stays separate (D-03). Output format should be copy-pasteable into .env. Do NOT write any file containing the private keys; stdout only.</action>
  <verify>
    <automated>node -e "require('fs').accessSync('zk-proofs/scripts/generateSafeOwners.js')" && grep -c "createRandom\|SAFE_OWNER" zk-proofs/scripts/generateSafeOwners.js</automated>
  </verify>
  <acceptance_criteria>
    - File zk-proofs/scripts/generateSafeOwners.js exists
    - Uses ethers Wallet.createRandom() to generate 3 keypairs (grep createRandom)
    - Prints SAFE_OWNER_ACADADMIN / SAFE_OWNER_REGISTRAR / SAFE_OWNER_DEAN address lines
    - Does NOT write private keys to any file (no fs.writeFileSync of key material; grep -c writeFileSync returns 0 OR only writes non-key content)
    - Contains a warning that keys are throwaway and must not be committed
  </acceptance_criteria>
  <done>generateSafeOwners.js prints 3 fresh owner addresses + private keys to stdout with a no-commit warning.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Generate owners, fund accounts, import to MetaMask</name>
  <what-built>
    generateSafeOwners.js (Task 1) is ready to produce the 3 throwaway owner keypairs. deploySafe.js (plan 09-01) is ready to deploy the Safe on Sepolia.
  </what-built>
  <how-to-verify>
    1. Run: cd zk-proofs && node scripts/generateSafeOwners.js — copy the 3 addresses into backend .env (SAFE_OWNER_ACADADMIN/REGISTRAR/DEAN) and frontend env (VITE_SAFE_OWNER_*).
    2. Import each of the 3 private keys as a separate MetaMask account, labeled AcadAdmin / Asst. Registrar / Dean.
    3. Fund the DEPLOYER EOA and the 3 owner accounts with Sepolia test ETH from a faucet (each needs gas; the deployer needs enough for Safe deploy + transferAdmin).
    4. Confirm the deployer EOA is NOT one of the 3 owners (D-03).
  </how-to-verify>
  <action>This step is human-only: generating keys is automatable (Task 1) but importing private keys into the MetaMask browser extension and obtaining faucet ETH have no CLI/API path. After completion the operator has 4 funded Sepolia accounts (1 deployer + 3 owners) and the owner addresses in env.</action>
  <verify>
    <human-check>Operator confirms: 3 owner accounts imported in MetaMask, deployer + 3 owners funded on Sepolia, owner addresses written to backend + frontend env, deployer is not an owner.</human-check>
  </verify>
  <resume-signal>Type "owners ready" with the 3 owner addresses, or describe any blocker (faucet, funding).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Deploy Sepolia Safe, hand off admin, verify live 2-of-3 cycle</name>
  <what-built>
    deploySafe.js (09-01), safeService.js + /api/safe routes (09-02/09-03), and the Pending Approvals UI (09-04) are all in place. Sepolia Safe Transaction Service is live (RESEARCH confirmed 200).
  </what-built>
  <how-to-verify>
    1. Deploy the Safe on Sepolia: cd zk-proofs && npx hardhat run scripts/deploySafe.js --network sepolia (with the 3 fresh owner addresses, threshold 2, deployer EOA as signer). Copy the printed SAFE_ADDRESS into backend .env (SAFE_ADDRESS) and frontend env. The script also calls registry.transferAdmin(SAFE_ADDRESS).
    2. Complete the handoff: via the Pending Approvals flow, propose acceptAdmin() as a Safe transaction, sign with 2 of the 3 owner MetaMask accounts, and manually Execute — this is the FIRST real Safe-governed transaction. Confirm registry.admin() now equals the Safe address (Etherscan read or a quick script).
    3. Verify the Safe resolves on the Safe Transaction Service / Etherscan (3 owners, threshold 2).
    4. Issue (or revoke) a test student through the admin flow -> confirm it appears in Pending Approvals as "Awaiting 1 more signature" after the proposer's signature.
    5. Sign with a 2nd owner account -> card flips to "Ready to execute — 2 of 2 signed"; confirm Execute did NOT auto-fire (D-06).
    6. Click Execute, confirm the window.confirm prompt, and verify the on-chain tx succeeds and the student's pendingRegistryAction clears to the terminal issued/revoked state.
    7. Negative check: attempt issueCredential/revokeCredential directly from the deployer EOA (script or Etherscan) -> reverts with onlyAdmin (GOV-02).
  </how-to-verify>
  <action>Operator runs the Sepolia deployment and performs the real multi-account MetaMask walkthrough. This is the GOV-04 dual-requirement's Sepolia half and the D-11 live verification. Capture the SAFE_ADDRESS, the handoff tx hash, and the issue/revoke execute tx hash in the SUMMARY.</action>
  <verify>
    <human-check>Operator confirms: Safe deployed on Sepolia (3 owners / threshold 2, resolvable on Etherscan/Tx Service); registry.admin() == Safe address; a full propose->2 signatures->manual execute issue/revoke cycle succeeded; Execute did not auto-fire; direct deployer-EOA issue/revoke reverts.</human-check>
  </verify>
  <resume-signal>Type "verified" with the SAFE_ADDRESS + execute tx hash, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployer EOA -> Sepolia Safe | One-time handoff; deployer must not be an owner |
| 3 owner MetaMask accounts -> Safe Transaction Service | Real ECDSA signatures over the Safe tx EIP-712 hash |
| generated private keys -> filesystem/git | Throwaway keys must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-18 | Information Disclosure | committing the 3 throwaway owner private keys | mitigate | generateSafeOwners.js prints to stdout only, never writes a key file; keys go to MetaMask + gitignored .env; .env.example stays placeholder-only. |
| T-09-19 | Elevation of Privilege | deployer EOA accidentally added as a Safe owner, retaining unilateral influence | mitigate | D-03 separation enforced in Task 2 human-check (deployer is not an owner); Safe is strictly 2-of-3 of the fresh keys. |
| T-09-20 | Tampering / Front-running | acceptAdmin replay/front-run during handoff | mitigate | acceptAdmin only succeeds for msg.sender == pendingAdmin (the Safe); the call is itself a 2-of-3 Safe tx — no single actor can complete or hijack it. |
| T-09-21 | Elevation of Privilege | Execute auto-firing on the 2nd Sepolia signature (D-06 violation) | mitigate | Task 3 step 5 explicitly verifies Execute does not auto-fire and requires the manual confirm. |
| T-09-22 | Spoofing | a single official's wallet mutating registry state alone (GOV-02) | mitigate | Task 3 step 7 negative check: direct deployer-EOA / single-owner issue/revoke reverts under onlyAdmin (now the Safe). |
</threat_model>

<verification>
- generateSafeOwners.js generates 3 fresh keypairs, no key written to disk.
- Sepolia Safe deployed (3 owners / threshold 2), registry admin handed off, resolvable on Etherscan/Tx Service.
- Full propose->2 signatures->manual execute issue/revoke cycle verified live; execute not auto-fired; direct EOA write reverts.
</verification>

<success_criteria>
- GOV-04 Sepolia half complete: real Safe 2-of-3 deployed and verifiable; live manual-signing UX exercised end-to-end.
- GOV-02 proven against a live Safe (single-key writes revert).
- D-02/D-03/D-06/D-11 honored.
</success_criteria>

<output>
Create `.planning/phases/09-multisig-registry-governance-e5/09-05-SUMMARY.md` when done — record SAFE_ADDRESS, handoff tx hash, and the issue/revoke execute tx hash.
</output>
