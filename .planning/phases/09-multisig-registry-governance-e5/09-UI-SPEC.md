---
phase: 9
slug: multisig-registry-governance-e5
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-21
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for: Per-Role Login screen, Pending Approvals screen (MetaMask connect + sign + execute), and the Dashboard's read-only pending-status indicator. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

Surface: `privdId_admin/frontend` (React 19 + Vite + Tailwind, **no component library/shadcn**). All tokens below are extracted verbatim from the existing shipped pages (`LoginPage.jsx`, `Layout.jsx`, `DashboardPage.jsx`, `StudentsTable.jsx`) and the shared utility classes in `src/index.css`. This phase introduces **zero new colors, sizes, or spacing values** — it reuses the existing dark zinc/blue admin theme exactly, including for the 3 new UI surfaces (Per-Role Login, Pending Approvals, Dashboard pending-status indicator).

**Primary visual anchor — Pending Approvals screen:** the pending-transaction card list (Sign/Execute action cards) is the focal point of the screen; the "Signing as: {Role name}" identity banner (D-04) is secondary context displayed above the list, establishing identity before the user acts but not competing with the cards for visual weight.

Note on `CLAUDE.md`'s "light modern indigo/neutral theme" line: the **already-shipped** `privdId_admin/frontend` is dark (zinc-900/black background, `#3b82f6` blue accent) — this is the live, working design system for this surface, not a future-state token system. Per the agent brief's instruction to "read the project's existing admin frontend design system... and reuse them," this contract follows what is actually built rather than retrofitting an unbuilt theme. Flag for a future `UI-01` token-system pass, not a Phase 9 concern.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — no `components.json`, no shadcn/Radix; hand-rolled Tailwind utility classes (`.panel`, `.field-input`, `.primary-button`, etc. in `src/index.css`) |
| Preset | not applicable |
| Component library | none — plain `<form>`/`<input>`/`<button>`/`<table>` elements styled via Tailwind `@apply` component classes |
| Icon library | none installed — existing screens use plain text/emoji-free labels and colored pill badges for status, no icon glyphs anywhere in this codebase; follow this convention, do not introduce an icon library this phase |
| Font | Inter (loaded via `:root` font-family stack in `src/index.css`), `ui-sans-serif`/system fallback |
| Wallet connection | `ethers` v6 `BrowserProvider` + `window.ethereum` (MetaMask) — no `wagmi`/`web3modal` exists in this codebase yet; this phase introduces the browser MetaMask connection per D-08, keep it to plain `ethers` `BrowserProvider.send("eth_requestAccounts")` to match the project's existing `ethers`-only pattern (no new wallet-connection library) |

Registry safety gate: **not applicable** — no shadcn/component registry in this stack.

---

## Spacing Scale

Source: extracted from `Layout.jsx` / `StudentsTable.jsx` / `index.css` (`.panel`, `.panel-soft`). All values already multiples of 4 or 8.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-free inline gaps (none currently used, reserved) |
| sm | 8px | Button/nav-link internal gaps (`gap-2`), badge horizontal padding (`px-3 py-1` pill badges) |
| md | 16px | `.panel`/`.panel-soft` padding base (`p-5` ≈ 20px rounds to this tier), form field gaps (`gap-5` in `StudentForm.jsx` is the established field-grid gap) |
| lg | 24px | Page-level `space-y-6` between stacked sections (Dashboard, Pending Approvals list) |
| xl | 32px | Layout-level outer padding on wide breakpoints (`lg:px-8` in `Layout.jsx`) |
| 2xl | 48px | reserved — no current usage |
| 3xl | 64px | reserved — no current usage |

Exceptions for this phase:
- **44px minimum touch target** on the "Execute" button (D-06) and each per-role login's password submit button — this is the single most consequence-bearing action in the phase (irreversible on-chain write), so it gets the accessibility-floor touch target even though the rest of the admin portal's buttons (`py-2.5`/`py-3`) already clear this informally.
- No other spacing exceptions — Pending Approvals list rows reuse `StudentsTable.jsx`'s exact row pattern (`rounded-2xl bg-white/5`, `px-4 py-4` cells).

---

## Typography

Source: extracted from `Layout.jsx` / `LoginPage.jsx` / `DashboardPage.jsx`. This phase's contract declares exactly 4 sizes + **2 weights** (400, 600) — no new sizes/weights introduced for the 3 new surfaces.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Display (page heading, e.g. "Pending Approvals", "Official sign-in") | 24px (`text-2xl`, matches `LoginPage.jsx` h1 / `Layout.jsx` mobile h1) | 600 (semibold, `font-semibold`) | 1.2 |
| Heading (panel/card heading, e.g. "Revoke — 22BCSD01") | 18-20px (`text-lg`/`text-xl`, matches `StudentsTable.jsx` "Students" heading) | 600 (semibold) | 1.2 |
| Body (descriptions, signature-status text, helper copy) | 14px (`text-sm`, matches `Layout.jsx` subtitle / `StudentsTable.jsx` row text) | 400 (regular) | 1.5 |
| Label / meta (eyebrow text, table headers, status pill text) | 12px (`text-xs`, matches `uppercase tracking-[0.35em]`/`tracking-[0.25em]` eyebrow + table header pattern) | 600 (semibold) — use the same semibold weight as Display/Heading, not a separate tier, for any *new* label/meta text this phase introduces | 1.4 |

Notes:
- Exactly 2 weights total for this phase's contract: 400 (regular body) and 600 (semibold — headings, buttons, and new label/meta text alike: `.primary-button`, `.secondary-button`, `h1`/`h3` all use `font-semibold`).
- **Pre-existing, unchanged, not counted:** the shipped `.field-label`/`.nav-link` classes use `font-medium` (500) in the already-built codebase (e.g. form field labels on `StudentForm.jsx`). This phase does not touch, restyle, or extend those classes — they are out of scope, carried forward as-is, and excluded from this contract's 2-weight count, same exception pattern as the spacing scale's 44px touch-target note above. If this phase's new label/meta text (table headers, status pill text, eyebrow on new screens) needs a non-400 weight, use 600 — do not reach for `.field-label`'s 500.
- "Who is signing as" identity banner (D-04) uses the Heading role (18-20px, 600) for the official's name/role, with Label role (12px uppercase tracking, 600) for the role badge — mirrors the existing eyebrow ("PrivdId Admin") + h1 pairing already used on every page.

---

## Color

Source: extracted from `index.css`, `Layout.jsx`, `StudentsTable.jsx`, `DashboardPage.jsx`. No new colors introduced — this phase reuses the existing dark zinc/blue palette exactly, including its existing 3-color status-pill convention (emerald/amber/red).

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#000000` (body background, `index.css` `body { background: #000000 }`) | Page background on all 3 new surfaces (Per-Role Login, Pending Approvals, Dashboard) |
| Secondary (30%) | `bg-zinc-900/85` (`.panel`) and `bg-zinc-800/60` (`.panel-soft`), both at `border-white/10` | Login form card, each pending-transaction card in Pending Approvals list, the new pending-status summary card on Dashboard |
| Accent (10%) | `#3b82f6` (`bg-blue-600`/`text-blue-400`, the existing `.primary-button` bg and `Edit` link color) | Reserved EXCLUSIVELY for: the "Sign" button (per-role login → MetaMask signature action), the focus ring on the password input (`focus:border-blue-500`, matches `LoginPage.jsx` verbatim), and the connected-wallet-address chip background when address matches expected owner |
| Destructive | `#ef4444` (`text-red-400`/`bg-red-400/15`, the existing `Revoke` button and "Revoked" status pill) | Reserved EXCLUSIVELY for: the "Execute" button (D-06 — this is the irreversible on-chain mutation), the address-mismatch warning (D-09, when connected MetaMask address ≠ expected owner for the logged-in role), and any "Reject"/"Cancel proposal" action if one exists |

Accent reserved for: the Sign button, password-input focus ring, and a matched-wallet-address chip. Never apply accent to body text, card backgrounds, or the Execute button (Execute is destructive-red, not accent-blue — these are two distinct actions and must not share a color, per D-06's "deliberate, not auto-fired" requirement: a color swap from blue→red between Sign and Execute is itself part of the "this is the consequential step" signal).

Existing semantic colors reused unchanged (status pill convention from `StudentsTable.jsx` L110):
- `bg-emerald-400/15 text-emerald-200` — "Executed" / "2-of-2 signed" success state (mirrors existing `emailSent` pill)
- `bg-amber-400/15 text-amber-200` — "Awaiting 1 more signature" pending state (mirrors existing `Pending` pill) — this is the literal copy/color D-07's dashboard indicator must use
- `bg-red-400/15 text-red-200` — "Revoked"/failed-execution state (mirrors existing `revoked` pill)
- `text-slate-400` / `text-slate-300` — secondary/muted body and helper text (unchanged)
- `text-zinc-400` uppercase tracking eyebrow — page/section super-headers (unchanged, "PrivdId Admin" pattern)

IIITDM maroon: **not used this phase** — consistent with Phase 8's precedent; blue remains the sole established accent, maroon stays an unwired optional future token.

---

## Copywriting Contract

Source: `09-CONTEXT.md` D-04 through D-09 + existing copy conventions from `LoginPage.jsx`/`StudentsTable.jsx`/`DashboardPage.jsx`. Quoted where CONTEXT.md specifies exact behavior; "new — derived" where this phase originates copy not given verbatim.

### Per-Role Login (D-09)

| Element | Copy |
|---------|------|
| Eyebrow | "PrivdId Admin" (reuse exact existing eyebrow text/style) |
| Screen heading | "Official sign-in" (new — derived, distinguishes from the existing student-registry "Admin sign in") |
| Role selector (new — derived) | A 3-option choice — "Academic Admin" / "Assistant Registrar" / "Dean" — presented as a simple select or 3 buttons above the password field; selecting one sets which fixed account (`acadadmin`/`registrar`/`dean`) is being authenticated |
| Password field label | "Password" (reuse exact `LoginPage.jsx` pattern, swap "Admin password" → "Password" since role is already selected) |
| Primary CTA | "Continue" (new — derived; reserve "Sign in" for the existing student-registry login to avoid copy collision between the two distinct login surfaces) |
| Error state (wrong password) | "Incorrect password for this role. Try again." (new — derived, mirrors `LoginPage.jsx`'s toast-based error pattern via `react-hot-toast`) |
| Error state (no role selected) | "Select your role first." (new — derived, inline validation, not a toast) |

### Pending Approvals (D-05, D-06)

| Element | Copy |
|---------|------|
| Screen heading | "Pending Approvals" |
| "Who is signing as" identity banner (D-04, new — derived) | "Signing as: {Role name}" — persistent banner at top of screen, Heading-role text, shown immediately after role login succeeds and before/after MetaMask connect |
| Wallet-not-connected state | "Connect MetaMask to review pending transactions." + "Connect Wallet" button (new — derived) |
| Wallet-connected, address mismatch (D-09, new — derived) | "Connected wallet does not match the expected address for {Role name}. Switch accounts in MetaMask and reconnect." — destructive-red banner, blocks signing until resolved |
| Empty state (no pending transactions) | "No pending approvals." / "All registry actions are currently signed and executed, or none have been proposed yet." (new — derived, follows existing `StudentsTable.jsx` empty-state two-line pattern: heading + explanatory body) |
| Pending transaction card heading (new — derived) | "{Issue / Revoke} — {student rollNo or identifier}" e.g. "Revoke — 22BCSD01" |
| Signature progress (new — derived, mirrors D-07's exact dashboard wording) | "Awaiting {N} more signature{s}" (amber pill) or "Ready to execute — 2 of 2 signed" (emerald pill) |
| Primary CTA, before threshold met | "Sign" (accent blue) — triggers MetaMask signature request for this official's confirmation |
| Already-signed-by-this-official state (new — derived) | "You already signed this." — button disabled, replaced with a static "Signed ✓"-style label (no checkmark glyph per no-icon-library convention; use text "Signed" in emerald) |
| Primary CTA, after threshold met (D-06 — explicit manual step, must not auto-fire) | "Execute" (destructive red) — separate, deliberate action distinct from "Sign"; never auto-triggered the instant the 2nd signature lands |
| Execute confirmation (D-06, destructive-action pattern) | Reuse the existing `window.confirm`-style pattern from `DashboardPage.jsx`'s `handleRevoke`: "Execute this {issue/revoke} transaction now? This will submit it on-chain and cannot be undone." |
| Execute success | toast: "Transaction executed successfully." (mirrors existing `toast.success` pattern) |
| Execute/Sign error | toast: error message from `getApiErrorMessage(error)` (reuse existing helper verbatim, same as every other page in this codebase) |

### Dashboard pending-status indicator (D-07)

| Element | Copy |
|---------|------|
| Indicator card heading (new — derived) | "Pending Registry Actions" — new `.panel-soft` card alongside the existing 3 stat cards (Total students / Emails sent / Programmes), or a 4th stat-style card if a single count suffices |
| Per-item line (D-07, copy given verbatim in CONTEXT.md) | "{Issue/Revoke} requested for {rollNo}, awaiting {N} more signature{s}" |
| No pending items | "No pending registry actions." (new — derived, terse since this is a glanceable dashboard widget, not the full Pending Approvals screen) |
| Read-only disclaimer (new — derived, reinforces D-07's "admin doesn't sign here") | small muted text below the list: "View and sign in Pending Approvals." — links/navigates to the Pending Approvals screen, does not embed signing controls inline |

### Destructive actions

| Action | Confirmation copy |
|--------|-------------------|
| Execute (Safe transaction execution, D-06) | "Execute this {issue/revoke} transaction now? This will submit it on-chain and cannot be undone." — `window.confirm`, same pattern as existing `handleRevoke` in `DashboardPage.jsx`. This is the phase's only destructive UI action — Sign is not destructive (it is a proposal/confirmation that does nothing on-chain alone until the 2nd signature + Execute occur). |

---

## Screen-Specific Interaction Notes

- **Per-Role Login → Pending Approvals flow (D-09):** role login and MetaMask connection are two sequential, visually distinct steps on the same screen-flow — role login first (password gate), then a wallet-connect prompt, then address-match validation, then the Pending Approvals list becomes interactive. Do not collapse these into a single combined form; each step's state (logged in as role? wallet connected? address matches?) must be independently visible, matching D-04's "feel like genuinely separate identities" intent.
- **Sign vs Execute color separation (D-06):** Sign uses the existing `.primary-button` blue; Execute must use a visually distinct destructive-red button (new `@apply` class needed, e.g. `.destructive-button` extending `.primary-button`'s shape with `bg-red-600 hover:bg-red-500` in place of `bg-blue-600`) — this is the one new utility class this phase may need to add to `index.css`, following the existing `@layer components` pattern exactly.
- **Pending transaction cards** reuse `StudentsTable.jsx`'s row visual language (`rounded-2xl bg-white/5`, pill-badge status) rather than introducing a new card component — list view, not table, since each entry needs a 2-button action area (Sign/Execute) that doesn't fit a table cell cleanly.
- **Dashboard indicator widget** is read-only by design (D-07) — it must contain no Sign/Execute buttons, only status text + a single navigation link to Pending Approvals, to keep the "admin doesn't sign here" boundary visually unambiguous.

---

## Registry Safety

Not applicable — no shadcn or component registry exists in this Vite/React project. `Tool: none` per the shadcn gate's N-path (no `components.json` found; project uses hand-rolled Tailwind `@apply` classes exclusively).

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
