---
phase: 09-multisig-registry-governance-e5
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - privdId_admin/backend/controllers/adminController.js
  - privdId_admin/backend/controllers/safeController.js
  - privdId_admin/backend/middleware/requireAuth.js
  - privdId_admin/backend/routes/adminRoutes.js
  - privdId_admin/backend/routes/safeRoutes.js
  - privdId_admin/backend/app.js
  - privdId_admin/backend/.env.example
autonomous: true
requirements: [GOV-03, GOV-04]
user_setup:
  - service: env-config
    why: "Per-role official login + Safe owner-address checks"
    env_vars:
      - name: ACADADMIN_PASSWORD
        source: "Operator-chosen password for the Academic Admin role login"
      - name: REGISTRAR_PASSWORD
        source: "Operator-chosen password for the Assistant Registrar role login"
      - name: DEAN_PASSWORD
        source: "Operator-chosen password for the Dean role login"
      - name: SAFE_ADDRESS
        source: "Output of deploySafe.js (09-01 local / 09-05 Sepolia)"
      - name: SAFE_OWNER_ACADADMIN
        source: "Address of the AcadAdmin Safe owner keypair"
      - name: SAFE_OWNER_REGISTRAR
        source: "Address of the Asst. Registrar Safe owner keypair"
      - name: SAFE_OWNER_DEAN
        source: "Address of the Dean Safe owner keypair"

must_haves:
  truths:
    - "POST /api/admin/role-login authenticates one of acadadmin/registrar/dean against its env password and returns a role-scoped JWT"
    - "A requireAuth middleware verifies an Authorization: Bearer <jwt> against JWT_SECRET and rejects missing/invalid tokens with 401"
    - "GET /api/safe/pending returns pending Safe transactions with per-tx signature counts and awaiting counts, behind requireAuth"
    - "POST /api/safe/sign relays a MetaMask-produced signature to the Safe Transaction Service via safeService.confirmSignature, behind requireAuth"
    - "POST /api/safe/execute executes a threshold-met Safe transaction via safeService.executeTransaction (manual, never auto-fired), and on success flips the matching Student out of pendingRegistryAction into the terminal issued/revoked state (D-12), behind requireAuth"
    - "The new /api/safe routes are mounted in app.js, require a valid JWT, and reject malformed safeTxHash/address inputs"
  artifacts:
    - path: "privdId_admin/backend/controllers/adminController.js"
      provides: "roleLogin export"
      exports: ["adminLogin", "roleLogin"]
    - path: "privdId_admin/backend/middleware/requireAuth.js"
      provides: "JWT Bearer verification middleware"
      contains: "jwt.verify"
    - path: "privdId_admin/backend/controllers/safeController.js"
      provides: "getPendingApprovals, signPendingTx, executePendingTx (executePendingTx clears pendingRegistryAction + sets terminal state on success)"
      exports: ["getPendingApprovals", "signPendingTx", "executePendingTx"]
    - path: "privdId_admin/backend/routes/safeRoutes.js"
      provides: "/pending, /sign, /execute routes behind requireAuth"
      contains: "requireAuth"
  key_links:
    - from: "privdId_admin/backend/app.js"
      to: "privdId_admin/backend/routes/safeRoutes.js"
      via: "app.use('/api/safe', safeRoutes)"
      pattern: "/api/safe"
    - from: "privdId_admin/backend/routes/safeRoutes.js"
      to: "privdId_admin/backend/middleware/requireAuth.js"
      via: "router-level requireAuth guard"
      pattern: "requireAuth"
    - from: "privdId_admin/backend/controllers/safeController.js"
      to: "privdId_admin/backend/services/safeService.js"
      via: "service function calls"
      pattern: "safeService\\.(getPendingTransactions|confirmSignature|executeTransaction)"
    - from: "privdId_admin/backend/controllers/safeController.js"
      to: "privdId_admin/backend/models/Student.js"
      via: "executePendingTx looks up Student by pendingRegistryAction.safeTxHash and flips terminal state"
      pattern: "pendingRegistryAction"
---

<objective>
Expose the backend API surface the Pending Approvals UI consumes: a per-role official login (D-09) on the admin controller, a JWT-verification middleware, and `/api/safe/*` routes (pending list, sign relay, execute) wired to `safeService.js` and gated by that middleware. The execute handler also closes the D-12 lifecycle: on a successful Safe execution it maps the safeTxHash back to its Student and flips it from the `pendingRegistryAction` state into the terminal issued/revoked state. This completes GOV-03's HTTP-path requirement and provides the GOV-04 propose/sign/execute control surface.

Purpose: Give the frontend (plan 09-04) the endpoints to log officials in by role and to drive the Safe propose->confirm->execute flow over authenticated routes, and ensure execution is what transitions a student to its terminal on-chain state.
Output: `roleLogin` on adminController; a new `requireAuth.js` middleware; new `safeController.js` (with execute-success student-state-clearing) + `safeRoutes.js` (JWT-gated); `/api/safe` mounted in app.js; `.env.example` documenting the new vars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-multisig-registry-governance-e5/09-CONTEXT.md
@.planning/phases/09-multisig-registry-governance-e5/09-RESEARCH.md
@.planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md

<interfaces>
safeService.js (from plan 09-02) exports — call these from safeController:
- getPendingTransactions() -> [{ safeTxHash, confirmations, signedCount, awaiting, ...txMeta }]
- confirmSignature(safeTxHash, signature, signerAddress) -> result
- executeTransaction(safeTxHash) -> { safeTxHash, txHash, blockNumber, ... } (09-02 Task 2 surfaces the on-chain txHash + blockNumber from the receipt so this plan can persist terminal issue state)

Student model (from 09-02 Task 3a) gains a pendingRegistryAction subdocument: { safeTxHash: String|null, type: 'issue'|'revoke'|null }. On a successful execute, executePendingTx looks up the Student whose pendingRegistryAction.safeTxHash matches the executed safeTxHash and clears it. Existing terminal fields the executor sets: onChainTxHash (String), onChainBlock (Number), revoked (Boolean), revokedAt (Date). (Verified against models/Student.js.)

app.js current shape (verified): imports at L7-9 (studentRoutes, adminRoutes, errorHandler {notFound, errorHandler}); pipeline middleware helmet/cors/json/urlencoded/morgan at L15-24; mounts at L30-31 — app.use("/api/students", studentRoutes); app.use("/api/admin", adminRoutes); then app.use(notFound); app.use(errorHandler) at L33-34.

adminController.js adminLogin shape (verified, PATTERNS adminController section): asyncHandler + AppError + jwt.sign({ role }, process.env.JWT_SECRET || "privid-admin-secret", { expiresIn: "24h" }).

middleware/ currently contains ONLY asyncHandler.js + errorHandler.js (verified). There is NO existing auth/JWT middleware — it does NOT exist and MUST be created in this plan (Task 2). /api/admin and /api/students are completely open today; adminLogin issues a JWT that nothing currently verifies. This plan adds requireAuth and applies it ONLY to the new /api/safe routes; retrofitting /api/admin and /api/students is OUT OF SCOPE (deferred to HARD-01).
</interfaces>

<constraints>
- Per-role login is a LIGHTWEIGHT plaintext-env password compare (D-09) mirroring the existing adminLogin EXACTLY — NOT bcrypt/JWT-session infra (HARD-01 stays deferred). Do NOT "upgrade" it to bcrypt (RESEARCH Security Domain V2 — explicit accepted interim gap). Reuse the existing jwt + JWT_SECRET pattern.
- Role -> env-key map is exactly { acadadmin: 'ACADADMIN_PASSWORD', registrar: 'REGISTRAR_PASSWORD', dean: 'DEAN_PASSWORD' }. Unknown role -> AppError('Select your role first.', 400). Missing env -> AppError(`${role} authentication is not configured.`, 500). Wrong password -> AppError('Incorrect password for this role. Try again.', 401). (PATTERNS roleLogin section — exact copy.)
- requireAuth is NEW: there is no existing auth-guard middleware to reuse (confirmed against middleware/, app.js, routes). It MUST verify the role-login JWT (Authorization: Bearer header) and is applied ONLY to the new /api/safe routes — do NOT retroactively add it to /api/admin or /api/students (out of phase scope; RESEARCH Security Domain V4 commits to JWT-gating /api/safe specifically).
- Mount /api/safe in app.js the SAME way adminRoutes is mounted at /api/admin (add import near L8, add app.use("/api/safe", safeRoutes) near L31, BEFORE the notFound/errorHandler at L33-34). Do NOT reorder existing middleware.
- All new handlers wrap in asyncHandler and throw AppError — never manual res.status().json() error branches (PATTERNS Shared Patterns auth gate). requireAuth itself may call next(new AppError(...)) so the central errorHandler formats the 401.
- Validate safeTxHash/address request bodies as well-formed hex with joi (already a backend dependency) or an explicit regex before passing to safeService (RESEARCH Security Domain V5). safeTxHash is a 0x-prefixed 32-byte hex (66 chars); addresses are 0x-prefixed 20-byte hex (42 chars).
- The execute/sign handlers operate on a safeTxHash the caller supplies; the actual owner-address check happens client-side (plan 09-04) but the route must still require the JWT (Task 2 requireAuth) AND reject malformed input here (RESEARCH Security Domain V4/V5).
- D-12 LIFECYCLE COMPLETION (REQUIRED): per D-12, "Only after execution succeeds does the student status flip to the terminal issued/revoked state." 09-02 sets pendingRegistryAction on propose but NEVER clears it — executePendingTx is the ONLY place that flips a student to terminal state. After safeService.executeTransaction(safeTxHash) succeeds, executePendingTx MUST map the safeTxHash back to its Student (Student.findOne({ 'pendingRegistryAction.safeTxHash': safeTxHash })) and, per pendingRegistryAction.type: for 'issue' set onChainTxHash + onChainBlock from the execution result and clear pendingRegistryAction to null; for 'revoke' set revoked = true + revokedAt = new Date() and clear pendingRegistryAction to null. This is what 09-05 Task 4's live verification checks for.
- Do NOT write a real .env (secrets) — add/update .env.example with the new keys and placeholder values only.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add roleLogin to adminController + /role-login route</name>
  <files>privdId_admin/backend/controllers/adminController.js, privdId_admin/backend/routes/adminRoutes.js</files>
  <read_first>
    - privdId_admin/backend/controllers/adminController.js (adminLogin full file L1-29 — asyncHandler/AppError/jwt.sign shape to mirror)
    - privdId_admin/backend/routes/adminRoutes.js (full file — express.Router(), router.post("/login", adminLogin), export default router)
    - .planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md (adminController.js roleLogin section + adminRoutes.js section — exact code)
  </read_first>
  <action>Add an exported roleLogin asyncHandler to adminController.js, parameterized by role: read { role, password } from req.body; map role via { acadadmin: 'ACADADMIN_PASSWORD', registrar: 'REGISTRAR_PASSWORD', dean: 'DEAN_PASSWORD' }; if role not in the map throw AppError('Select your role first.', 400); if the mapped env var is unset throw AppError(`${role} authentication is not configured.`, 500); if password missing or != expected throw AppError('Incorrect password for this role. Try again.', 401); on success jwt.sign({ role }, process.env.JWT_SECRET || 'privid-admin-secret', { expiresIn: '24h' }) and res.json({ status: 'success', token, role }). Then in adminRoutes.js add import of roleLogin and router.post("/role-login", roleLogin) directly below the existing /login line — same router instance, no new sub-router.</action>
  <verify>
    <automated>cd privdId_admin/backend && node --check controllers/adminController.js && node --check routes/adminRoutes.js && grep -c "export const roleLogin\|/role-login" controllers/adminController.js routes/adminRoutes.js</automated>
  </verify>
  <acceptance_criteria>
    - node --check passes for adminController.js and adminRoutes.js
    - grep "export const roleLogin" controllers/adminController.js matches
    - grep 'router.post("/role-login"' routes/adminRoutes.js matches
    - The three exact error strings present: "Select your role first.", "authentication is not configured.", "Incorrect password for this role. Try again."
    - The role->env map keys are exactly acadadmin, registrar, dean mapped to ACADADMIN_PASSWORD/REGISTRAR_PASSWORD/DEAN_PASSWORD
    - No bcrypt import added (grep -c bcrypt returns 0)
  </acceptance_criteria>
  <done>roleLogin authenticates the 3 fixed roles against env passwords and issues a role-scoped JWT; /role-login route registered.</done>
</task>

<task type="auto">
  <name>Task 2: Create requireAuth JWT middleware</name>
  <files>privdId_admin/backend/middleware/requireAuth.js</files>
  <read_first>
    - privdId_admin/backend/middleware/asyncHandler.js (existing middleware module style / ESM export convention)
    - privdId_admin/backend/middleware/errorHandler.js (AppError handling — errors thrown here get formatted centrally)
    - privdId_admin/backend/controllers/adminController.js (the jwt.sign({ role }, process.env.JWT_SECRET || "privid-admin-secret", ...) shape — requireAuth must verify with the SAME secret + default)
    - privdId_admin/backend/utils/appError.js (AppError constructor signature)
  </read_first>
  <action>Create privdId_admin/backend/middleware/requireAuth.js (ESM, matching the existing middleware module style). Export a default (or named) requireAuth(req, res, next) middleware that: reads the Authorization header, requires a "Bearer <token>" shape (else next(new AppError('Authentication required.', 401))); verifies the token via jwt.verify(token, process.env.JWT_SECRET || 'privid-admin-secret') using the SAME secret + fallback as adminController's jwt.sign so role-login tokens validate; on success attaches the decoded payload to req.user (so { role } is available downstream) and calls next(); on a thrown/invalid/expired token, next(new AppError('Invalid or expired session.', 401)). Import jwt from 'jsonwebtoken' (already a backend dependency, used by adminController) and AppError from the utils path adminController uses. Do NOT add bcrypt or any session store — this is stateless JWT verification only (HARD-01 stays deferred).</action>
  <verify>
    <automated>cd privdId_admin/backend && node --check middleware/requireAuth.js && grep -c "jwt.verify\|Bearer\|JWT_SECRET" middleware/requireAuth.js</automated>
  </verify>
  <acceptance_criteria>
    - node --check passes for middleware/requireAuth.js
    - File exports a requireAuth middleware (grep "requireAuth")
    - Calls jwt.verify with process.env.JWT_SECRET || 'privid-admin-secret' (SAME secret+fallback as adminController.jwt.sign — grep JWT_SECRET and the default string)
    - Parses an "Authorization: Bearer" header (grep Bearer)
    - Rejects missing/invalid tokens via next(new AppError(..., 401)) — no manual res.status().json()
    - No bcrypt import (grep -c bcrypt returns 0)
  </acceptance_criteria>
  <done>requireAuth verifies a Bearer JWT against the same JWT_SECRET the role-login uses and 401s on missing/invalid tokens via AppError.</done>
</task>

<task type="auto">
  <name>Task 3: safeController.js + safeRoutes.js (requireAuth-gated) + app.js mount (executePendingTx completes the D-12 lifecycle)</name>
  <files>privdId_admin/backend/controllers/safeController.js, privdId_admin/backend/routes/safeRoutes.js, privdId_admin/backend/app.js</files>
  <read_first>
    - privdId_admin/backend/controllers/adminController.js (asyncHandler/AppError import + handler shape to mirror)
    - privdId_admin/backend/routes/adminRoutes.js (router shape to mirror for safeRoutes)
    - privdId_admin/backend/middleware/requireAuth.js (the guard created in Task 2 — apply it to safeRoutes)
    - privdId_admin/backend/app.js (imports L7-9, mounts L30-31, notFound/errorHandler L33-34 — where to add the safe import and mount)
    - privdId_admin/backend/services/safeService.js (the exported functions safeController calls; executeTransaction returns { safeTxHash, txHash, blockNumber, ... } per 09-02 Task 2)
    - privdId_admin/backend/models/Student.js (default-exported Student model; pendingRegistryAction subdocument added in 09-02 Task 3a; terminal fields onChainTxHash/onChainBlock/revoked/revokedAt — needed for the execute-success flip)
    - .planning/phases/09-multisig-registry-governance-e5/09-CONTEXT.md (D-12 — "Only after execution succeeds does the student status flip to the terminal issued/revoked state")
    - .planning/phases/09-multisig-registry-governance-e5/09-PATTERNS.md (safeController.js section + safeRoutes.js section — exact handler/route shapes)
  </read_first>
  <action>Create controllers/safeController.js (ESM) importing asyncHandler + AppError + the default-exported Student model (import Student from '../models/Student.js'). Export getPendingApprovals (calls await safeService.getPendingTransactions(); res.json({ status:'success', pending })), signPendingTx (reads { safeTxHash, signature, signerAddress }; joi-validate or regex-validate safeTxHash as 0x 66-char hex and signerAddress as 0x 42-char hex; throw AppError(...,400) on missing/malformed; calls await safeService.confirmSignature(safeTxHash, signature, signerAddress)), and executePendingTx (reads { safeTxHash }; validate as 0x 66-char hex).

  executePendingTx MUST close the D-12 lifecycle:
  1. const result = await safeService.executeTransaction(safeTxHash); (this returns the on-chain txHash + blockNumber per 09-02 Task 2).
  2. On success, map the safeTxHash back to its student: const student = await Student.findOne({ 'pendingRegistryAction.safeTxHash': safeTxHash });
  3. If a student is found, branch on student.pendingRegistryAction.type:
     - 'issue': set student.onChainTxHash = result.txHash; student.onChainBlock = result.blockNumber; student.pendingRegistryAction = { safeTxHash: null, type: null } (clear it); await student.save().
     - 'revoke': set student.revoked = true; student.revokedAt = new Date(); student.pendingRegistryAction = { safeTxHash: null, type: null } (clear it); await student.save().
     - If no matching student (e.g. an admin-handoff acceptAdmin tx from 09-05 that has no Student), skip the student update silently — not every executed Safe tx maps to a student.
  4. res.json({ status:'success', result }).
  Use a [safe] console.log noting the terminal flip (e.g. [safe] Executed ${student.pendingRegistryAction.type} for ${student.rollNo} — student now terminal). Import safeService as a namespace (import * as safeService from '../services/safeService.js') or named imports matching the 09-02 exports.

  Create routes/safeRoutes.js mirroring adminRoutes.js, importing requireAuth from '../middleware/requireAuth.js' and applying it at the router level (router.use(requireAuth)) BEFORE the route definitions so ALL /api/safe routes require the JWT: router.get("/pending", getPendingApprovals); router.post("/sign", signPendingTx); router.post("/execute", executePendingTx); export default router. MODIFY app.js: add import safeRoutes from "./routes/safeRoutes.js" near L8 (with the other route imports), and app.use("/api/safe", safeRoutes) near L31 (after the /api/admin mount, BEFORE notFound/errorHandler). Do NOT reorder existing middleware lines, and do NOT add requireAuth to /api/admin or /api/students.</action>
  <verify>
    <automated>cd privdId_admin/backend && node --check controllers/safeController.js && node --check routes/safeRoutes.js && node --check app.js && grep -c "/api/safe" app.js && grep -c "requireAuth" routes/safeRoutes.js && grep -c "getPendingApprovals\|signPendingTx\|executePendingTx" controllers/safeController.js && grep -c "pendingRegistryAction.safeTxHash\|pendingRegistryAction" controllers/safeController.js</automated>
  </verify>
  <acceptance_criteria>
    - node --check passes for safeController.js, safeRoutes.js, app.js
    - app.js contains app.use("/api/safe", safeRoutes) placed BEFORE app.use(notFound)
    - safeRoutes.js applies requireAuth to all routes (grep "requireAuth" matches; router.use(requireAuth) precedes the route definitions)
    - requireAuth is NOT added to /api/admin or /api/students in app.js or their route files (grep requireAuth in routes/adminRoutes.js and routes/studentRoutes.js returns 0 — out of scope)
    - safeController.js exports getPendingApprovals, signPendingTx, executePendingTx and calls safeService.getPendingTransactions / confirmSignature / executeTransaction
    - safeRoutes.js registers GET /pending, POST /sign, POST /execute
    - safeController.js validates safeTxHash as a 0x-prefixed hex (joi or explicit regex /^0x[0-9a-fA-F]{64}$/) before calling safeService
    - The execute handler does NOT auto-fire on sign — execute is reachable only via its own POST /execute route (no executeTransaction call inside signPendingTx)
    - D-12 LIFECYCLE: executePendingTx imports the Student model and, AFTER a successful safeService.executeTransaction, looks up the Student by pendingRegistryAction.safeTxHash and writes terminal state — verify by reading executePendingTx that it (a) does Student.findOne({ 'pendingRegistryAction.safeTxHash': safeTxHash }), (b) for type 'issue' sets onChainTxHash + onChainBlock from the execution result, (c) for type 'revoke' sets revoked = true + revokedAt, (d) clears pendingRegistryAction to null in both branches, (e) calls student.save(), and (f) tolerates a no-match (no student) without erroring
    - automated grep confirms executePendingTx references pendingRegistryAction.safeTxHash (the DB lookup) — grep "pendingRegistryAction" controllers/safeController.js returns >= 1
  </acceptance_criteria>
  <done>safeController + safeRoutes expose pending/sign/execute against safeService behind requireAuth; /api/safe mounted in app.js with input validation; execute is a separate route AND, on success, maps the safeTxHash to its Student and flips it from pendingRegistryAction into the terminal issued/revoked state (D-12), tolerating non-student Safe txs; /api/admin and /api/students untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Document new env vars in .env.example</name>
  <files>privdId_admin/backend/.env.example</files>
  <read_first>
    - privdId_admin/backend/.env.example (existing var conventions; if it does not exist, infer keys from .env usage in adminController/credentialService/safeService — ADMIN_PASSWORD, JWT_SECRET, REGISTRY_ADDRESS, SEPOLIA_RPC_URL, PRIVATE_KEY)
  </read_first>
  <action>Add/update privdId_admin/backend/.env.example with placeholder entries for: ACADADMIN_PASSWORD, REGISTRAR_PASSWORD, DEAN_PASSWORD (per-role login), SAFE_ADDRESS (from deploySafe.js), SAFE_CHAIN_ID (default 11155111), SAFE_OWNER_ACADADMIN, SAFE_OWNER_REGISTRAR, SAFE_OWNER_DEAN (expected owner addresses for the D-09 client-side match check), and USE_LOCAL_SIGNERS (default false). Ensure JWT_SECRET is present (requireAuth + role-login both depend on it). Use placeholder values only (e.g. CHANGE_ME / 0x... ), never real secrets. Keep the existing example keys intact. Add a short comment that PRIVATE_KEY remains the deployer EOA only (D-03) and is never an official signer.</action>
  <verify>
    <automated>grep -c "ACADADMIN_PASSWORD\|REGISTRAR_PASSWORD\|DEAN_PASSWORD\|SAFE_ADDRESS\|SAFE_OWNER_ACADADMIN\|SAFE_OWNER_REGISTRAR\|SAFE_OWNER_DEAN\|USE_LOCAL_SIGNERS\|JWT_SECRET" privdId_admin/backend/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - All eight new keys present in .env.example: ACADADMIN_PASSWORD, REGISTRAR_PASSWORD, DEAN_PASSWORD, SAFE_ADDRESS, SAFE_OWNER_ACADADMIN, SAFE_OWNER_REGISTRAR, SAFE_OWNER_DEAN, USE_LOCAL_SIGNERS
    - JWT_SECRET present (requireAuth + role-login depend on it)
    - Values are placeholders, not real secrets (no real 64-hex private keys committed)
    - Existing example keys are preserved
  </acceptance_criteria>
  <done>.env.example documents all new Phase 9 env vars (incl. JWT_SECRET) with placeholders; existing keys preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/admin/role-login | Untrusted password input; lightweight env compare |
| browser -> /api/safe/* | Untrusted safeTxHash/signature input crossing into Safe Tx Service; gated by requireAuth |
| role-login JWT -> requireAuth -> /api/safe authorization | JWT gates UI/API access but not signing capability (signing needs MetaMask key) |
| executePendingTx -> Student terminal-state write | Execution result is the sole trigger that flips a student to issued/revoked (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Spoofing | role-login password brute-force / compromise reaching Pending Approvals | mitigate (layered) | Compromising the lightweight password grants UI visibility only — it cannot produce a Safe signature without the corresponding MetaMask private key (D-01). Accepted interim auth posture per D-09/HARD-01 (no bcrypt this phase). |
| T-09-10 | Tampering | malformed/injected safeTxHash or signature reaching api-kit | mitigate | joi/regex validation of safeTxHash (0x + 64 hex) and signerAddress (0x + 40 hex) at the route boundary before any safeService call (RESEARCH V5). |
| T-09-11 | Elevation of Privilege | execute auto-firing the instant threshold is met (violates D-06) | mitigate | Execute is its own POST /execute route; signPendingTx never calls executeTransaction. Acceptance criteria assert separation. |
| T-09-12 | Spoofing | unauthenticated access to /api/safe pending/sign/execute | mitigate | requireAuth (new in Task 2) is applied at the safeRoutes router level; every /api/safe request requires a valid role-login JWT or is 401'd. Acceptance criteria assert router.use(requireAuth) precedes the routes. |
| T-09-13 | Information Disclosure | committing real passwords/keys to .env.example | mitigate | Task 4 uses placeholders only; acceptance criteria forbid real 64-hex keys in the example file. |
| T-09-15 | Tampering / Data Integrity | student left stuck in pendingRegistryAction after a successful execute (terminal state never set), violating D-12 and stranding the credential | mitigate | executePendingTx maps safeTxHash -> Student and sets onChainTxHash/onChainBlock (issue) or revoked/revokedAt (revoke), clearing pendingRegistryAction, only after executeTransaction succeeds. Acceptance criteria assert this DB write; 09-05 Task 4 verifies it live on Sepolia. |
</threat_model>

<verification>
- node --check clean on adminController.js, adminRoutes.js, requireAuth.js, safeController.js, safeRoutes.js, app.js.
- /api/safe mounted before notFound and gated by requireAuth; role-login + 3 safe routes registered; input validated; execute separated from sign.
- executePendingTx flips the matching student to terminal state (issue: onChainTxHash/onChainBlock; revoke: revoked/revokedAt) and clears pendingRegistryAction on success, tolerating non-student txs (D-12).
- requireAuth verifies the same JWT_SECRET role-login signs with; /api/admin and /api/students left open (deferred to HARD-01).
- .env.example documents new keys (incl. JWT_SECRET) with placeholders.
</verification>

<success_criteria>
- GOV-03 HTTP path: officials log in by role and drive pending/sign/execute against safeService over JWT-protected routes.
- GOV-04 control surface: explicit execute route, never auto-fired (D-06); successful execution is what flips the student to terminal issued/revoked (D-12).
- Lightweight per-role auth (D-09) with no bcrypt upgrade; /api/safe gated by requireAuth (RESEARCH Security Domain V4).
</success_criteria>

<output>
Create `.planning/phases/09-multisig-registry-governance-e5/09-03-SUMMARY.md` when done.
</output>
</output>
</output>
</content>
