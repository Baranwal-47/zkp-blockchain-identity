---
phase: 06-encryption-ciphertext-storage
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - privdId_admin/backend/services/credentialService.js
  - privdId_admin/backend/services/studentService.js
autonomous: true
requirements: [STORE-01, STORE-02]
must_haves:
  truths:
    - "Issuing a credential builds the §E3.2 JSON (7 attrs + 7 salts + merkleRoot + issuedAt/issuer/type/version), encrypts it under a DEK, and pins only the ciphertext blob — never the plaintext credential"
    - "createStudent and insertBulkStudents generate a fresh DEK (D-06); updateStudent reuses the existing student.dek and never rotates it (D-04, D-05). Phase 6 only writes the DEK; Phase 7 wipes it (D-03)"
    - "The DEK is persisted to Student.dek (base64) on first issuance (D-01) and the renamed ciphertextCID is persisted at all 3 call sites (D-07)"
    - "sanitizeStudent() returns ciphertextCID (renamed) and never includes dek (D-02)"
    - "No code path remains that pins an unencrypted credential JSON to IPFS"
  artifacts:
    - path: "privdId_admin/backend/services/credentialService.js"
      provides: "buildCredentialJson(student) helper + issueCredentialOnChain encrypts before pinning, returns dek"
      contains: "encryptCredential"
    - path: "privdId_admin/backend/services/studentService.js"
      provides: "3 call sites with DEK generate/reuse policy; sanitizeStudent dek exclusion + ciphertextCID rename"
      contains: "ciphertextCID"
  key_links:
    - from: "privdId_admin/backend/services/credentialService.js"
      to: "privdId_admin/backend/crypto/aesgcm.js"
      via: "import { encryptCredential }"
      pattern: "import.*encryptCredential.*aesgcm"
    - from: "privdId_admin/backend/services/studentService.js"
      to: "privdId_admin/backend/services/credentialService.js"
      via: "issueCredentialOnChain returns { cid, txHash, blockNumber, dek }"
      pattern: "issueCredentialOnChain"
---

<objective>
Wire the Plan 01 crypto module into the issuance flow. Add a shared `buildCredentialJson(student)` helper, encrypt the §E3.2 credential JSON inside `issueCredentialOnChain()` so only ciphertext is pinned, apply the DEK generate-vs-reuse policy across all 3 call sites in `studentService.js`, persist the DEK + renamed `ciphertextCID`, and lock the `dek` field out of `sanitizeStudent()`.

Purpose: STORE-01 (encrypt + pin ciphertext, return ciphertextCID) and STORE-02 (no plaintext blob ever pinned). This is the behavioral heart of the phase.
Output: Modified `credentialService.js` and `studentService.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-encryption-ciphertext-storage/06-CONTEXT.md
@.planning/phases/06-encryption-ciphertext-storage/06-RESEARCH.md
@.planning/phases/06-encryption-ciphertext-storage/06-PATTERNS.md
@.planning/phases/06-encryption-ciphertext-storage/06-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (crypto/aesgcm.js) and the existing services. -->

From privdId_admin/backend/crypto/aesgcm.js (Plan 01):
  generateDEK(): Buffer            // 32 random bytes
  async encryptCredential(plaintextObj, dek): Promise<{iv, authTag, ciphertext}>  // dek = 32-byte Buffer; returns base64 blob
  decryptCredential(blob, dek): object

Frozen 7-attribute leaf order (from utils/identityCommitment.js — DO NOT DRIFT):
  name, rollNo, dobInt, programmeLevel, discipline, batch, email
The §E3.2 credential JSON MUST list attributes in this order; salts (array of 7) live INSIDE this object in the same frozen order.

Current issueCredentialOnChain(student) (credentialService.js ~lines 60-76) pins a small PLAINTEXT object {rollNo,email,merkleRoot,issuedAt,issuer,type,version} and returns {cid, txHash, blockNumber}. pinToIPFS(credential) (lines 17-32) accepts any JSON object and returns response.data.IpfsHash; metadata name is `privid-credential-${credential.rollNo}`. anchorOnChain(rollNo, cid, merkleRoot) (lines 34-46) is UNCHANGED.

studentService.js call sites (verified line ranges):
  createStudent     — anchoring try/catch ~148-161, uses student.save()
  insertBulkStudents — anchoring loop try/catch ~188-201, uses Student.updateOne (patch object)
  updateStudent     — re-anchoring try/catch ~271-284, uses student.save()
  sanitizeStudent   — allowlist ~43-62, currently lists `ipfsCID: student.ipfsCID ?? null` at ~line 56

The full student doc already carries: name, rollNo, dobInt, programmeLevel, discipline, batch, email, salts (array of 7), merkleRoot. No new source fields needed beyond Plan 01's dek/ciphertextCID.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildCredentialJson() and encrypt-before-pin in credentialService.js</name>
  <files>privdId_admin/backend/services/credentialService.js</files>
  <read_first>
    - privdId_admin/backend/services/credentialService.js (full file — pinToIPFS lines 17-32, anchorOnChain 34-46, issueCredentialOnChain 60-76, logging discipline at line 74)
    - privdId_admin/backend/crypto/aesgcm.js (Plan 01 — generateDEK / encryptCredential signatures)
    - privdId_admin/backend/utils/identityCommitment.js (the frozen leaf order: name, rollNo, dobInt, programmeLevel, discipline, batch, email)
    - docs/CLAUDE_CODE_BLUEPRINT.md §E3.2 (lines 245-257) — exact §E3.2 JSON shape; salts live INSIDE this object
    - .planning/phases/06-encryption-ciphertext-storage/06-RESEARCH.md (§E3.2 credential JSON builder example; Pitfall 1 field-set drift; Pitfall 4 encoding shape)
    - .planning/phases/06-encryption-ciphertext-storage/06-PATTERNS.md (credentialService.js section)
  </read_first>
  <action>
    Add `import { encryptCredential } from '../crypto/aesgcm.js';` to the imports.

    Add and export a shared helper `buildCredentialJson(student)` that returns the §E3.2 object with attributes in the EXACT frozen leaf order: `name`, `rollNo`, `dobInt`, `programmeLevel`, `discipline`, `batch`, `email`, then `salts: student.salts` (the array of 7, already in frozen order on the doc), `merkleRoot: student.merkleRoot`, `issuedAt: new Date().toISOString()`, `issuer: 'PrivdID — IIITDM Jabalpur'`, `type: 'StudentIdentityCredential'`, `version: '2.0'`. This is the single source of truth for the credential JSON shape — all 3 call sites go through it (closes Pitfall 1 field-set drift). Export it so studentService can reference the shape if needed, but the encryption stays inside issueCredentialOnChain.

    Modify `issueCredentialOnChain(student, dek)` to take a second parameter `dek` (a 32-byte Buffer supplied by the caller per the generate/reuse policy in Task 2 — do NOT generate the DEK here; the caller owns DEK lifecycle). Inside: build `const credentialJson = buildCredentialJson(student)`, then `const encryptedBlob = await encryptCredential(credentialJson, dek)`, then pin the BLOB (not the plaintext credential): `const cid = await pinToIPFS(encryptedBlob, student.rollNo)`. The old plaintext `credential` object construction (rollNo/email/merkleRoot/issuedAt/...) must be DELETED — there must be zero remaining code path that pins an unencrypted credential JSON (STORE-02). Keep `anchorOnChain(student.rollNo, cid, student.merkleRoot)` unchanged. Return `{ cid, txHash, blockNumber }` — the caller already holds the dek it passed in, so no need to return it.

    Modify `pinToIPFS(credential)` to accept a second optional `pinName` (the rollNo) so the Pinata metadata name can become `privid-ciphertext-${pinName}` (per CONTEXT.md discretion — the blob no longer has a top-level `rollNo` key, so the old `credential.rollNo` interpolation would be undefined). Keep its axios/auth internals otherwise verbatim.

    Preserve the non-blocking nature and logging discipline: the existing `console.log('[credential] Anchored ${student.rollNo} → IPFS: ${cid} | Tx: ${txHash}')` line stays (logs only rollNo/cid/txHash). Do NOT add any log that prints `dek`, `credentialJson`, or `encryptedBlob`.
  </action>
  <verify>
    <automated>cd privdId_admin/backend && grep -qE "import .*encryptCredential.* from .\.\./crypto/aesgcm\.js." services/credentialService.js && grep -q "buildCredentialJson" services/credentialService.js && grep -qE "encryptCredential\(" services/credentialService.js && test "$(grep -vE '^\s*(//|\*)' services/credentialService.js | grep -cE "pinToIPFS\(\s*credentialJson|pinToIPFS\(\s*\{[^}]*rollNo")" = "0" && grep -qE "privid-ciphertext-" services/credentialService.js && node --check services/credentialService.js 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `credentialService.js` imports `encryptCredential` from `../crypto/aesgcm.js`.
    - A `buildCredentialJson` function exists; its object literal lists keys in order name, rollNo, dobInt, programmeLevel, discipline, batch, email, salts, merkleRoot, issuedAt, issuer, type, version (verify by reading — order matches identityCommitment frozen leaf order).
    - `issueCredentialOnChain` calls `encryptCredential(...)` and passes the resulting blob (not a plaintext credential object) to `pinToIPFS`.
    - The old plaintext credential object (with literal keys rollNo+email+merkleRoot built inline for pinning) is deleted: `grep -vE '^\s*(//|\*)' services/credentialService.js | grep -cE "pinToIPFS\(\s*credential\b"` returns 0 (no plaintext credential pinned).
    - Pinata metadata name is `privid-ciphertext-${...}`: `grep -c "privid-ciphertext-" services/credentialService.js` >= 1.
    - No secret logging: `grep -vE '^\s*(//|\*)' services/credentialService.js | grep -E "console\.(log|error).*\b(dek|encryptedBlob|credentialJson)\b"` returns nothing.
    - `node --check services/credentialService.js` exits 0.
  </acceptance_criteria>
  <done>issueCredentialOnChain builds the §E3.2 JSON via shared buildCredentialJson, encrypts it, and pins only the ciphertext blob under `privid-ciphertext-${rollNo}`; no plaintext credential is pinned; no secrets logged.</done>
</task>

<task type="auto">
  <name>Task 2: Apply DEK generate/reuse policy and ciphertextCID persistence across 3 call sites</name>
  <files>privdId_admin/backend/services/studentService.js</files>
  <read_first>
    - privdId_admin/backend/services/studentService.js (createStudent anchoring ~148-161; insertBulkStudents loop ~188-201; updateStudent re-anchoring ~271-284)
    - privdId_admin/backend/crypto/aesgcm.js (generateDEK signature — returns 32-byte Buffer)
    - privdId_admin/backend/services/credentialService.js (the modified issueCredentialOnChain(student, dek) signature from Task 1)
    - .planning/phases/06-encryption-ciphertext-storage/06-CONTEXT.md (D-04 reuse on update, D-06 fresh on create/bulk, D-03 Phase 7 wipes)
    - .planning/phases/06-encryption-ciphertext-storage/06-RESEARCH.md (Pitfall 3 — make reuse explicit and loud on missing DEK)
    - .planning/phases/06-encryption-ciphertext-storage/06-PATTERNS.md (studentService.js section — the 3 call-site code blocks, updateOne-vs-save asymmetry)
  </read_first>
  <action>
    Import `generateDEK` from `../crypto/aesgcm.js`.

    `createStudent()` (first issuance, FRESH DEK per D-06): inside the existing non-blocking anchoring try/catch, generate `const dek = generateDEK();` BEFORE calling issue, pass it: `issueCredentialOnChain(student, dek)` (pass the full student doc — it now needs name/dobInt/programmeLevel/discipline/batch/salts, not just rollNo/email/merkleRoot). After success, persist `student.dek = dek.toString('base64')` AND `student.ciphertextCID = cid` (renamed from ipfsCID), then `student.onChainTxHash`, `student.onChainBlock`, `await student.save()`. Keep the try/catch non-blocking — a crypto/Pinata failure must not block the student save.

    `insertBulkStudents()` (loop, FRESH DEK per student per D-06): inside the loop's try/catch, generate `const dek = generateDEK();` per student, call `issueCredentialOnChain(student, dek)`, and extend the existing `Student.updateOne(...)` patch object to set `ciphertextCID: cid` (renamed), `dek: dek.toString('base64')`, `onChainTxHash`, `onChainBlock`. Preserve the updateOne-vs-save asymmetry (this site uses updateOne, the other two use save) — just extend the patch object.

    `updateStudent()` (re-issuance, REUSE existing DEK per D-04 — NEVER rotate): inside the re-anchoring try/catch, read `const dek = Buffer.from(student.dek, 'base64')`. Per Pitfall 3, make reuse explicit and LOUD: if `student.dek` is missing/null, do NOT silently `generateDEK()` as a fallback — instead `console.error('[credential] updateStudent: missing DEK for', student.rollNo, '— cannot re-issue without rotating')` and skip re-issuance (or throw inside the try so the existing catch logs it). The DEK must NEVER change value after first issuance (rotating would silently invalidate the Phase 7 ECIES envelope, D-05). Pass the reused dek to `issueCredentialOnChain(student, dek)`. Persist `student.ciphertextCID = cid` (renamed) but do NOT overwrite `student.dek` (it is unchanged). `await student.save()`.
  </action>
  <verify>
    <automated>cd privdId_admin/backend && grep -qE "import .*generateDEK.* from .\.\./crypto/aesgcm\.js." services/studentService.js && test "$(grep -c "generateDEK()" services/studentService.js)" -ge "2" && grep -q "Buffer.from(student.dek, 'base64')" services/studentService.js && grep -q "ciphertextCID" services/studentService.js && test "$(grep -c '\bipfsCID\b' services/studentService.js)" = "0" && node --check services/studentService.js 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `generateDEK()` is called in exactly the create and bulk paths (>= 2 occurrences) and NOT in the update path: reading updateStudent shows `Buffer.from(student.dek, 'base64')` and no `generateDEK()` inside it.
    - createStudent persists `student.dek = dek.toString('base64')` and `student.ciphertextCID = cid`.
    - insertBulkStudents' `updateOne` patch object sets `ciphertextCID`, `dek`, `onChainTxHash`, `onChainBlock`.
    - updateStudent reuses `student.dek`, sets `ciphertextCID`, and does NOT reassign `student.dek` (no `student.dek =` inside updateStudent) — verify by reading the function.
    - updateStudent handles a missing DEK loudly (a console.error mentioning missing DEK), not a silent fresh-DEK fallback.
    - Zero `ipfsCID` references remain: `grep -c '\bipfsCID\b' services/studentService.js` returns 0.
    - `node --check services/studentService.js` exits 0.
  </acceptance_criteria>
  <done>Fresh DEK on create/bulk, reused DEK on update (loud on missing, never rotated), ciphertextCID persisted at all 3 sites, dek persisted base64 on first issuance only; non-blocking try/catch preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Exclude dek from sanitizeStudent and rename ipfsCID to ciphertextCID in the allowlist</name>
  <files>privdId_admin/backend/services/studentService.js</files>
  <read_first>
    - privdId_admin/backend/services/studentService.js (sanitizeStudent allowlist ~lines 43-62, the `ipfsCID: student.ipfsCID ?? null` line ~56)
    - .planning/phases/06-encryption-ciphertext-storage/06-CONTEXT.md (D-02 — dek MUST be excluded from sanitizeStudent, hard requirement)
    - .planning/phases/06-encryption-ciphertext-storage/06-RESEARCH.md (Pitfall 2 — sanitizeStudent is the sole response-shaping chokepoint; allowlist excludes by default)
    - .planning/phases/06-encryption-ciphertext-storage/06-PATTERNS.md (sanitizeStudent allowlist excerpt)
  </read_first>
  <action>
    In `sanitizeStudent()` (the explicit allowlist around lines 43-62), rename the `ipfsCID: student.ipfsCID ?? null` line to `ciphertextCID: student.ciphertextCID ?? null` (D-07 rename).

    Do NOT add a `dek` line to the returned object — its ABSENCE from this allowlist IS the D-02 enforcement (the allowlist excludes any field not explicitly listed). Add a short inline comment at the appropriate spot noting that `dek` is intentionally excluded (single-custody interim secret, must never leave the backend per D-02) so a future maintainer does not "helpfully" add it.

    This is a distinct, explicit task (not folded into Task 2) because D-02 is a hard security requirement: the per-student plaintext DEK must never leak through any API response. After this change, confirm no other `res.json()` path in the controller returns a raw Mongoose document (research confirmed sanitizeStudent is the sole chokepoint — re-verify in the acceptance criteria).
  </action>
  <verify>
    <automated>cd privdId_admin/backend && grep -qE "ciphertextCID:\s*student\.ciphertextCID" services/studentService.js && test "$(awk '/export function sanitizeStudent/,/^}/' services/studentService.js | grep -c "dek")" = "0" && test "$(grep -rnE "res\.json\(\s*student\b" controllers/studentController.js | grep -vc "sanitizeStudent")" = "0" && node --check services/studentService.js 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `sanitizeStudent()` returns `ciphertextCID: student.ciphertextCID ?? null` (renamed from ipfsCID).
    - The `sanitizeStudent` function body contains NO `dek` key (verified: `awk '/export function sanitizeStudent/,/^}/'` body has 0 occurrences of `dek`) — its exclusion is the D-02 fix.
    - No controller path returns a raw student doc bypassing the sanitizer: `grep -rnE "res\.json\(\s*student\b" controllers/studentController.js` returns no line lacking `sanitizeStudent` (i.e., every res.json on a student goes through sanitizeStudent or a service that does).
    - `node --check services/studentService.js` exits 0.
  </acceptance_criteria>
  <done>sanitizeStudent returns the renamed ciphertextCID and never includes dek; no API response path leaks the plaintext DEK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → IPFS (Pinata) | Pinned content is publicly fetchable by CID. After this plan, only the AES-GCM ciphertext blob crosses this boundary — never the §E3.2 plaintext JSON (STORE-02). |
| backend → admin API client | `sanitizeStudent()` is the sole response-shaping chokepoint; the plaintext `dek` must never cross this boundary (D-02). |
| backend memory → MongoDB | Plaintext DEK persisted to `Student.dek` — accepted interim gap (T-06-02 in Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | Plaintext credential PII pinned to public IPFS | mitigate | issueCredentialOnChain encrypts via encryptCredential and pins ONLY the `{iv,authTag,ciphertext}` blob; the old plaintext credential object is deleted. Verified by Task 1 acceptance (grep for no `pinToIPFS(credential` plaintext path). This is STORE-02's core control. |
| T-06-06 | Information Disclosure | `dek` leaking via admin API response | mitigate | `sanitizeStudent()` allowlist omits `dek` (D-02), and research confirmed it is the sole response-shaping chokepoint. Verified by Task 3 acceptance (no `dek` in sanitize body; no raw-doc res.json bypass). |
| T-06-07 | Tampering / Information Disclosure | DEK rotation on update silently invalidating future ECIES envelope | mitigate | updateStudent REUSES `student.dek` and never reassigns it (D-04/D-05); missing-DEK case fails loudly instead of silently rotating. Verified by Task 2 acceptance (no `generateDEK()` and no `student.dek =` inside updateStudent). |
| T-06-08 | Information Disclosure | DEK / plaintext credential leaking via logs | mitigate | Existing logging discipline preserved: only rollNo/cid/txHash/err.message logged. Verified by no-secret-log greps in Tasks 1 and 2. |
| T-06-09 | Denial of Service | crypto/Pinata failure blocking student save | accept | The encrypt+pin step stays inside the existing non-blocking try/catch at all 3 call sites; a failure logs and the student record still saves (pre-existing accepted behavior, unchanged by this phase). |
| T-06-SC | Tampering | npm installs | mitigate | Zero new npm packages (Node built-in crypto + existing axios/mongoose only). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- Issuing a credential pins only ciphertext (manual: trigger createStudent against a test DB, fetch the resulting CID from Pinata gateway, confirm the object has only `{iv, authTag, ciphertext}` keys and no readable PII).
- Two different students yield different DEKs and different ciphertexts (manual/scripted: create two students with identical attrs, confirm `student.dek` and `ciphertextCID` differ — confirms DEK randomness, ROADMAP success criterion 3).
- `grep -rc '\bipfsCID\b' privdId_admin/backend/services privdId_admin/backend/models` returns 0 across the admin backend.
- `sanitizeStudent` never returns `dek`.
</verification>

<success_criteria>
- STORE-01: issuance generates a DEK, AES-256-GCM-encrypts the §E3.2 JSON, pins ciphertext, returns ciphertextCID.
- STORE-02: no plaintext credential blob is ever pinned — verified by deletion of the plaintext-pin path.
- DEK lifecycle correct: fresh on create/bulk, reused on update, never rotated, never leaked.
</success_criteria>

<output>
Create `.planning/phases/06-encryption-ciphertext-storage/06-02-SUMMARY.md` when done.
</output>
