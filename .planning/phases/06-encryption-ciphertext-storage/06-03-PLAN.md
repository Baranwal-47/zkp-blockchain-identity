---
phase: 06-encryption-ciphertext-storage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - zkp-backend/server.js
  - digital-app/screens/VerifyProof.js
autonomous: true
requirements: [STORE-01]
must_haves:
  truths:
    - "zkp-backend's /credential-info handler refers to the registry CID as ciphertextCID (renamed from ipfsCID) in its destructure, guard, response key, and gateway URL"
    - "digital-app's VerifyProof screen reads result.registry.ciphertextCID (renamed from ipfsCID)"
    - "No ipfsCID identifier remains in either file"
  artifacts:
    - path: "zkp-backend/server.js"
      provides: "/credential-info handler with ciphertextCID naming"
      contains: "ciphertextCID"
    - path: "digital-app/screens/VerifyProof.js"
      provides: "registry CID render reading ciphertextCID"
      contains: "ciphertextCID"
  key_links:
    - from: "digital-app/screens/VerifyProof.js"
      to: "zkp-backend/server.js"
      via: "reads the ciphertextCID key from /credential-info response"
      pattern: "registry\\.ciphertextCID"
---

<objective>
Complete the `ipfsCID → ciphertextCID` rename in the two read-only consumers outside the admin backend so the field name is consistent across all 4 files identified in CONTEXT.md D-08. This is a purely mechanical rename of an off-chain JS variable/response key — no encryption logic, no on-chain contract change.

Purpose: D-07/D-08 require renaming the field outright everywhere (no alongside). Plan 02 covers the admin backend (Student.js, studentService.js); this plan covers the remaining two: `zkp-backend/server.js` and `digital-app/screens/VerifyProof.js`. Independent of the crypto work — runs in parallel.
Output: Modified `zkp-backend/server.js` and `digital-app/screens/VerifyProof.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-encryption-ciphertext-storage/06-CONTEXT.md
@.planning/phases/06-encryption-ciphertext-storage/06-PATTERNS.md

<interfaces>
<!-- The exact rename targets, extracted in the pattern session. -->

zkp-backend/server.js — /credential-info handler (~lines 207-235). The on-chain registry tuple is destructured POSITIONALLY:
  const [rollNo, ipfsCID, issuedAt, exists, revoked] = await registryContract.getCredentialByHash(pubHashBytes32);
Rename only the JS variable `ipfsCID` → `ciphertextCID` at its 4 occurrences:
  (1) the destructure (~216), (2) the `!exists || !ipfsCID` guard (~218),
  (3) the response key `ipfsCID,` (~225), (4) the gateway URL `.../ipfs/${ipfsCID}` (~228).
The contract call, struct order, and error handling are UNCHANGED — this is an off-chain variable rename only. The on-chain CID value semantics are unaffected.

digital-app/screens/VerifyProof.js — registry render block (~lines 226-231):
  <Text style={styles.registryValue}>{result.registry.ipfsCID?.slice(0, 20)}...</Text>
Rename `result.registry.ipfsCID` → `result.registry.ciphertextCID` (~line 230) to match the renamed key from /credential-info. The user-facing label text "IPFS CID:" may stay as-is (it is still a content identifier on IPFS; the rename is about the field name, not display copy).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ipfsCID to ciphertextCID in zkp-backend and VerifyProof</name>
  <files>zkp-backend/server.js, digital-app/screens/VerifyProof.js</files>
  <read_first>
    - zkp-backend/server.js (the /credential-info handler ~lines 207-235 — the 4 ipfsCID occurrences)
    - digital-app/screens/VerifyProof.js (the registry render block ~lines 226-231 — result.registry.ipfsCID)
    - .planning/phases/06-encryption-ciphertext-storage/06-CONTEXT.md (D-07 rename outright; D-08 all 4 files)
    - .planning/phases/06-encryption-ciphertext-storage/06-PATTERNS.md (zkp-backend/server.js and VerifyProof.js sections — exact occurrences and line numbers)
  </read_first>
  <action>
    In `zkp-backend/server.js`, in the `/credential-info` handler, rename the off-chain JS variable `ipfsCID` to `ciphertextCID` at all 4 occurrences: the positional destructure `const [rollNo, ciphertextCID, issuedAt, exists, revoked] = ...`, the guard `if (!exists || !ciphertextCID)`, the response key `ciphertextCID,` in the `res.json({...})`, and the gateway URL interpolation `ipfsUrl: \`https://gateway.pinata.cloud/ipfs/${ciphertextCID}\``. Do NOT change the contract call `registryContract.getCredentialByHash(...)`, the tuple ORDER, or the error handling — the on-chain struct order is untouched by this phase; only the local variable name changes. The response key `ipfsUrl` itself can stay named `ipfsUrl` (it is the gateway URL, semantically fine) — only the CID variable/key renames.

    In `digital-app/screens/VerifyProof.js`, rename `result.registry.ipfsCID` to `result.registry.ciphertextCID` in the registry render block so the screen reads the renamed key coming from `/credential-info`. The display label text "IPFS CID:" may remain unchanged (executor discretion — it is still an IPFS content identifier; no functional impact).

    After both edits, there must be ZERO `ipfsCID` identifiers remaining in either file (D-07: rename outright, no alongside).
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app && grep -qE "ciphertextCID" zkp-backend/server.js && grep -qE "ciphertextCID" digital-app/screens/VerifyProof.js && test "$(grep -c '\bipfsCID\b' zkp-backend/server.js)" = "0" && test "$(grep -c '\bipfsCID\b' digital-app/screens/VerifyProof.js)" = "0" && node --check zkp-backend/server.js 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\bipfsCID\b' zkp-backend/server.js` returns 0 and `grep -c '\bipfsCID\b' digital-app/screens/VerifyProof.js` returns 0 (renamed outright, no alongside).
    - `zkp-backend/server.js` `/credential-info` destructures `ciphertextCID` positionally, guards on it, returns it as a response key, and interpolates it in the gateway URL.
    - `digital-app/screens/VerifyProof.js` reads `result.registry.ciphertextCID`.
    - The contract call `getCredentialByHash` and the tuple order are unchanged (no on-chain logic edited).
    - `node --check zkp-backend/server.js` exits 0.
  </acceptance_criteria>
  <done>Both read-only consumers use `ciphertextCID`; zero `ipfsCID` identifiers remain in either file; no on-chain/contract logic changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zkp-backend → on-chain registry (read) | Read-only lookup of the registry CID; no secrets cross this boundary. The rename is an off-chain variable name only. |
| zkp-backend → digital-app client | The /credential-info response now uses key `ciphertextCID`; the client must read the matching key. No secret material involved (just a public IPFS CID). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | positional registry tuple destructure | mitigate | Only the JS variable NAME changes; the tuple position/order and the `getCredentialByHash` contract call are untouched, so the renamed `ciphertextCID` still binds to the same on-chain CID slot. Verified by Task 1 acceptance (contract call + tuple order unchanged). |
| T-06-11 | (None — no new attack surface) | client/backend key rename | accept | This is a mechanical identifier rename of a public IPFS CID field; no secret, auth, or input-validation surface is introduced or removed. No new threat. |
| T-06-SC | Tampering | npm installs | mitigate | Zero new npm packages in either file. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep -rc '\bipfsCID\b' zkp-backend/server.js digital-app/screens/VerifyProof.js` returns 0 across both files.
- Combined with Plan 02's admin-backend rename, a repo-wide `grep -rn '\bipfsCID\b' --include=*.js privdId_admin/backend zkp-backend digital-app` returns nothing (all 4 D-08 files renamed).
- `/credential-info` still returns a CID for an existing on-chain credential (manual smoke test: POST a known pubHash, confirm `ciphertextCID` is populated in the response).
</verification>

<success_criteria>
- All 4 files from D-08 now use `ciphertextCID` (this plan covers files 3 and 4; Plan 02 covers files 1 and 2).
- The mechanical rename introduces no logic or on-chain change.
</success_criteria>

<output>
Create `.planning/phases/06-encryption-ciphertext-storage/06-03-SUMMARY.md` when done.
</output>
