---
phase: 04-zkp-backend-integration-nonce-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zkp-backend/lib/witnessBuilder.js
  - zkp-backend/lib/predicates.js
  - zkp-backend/lib/encoding.js
  - zkp-backend/test/witnessBuilder.test.js
  - zkp-backend/package.json
autonomous: true
requirements: [BACK-01]
must_haves:
  truths:
    - "The witness builder produces a circuit input whose pubHash equals identityCommitment.js::computeMerkleRoot for the same attrs+salts (field-set consistency holds)"
    - "isOver18/isPostgrad are computed server-side from the real dob/programmeLevel, never trusted from the caller"
    - "The old stringToBigInt encoding is NOT used anywhere in the new code path"
  artifacts:
    - path: "zkp-backend/lib/witnessBuilder.js"
      provides: "Full snarkjs input object construction from {attrs, salts, reveal, nonce, currentDateInt}"
      exports: ["buildWitnessInput"]
    - path: "zkp-backend/lib/predicates.js"
      provides: "computeIsOver18 / computeIsPostgrad mirroring the circuit's digit-shift logic"
      exports: ["computeIsOver18", "computeIsPostgrad"]
    - path: "zkp-backend/lib/encoding.js"
      provides: "hashToField + CHUNK_COUNTS byte-identical to identityCommitment.js"
      exports: ["hashToField", "CHUNK_COUNTS", "generateSalts"]
    - path: "zkp-backend/test/witnessBuilder.test.js"
      provides: "Parity assertion: witness pubHash === oracle computeMerkleRoot"
  key_links:
    - from: "zkp-backend/lib/witnessBuilder.js"
      to: "zkp-backend/lib/encoding.js"
      via: "hashToField(name/rollNo/email, CHUNK_COUNTS.*)"
      pattern: "hashToField\\("
    - from: "zkp-backend/lib/witnessBuilder.js"
      to: "zkp-backend/lib/predicates.js"
      via: "computeIsOver18 / computeIsPostgrad"
      pattern: "computeIsOver18|computeIsPostgrad"
---

<objective>
Build the encoding + predicate + witness-construction library that makes zkp-backend's proof generation field-set-consistent with the admin issuance path. This is the highest-risk piece of Phase 4 (CLAUDE.md ground rule #3): the witness builder MUST produce the identical 7-attribute salted Merkle root that `identityCommitment.js::computeMerkleRoot` produces, or every on-chain verification silently fails.

Purpose: Isolate the parity-critical witness math into a tested library so the route rewrite in plan 02 is thin wiring, not crypto.
Output: `zkp-backend/lib/{encoding,predicates,witnessBuilder}.js` + a parity test that anchors against the real JS oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-zkp-backend-integration-nonce-enforcement/04-RESEARCH.md

<interfaces>
<!-- Canonical oracle (ESM) — zkp-backend is CommonJS, so either vendor these
     functions verbatim into lib/encoding.js OR cross-import via await import().
     The encoding logic MUST be byte-for-byte identical (ground rule #3). -->

From privdId_admin/backend/utils/identityCommitment.js (ESM):
  export const CHUNK_COUNTS = Object.freeze({ name: 4, rollNo: 2, email: 2 });
  export async function hashToField(str, maxChunks): Promise<string>  // decimal field-element string
  export function generateSalt(): string            // crypto.randomBytes(31) -> BigInt -> decimal string
  export function generateSalts(count = 7): string[]
  export async function computeLeaf(encodedAttr, salt): Promise<string>  // Poseidon(2)
  export async function computeMerkleRoot(attrs, salts): Promise<string> // depth-3, 8-leaf, leaf[7]=Poseidon(2)(0,0)
  const BN128_FIELD_ORDER = 21888242871839275222246405745257275088548364400416034343698204186575808495617n;

From privdId_admin/backend/constants/enumCodes.js (ESM):
  export const PROGRAMME_LEVEL = { "B.Tech":1,"B.Des":2,"Dual":3,"M.Tech":4,"M.Des":5,"PhD":6 };
  export const DISCIPLINE = { "CSE":1,"ECE":2,"ME":3,"SmartMfg":4,"Design":5,"NatSci":6 };
  export const POSTGRAD_CODES = new Set([4, 5, 6]);  // M.Tech, M.Des, PhD; Dual(3) excluded

Frozen leaf encoding (from studentService.js issuance, leaf-index order):
  attr[0] = hashToField(name,   CHUNK_COUNTS.name)    // maxChunks=4
  attr[1] = hashToField(rollNo, CHUNK_COUNTS.rollNo)  // maxChunks=2
  attr[2] = String(dobInt)         // YYYYMMDD integer string (e.g. "20040215")
  attr[3] = String(programmeLevelCode)   // PROGRAMME_LEVEL[name]
  attr[4] = String(disciplineCode)       // DISCIPLINE[name]
  attr[5] = String(batch)          // batch year integer string (e.g. "2021")
  attr[6] = hashToField(email,  CHUNK_COUNTS.email)   // maxChunks=2

Predicate logic (from circuit + circuitParity.test.js):
  isOver18   = (currentDateInt - 180000) >= dobInt   ? 1 : 0   // inclusive (GreaterEqThan); 180000 = 18*10000
  isPostgrad = programmeLevelCode in {4,5,6}          ? 1 : 0

Witness input shape (from circuitParity.test.js buildInput, lines 84-98):
  { attr: string[7], salt: string[7], nonce: string, currentDateInt: string,
    isOver18: "0"|"1", isPostgrad: "0"|"1",
    revealedValue: string[7], revealMask: string[7] }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor encoding + predicates from the canonical oracle</name>
  <files>zkp-backend/lib/encoding.js, zkp-backend/lib/predicates.js, zkp-backend/package.json</files>
  <read_first>
    - privdId_admin/backend/utils/identityCommitment.js (hashToField lines 75-99, generateSalt/generateSalts lines 111-126, BN128_FIELD_ORDER line 31 — copy these byte-for-byte)
    - privdId_admin/backend/constants/enumCodes.js (PROGRAMME_LEVEL, DISCIPLINE, POSTGRAD_CODES)
    - zkp-backend/server.js lines 23-30 (the stringToBigInt function being REPLACED — read it so you know what NOT to reproduce)
    - .planning/phases/04-zkp-backend-integration-nonce-enforcement/04-RESEARCH.md Pattern 3 + "Don't Hand-Roll" table
  </read_first>
  <behavior>
    - hashToField("Utkarsh Baranwal", 4) === "2689494646062948360487866858549161268023147861439580363715484426041810573382"
    - hashToField("21BCS027", 2) === "15150160435819557810078120971221321758887516517285291325240673283662695955468"
    - hashToField("21bcs027@iiitdmj.ac.in", 2) === "6744441775314583329532040559385253235651674879202368422786321712697490882813"
    - computeIsOver18("20260617", "20040215") === 1  (well over 18)
    - computeIsOver18("20260617", "20080617") === 1  (exactly at boundary, inclusive)
    - computeIsOver18("20260617", "20080618") === 0  (one day under)
    - computeIsPostgrad(4) === 1, computeIsPostgrad(5) === 1, computeIsPostgrad(6) === 1
    - computeIsPostgrad(1) === 0, computeIsPostgrad(3) === 0  (Dual excluded)
  </behavior>
  <action>
    Create `zkp-backend/lib/encoding.js` exporting `hashToField(str, maxChunks)`, `CHUNK_COUNTS` ({name:4, rollNo:2, email:2}), and `generateSalts(count=7)`. Copy the hashToField body (UTF-8 → ≤31-byte big-endian chunks → zero-pad to maxChunks → Poseidon(maxChunks) → poseidon.F.toString) and the generateSalt body (crypto.randomBytes(31) → BigInt → decimal string) verbatim from identityCommitment.js. Hardcode BN128_FIELD_ORDER inline with the source-citing comment (do NOT deep-import ffjavascript — per RESEARCH Alternatives Considered, it throws ERR_PACKAGE_PATH_NOT_EXPORTED). Use circomlibjs `buildPoseidon` cached in a module-level promise, mirroring identityCommitment.js's getPoseidon().

    Use CommonJS (`module.exports`) since zkp-backend is CJS. circomlibjs is already a dependency.

    Create `zkp-backend/lib/predicates.js` exporting `computeIsOver18(currentDateInt, dobInt)` returning Number 0|1 via `(BigInt(currentDateInt) - 180000n >= BigInt(dobInt)) ? 1 : 0` (inclusive boundary — matches circuit's GreaterEqThan), and `computeIsPostgrad(programmeLevelCode)` returning `[4,5,6].includes(Number(programmeLevelCode)) ? 1 : 0`. Do NOT accept isOver18/isPostgrad as caller input anywhere — these are derived (RESEARCH anti-pattern, Pitfall 3).

    Add a `test` script to package.json: `"test": "mocha test/**/*.test.js"` (add mocha as a devDependency; it is the test runner — install with npm).
  </action>
  <verify>
    <automated>cd zkp-backend && npm test -- --grep "encoding|predicate" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - zkp-backend/lib/encoding.js exists; `grep -c stringToBigInt zkp-backend/lib/encoding.js` returns 0
    - The three hashToField vectors above match exactly (asserted in the test)
    - All computeIsOver18/computeIsPostgrad behavior cases above pass
    - BN128_FIELD_ORDER appears literally as 21888242871839275222246405745257275088548364400416034343698204186575808495617 in encoding.js
  </acceptance_criteria>
  <done>encoding.js and predicates.js export the functions; behavior tests pass; no stringToBigInt reproduced</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build witnessBuilder and assert pubHash parity against the oracle</name>
  <files>zkp-backend/lib/witnessBuilder.js, zkp-backend/test/witnessBuilder.test.js</files>
  <read_first>
    - zk-proofs/test/circuitParity.test.js (buildInput lines 84-98 — the canonical witness shape; WITNESS_IDX lines 42-48; the section-9 attr vectors lines 118-170)
    - zkp-backend/lib/encoding.js and zkp-backend/lib/predicates.js (created in Task 1)
    - privdId_admin/backend/utils/identityCommitment.js computeMerkleRoot (lines 175+) — the parity oracle the test imports
    - .planning/phases/04-zkp-backend-integration-nonce-enforcement/04-RESEARCH.md Pattern 1 + Pattern 2 + Pitfall 1 (public-signal index drift)
  </read_first>
  <behavior>
    - buildWitnessInput with section-9 attrs (Utkarsh Baranwal vector, FIXED_SALTS=["1".."7"], nonce="1", currentDateInt="20260617", reveal all false) produces input.attr[0..6] equal to the encoded attrs, input.salt = the 7 salts, input.isOver18="1", input.isPostgrad="0"
    - snarkjs.wtns.calculate on that input yields witness[1] (pubHash) === computeMerkleRoot(attrs, salts) from the oracle
    - When reveal.dob=true, input.revealedValue[2]===attr[2] and input.revealMask[2]==="1"; all other revealedValue/revealMask entries are "0"
    - Every numeric field in the returned input is a string (not a JS number / raw BigInt) — RESEARCH Pitfall 2
  </behavior>
  <action>
    Create `zkp-backend/lib/witnessBuilder.js` exporting `async function buildWitnessInput({ attrs, salts, reveal, nonce, currentDateInt })` where `attrs` is the raw student attribute object {name, rollNo, dob (display "YYYY-MM-DD" or dobInt), programmeLevel (name string), discipline (name string), batch, email} and `reveal` is a 7-key boolean object keyed by leaf position OR by attribute name (pick name-keyed: {name, rollNo, dob, programmeLevel, discipline, batch, email} — document the chosen key set in a comment).

    Internally: encode attr[] in frozen leaf order using encoding.hashToField for name/rollNo/email (with CHUNK_COUNTS) and enumCodes for programmeLevel/discipline (require PROGRAMME_LEVEL/DISCIPLINE — vendor or cross-import from enumCodes.js; throw a clear Error on unknown code, mirroring studentService.js). Derive dobInt from the dob string (strip dashes, parseInt) if a display string is passed; accept an already-integer dobInt too. Compute isOver18 = predicates.computeIsOver18(currentDateInt, dobInt) and isPostgrad = predicates.computeIsPostgrad(programmeLevelCode). Build revealedValue[i] = attr[i] when reveal flag set else "0", revealMask[i] = "1"/"0". Return the object in EXACTLY the circuitParity.test.js buildInput shape, with all numeric values normalized to decimal strings.

    Create `zkp-backend/test/witnessBuilder.test.js` that: imports computeMerkleRoot via `await import("../../privdId_admin/backend/utils/identityCommitment.js")` (proven viable in circuitParity.test.js), builds the witness input for the Utkarsh Baranwal section-9 vector with FIXED_SALTS, runs `snarkjs.wtns.calculate` against `identity.wasm`, exports the witness JSON, and asserts witness[1] (pubHash, per WITNESS_IDX) strictly equals the oracle's computeMerkleRoot. WASM is at zkp-backend/identity.wasm.
  </action>
  <verify>
    <automated>cd zkp-backend && npm test -- --grep "witnessBuilder|parity" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - witness[1] (circuit pubHash) === computeMerkleRoot(attrs, salts) — strict equality assertion passes
    - buildWitnessInput output keys are exactly: attr, salt, nonce, currentDateInt, isOver18, isPostgrad, revealedValue, revealMask
    - `grep -c stringToBigInt zkp-backend/lib/witnessBuilder.js` returns 0
    - reveal-flag test: revealedValue[2]===attr[2], revealMask[2]==="1", others "0"
  </acceptance_criteria>
  <done>buildWitnessInput produces a parity-correct witness input; the pubHash===oracle-root test passes against the real wasm</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → /generate-proof (future plan 02) | Untrusted attrs/salts/reveal/nonce/currentDateInt enter; this plan only builds the witness math the route will call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | witnessBuilder predicate inputs | mitigate | isOver18/isPostgrad are recomputed server-side from dob/programmeLevel in predicates.js; caller cannot inject predicate values (RESEARCH Pitfall 3) |
| T-04-02 | Information Disclosure | encoding.js field-set | mitigate | hashToField copied byte-identical from the parity-tested oracle; a divergence is caught by the witnessBuilder.test.js pubHash-parity assertion before any proof ships |
| T-04-SC | Tampering | npm install (mocha devDependency) | mitigate | mocha is a long-established, high-download mainstream test runner; no [ASSUMED]/[SUS] packages (RESEARCH Package Legitimacy Audit: no new runtime installs) |
</threat_model>

<verification>
- `cd zkp-backend && npm test` runs the encoding, predicate, and witnessBuilder suites green
- The witnessBuilder pubHash matches the admin-side computeMerkleRoot for the section-9 vector (field-set consistency proven, ground rule #3)
- No occurrence of stringToBigInt in zkp-backend/lib/
</verification>

<success_criteria>
- buildWitnessInput produces a circuit input whose witness pubHash equals identityCommitment.js::computeMerkleRoot for identical attrs+salts
- isOver18/isPostgrad are derived server-side, never accepted from the caller
- The library is CommonJS-importable by server.js (plan 02 consumes it)
</success_criteria>

<output>
Create `.planning/phases/04-zkp-backend-integration-nonce-enforcement/04-01-SUMMARY.md` when done
</output>
