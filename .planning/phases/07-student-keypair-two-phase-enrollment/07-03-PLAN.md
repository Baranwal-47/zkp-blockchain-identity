---
phase: 07-student-keypair-two-phase-enrollment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - digital-app/index.js
  - digital-app/package.json
  - digital-app/utils/keypair.js
autonomous: false
requirements: [KEY-01]
user_setup:
  - service: react-native-runtime
    why: "On-device crypto RNG smoke test requires a real device, Expo Go, or a simulator that the developer launches"
    dashboard_config:
      - task: "Run the Expo dev client / Expo Go and trigger the keypair smoke test, confirm a key generates without a crypto.getRandomValues error"
        location: "digital-app on a connected device or simulator"
must_haves:
  truths:
    - "react-native-get-random-values is imported as the literal first line of digital-app/index.js, before any other import"
    - "expo-secure-store is installed via npx expo install (SDK-53-resolved version), not a hardcoded ^56 version"
    - "digital-app/utils/keypair.js exports generateAndStoreKeypair() and getStoredPublicKeyHexForRetry()"
    - "generateAndStoreKeypair() generates a secp256k1 keypair on-device, writes the private key hex to expo-secure-store BEFORE returning, and returns only { pubKeyHex }"
    - "On a real device/simulator, generating a keypair does not throw 'crypto.getRandomValues must be defined' (the RNG polyfill works)"
  artifacts:
    - path: "digital-app/index.js"
      provides: "RNG polyfill import as first statement"
      contains: "react-native-get-random-values"
    - path: "digital-app/utils/keypair.js"
      provides: "on-device keypair generation + SecureStore persistence"
      exports: ["generateAndStoreKeypair", "getStoredPublicKeyHexForRetry"]
    - path: "digital-app/package.json"
      provides: "eciesjs, expo-secure-store, react-native-get-random-values dependencies"
      contains: "eciesjs"
  key_links:
    - from: "digital-app/utils/keypair.js"
      to: "eciesjs"
      via: "new PrivateKey() / PrivateKey.fromHex()"
      pattern: "from ['\"]eciesjs['\"]"
    - from: "digital-app/utils/keypair.js"
      to: "expo-secure-store"
      via: "SecureStore.setItemAsync / getItemAsync"
      pattern: "expo-secure-store"
    - from: "digital-app/index.js"
      to: "react-native-get-random-values"
      via: "first-line polyfill import"
      pattern: "react-native-get-random-values"
---

<objective>
Stand up the mobile crypto foundation for first-login keypair generation: install the verified dependency set (`eciesjs@^0.5.0`, `expo-secure-store` via the SDK-53 resolver, and the mandatory `react-native-get-random-values` RNG polyfill), wire the polyfill as the first line of `index.js`, and create `utils/keypair.js` that generates a secp256k1 keypair on-device and persists the private key in `expo-secure-store` — never exporting or transmitting it. This plan front-loads the single highest-risk unknown from RESEARCH.md (Open Question 1: does the RNG polyfill actually work under Hermes?) as its first task, before any screen depends on it.

Purpose: Closes KEY-01 (on-device secp256k1 keypair at first login, private key in Keystore/Keychain-backed expo-secure-store, never exported). Provides `generateAndStoreKeypair`/`getStoredPublicKeyHexForRetry` for plan 07-04's ClaimCredentialScreen.
Output: Modified `index.js`, modified `package.json` (3 deps), new `utils/keypair.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-CONTEXT.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-RESEARCH.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-PATTERNS.md

<interfaces>
<!-- Verified API shapes from RESEARCH.md (Patterns 2 & 3, smoke-tested in Node). -->

eciesjs (verified): `new PrivateKey()` -> 32-byte priv; `.toHex()` (64 hex chars);
  `.publicKey.toHex()` (33-byte compressed pubkey hex); `PrivateKey.fromHex(hex)` re-derives.

expo-secure-store: `SecureStore.setItemAsync(key, value)` / `SecureStore.getItemAsync(key)`.

Required storage key constant: 'privid_student_privkey' (RESEARCH.md Pattern 2).

index.js current (8 lines): import { registerRootComponent } from 'expo'; import App from './App'; registerRootComponent(App);

CRITICAL ordering: react-native-get-random-values MUST be the first statement in index.js,
before expo/App or anything that transitively imports eciesjs/ethers (RESEARCH.md Pitfall 1 + Anti-Patterns).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify react-native-get-random-values legitimacy before install</name>
  <what-built>This is a pre-install legitimacy gate. RESEARCH.md Package Legitimacy Audit flagged `react-native-get-random-values` as `[ASSUMED]` — it was discovered after the slopcheck audit batch and never run through slopcheck. eciesjs and expo-secure-store are already `[OK]` and do not need this gate.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-native-get-random-values and confirm: the package exists, is widely used (weekly downloads in the millions range), and points to source repo github.com/LinusU/react-native-get-random-values.
    2. Optionally run `slopcheck install react-native-get-random-values --ecosystem npm` in a scratch directory (NOT the live digital-app — per RESEARCH.md's "inadvertent install" note, slopcheck install mutates the target package.json).
    3. Confirm it is the standard noble-curves-in-RN crypto.getRandomValues polyfill (it is the package ethers/wagmi/uuid RN setups use).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or describe any concern that should change the library choice (RESEARCH.md D-05 fallback path).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install deps, wire RNG polyfill, and run the on-device crypto smoke test</name>
  <read_first>
    - digital-app/index.js (current 8-line entry point to modify)
    - digital-app/package.json (current deps; confirm no existing crypto polyfill; Expo SDK 53 pinning)
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-RESEARCH.md (Pitfall 1 + Pitfall 2 + Open Questions 1 & 2 — the exact install commands and the on-device smoke-test recommendation)
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-PATTERNS.md (index.js section lines 435-452)
  </read_first>
  <action>Install the dependencies into `digital-app`: run `cd digital-app && npx expo install expo-secure-store` (this resolves the SDK-53-correct version via Expo's compatibility table — do NOT hardcode `^56.0.4`, per RESEARCH.md Pitfall 2), then `cd digital-app && npm install eciesjs@^0.5.0 react-native-get-random-values` (eciesjs pinned to the ^0.5 pure-JS line, never ^0.3 native-binding line). Modify `digital-app/index.js` to add `import 'react-native-get-random-values';` as the LITERAL FIRST LINE, before `import { registerRootComponent } from 'expo';` and `import App from './App';` — add a trailing comment noting it must stay first. Then run the on-device RNG smoke test (RESEARCH.md Open Question 1's recommended first task): temporarily add a minimal keygen probe (e.g. a one-off effect or a logged call to `new PrivateKey()` from eciesjs that logs ONLY the key length/type, never the key itself) and launch the app on a real device / Expo Go / simulator; confirm no `crypto.getRandomValues must be defined` error fires and a key generates. Remove the temporary probe after the smoke test passes (the permanent keygen logic lives in keypair.js, Task 3). Record the resolved expo-secure-store version and the smoke-test result (pass + device/simulator used) in the SUMMARY. This converts RESEARCH.md assumptions A2/A3 into verified facts.</action>
  <verify>
    <human-check>On a real device / Expo Go / simulator: the app launches, the temporary keygen probe logs a key length/type (NOT the key), and NO "crypto.getRandomValues must be defined" error appears. Record which device/simulator was used.</human-check>
  </verify>
  <acceptance_criteria>
    - `head -1 digital-app/index.js` (or first non-comment import) is `import 'react-native-get-random-values';` placed before the expo/App imports.
    - `grep -n 'react-native-get-random-values\|eciesjs\|expo-secure-store' digital-app/package.json` shows all three dependencies present; eciesjs range is `^0.5` (or higher), never `^0.3`; expo-secure-store is the version `npx expo install` resolved (recorded in SUMMARY).
    - On-device smoke test passes: a keypair generates with no `crypto.getRandomValues` error (human-check, device/simulator recorded in SUMMARY).
    - The temporary keygen probe is removed from index.js/App after the smoke test (no key material logged in committed code).
  </acceptance_criteria>
  <done>The three deps are installed at SDK-correct versions, the RNG polyfill is the first line of index.js, and on-device keygen is verified to work without the getRandomValues error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create utils/keypair.js (generate + store + retry)</name>
  <read_first>
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-RESEARCH.md (Pattern 2 verbatim code — generateAndStoreKeypair / getStoredPublicKeyHexForRetry; Anti-Patterns: never regenerate on retry, never log the private key)
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-PATTERNS.md (keypair.js "No Analog Found" note lines 501-505 — greenfield, use RESEARCH.md Pattern 2 as starting point)
    - digital-app/index.js (confirm the polyfill is in place from Task 2 before this module's eciesjs import is loaded)
  </read_first>
  <behavior>
    - generateAndStoreKeypair(): creates `new PrivateKey()`, writes `priv.toHex()` to SecureStore under key 'privid_student_privkey' BEFORE returning (D-02), and returns `{ pubKeyHex }` where pubKeyHex = `priv.publicKey.toHex()` (33-byte compressed). It never returns, logs, or otherwise exposes the private key.
    - getStoredPublicKeyHexForRetry(): reads the stored private key hex from SecureStore, throws if none is stored, re-derives via `PrivateKey.fromHex(...)`, and returns `.publicKey.toHex()` — it NEVER generates a new keypair (D-02 retry path).
  </behavior>
  <action>Create `digital-app/utils/keypair.js` from RESEARCH.md Pattern 2 verbatim. Import `{ PrivateKey }` from `'eciesjs'` and `* as SecureStore` from `'expo-secure-store'`. Define `const PRIVATE_KEY_STORAGE_KEY = 'privid_student_privkey'`. Export async `generateAndStoreKeypair()`: `const priv = new PrivateKey(); const privKeyHex = priv.toHex(); const pubKeyHex = priv.publicKey.toHex();` then `await SecureStore.setItemAsync(PRIVATE_KEY_STORAGE_KEY, privKeyHex)` (D-02: write BEFORE any network use), then `return { pubKeyHex }`. Export async `getStoredPublicKeyHexForRetry()`: read the stored hex with `SecureStore.getItemAsync(PRIVATE_KEY_STORAGE_KEY)`, throw `new Error('No stored private key found for retry')` if null, then `PrivateKey.fromHex(privKeyHex).publicKey.toHex()` and return it. Do NOT add any `console.log` of `privKeyHex`, the PrivateKey object, or the storage value. Do NOT add a regenerate-on-retry path (that is the explicit anti-pattern in RESEARCH.md). The pubkey POST itself lives in the screen (plan 07-04), not here — this module is keygen + storage only, no network.</action>
  <verify>
    <automated>cd digital-app && node -e "const s=require('fs').readFileSync('utils/keypair.js','utf8'); if(!/generateAndStoreKeypair/.test(s)||!/getStoredPublicKeyHexForRetry/.test(s)){console.error('MISSING EXPORTS');process.exit(1);} if(!/privid_student_privkey/.test(s)){console.error('NO STORAGE KEY');process.exit(1);} if(/console\.log[^)]*priv/i.test(s)){console.error('LOGS PRIVATE KEY');process.exit(1);} if(/new PrivateKey\(\)/.test(s.split('getStoredPublicKeyHexForRetry')[1]||'')){console.error('REGENERATES ON RETRY');process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `OK` (both exports present; storage key correct; no private-key logging; retry path does NOT call `new PrivateKey()`).
    - `generateAndStoreKeypair` returns ONLY `{ pubKeyHex }` — the private key never appears in the return value.
    - `SecureStore.setItemAsync` is called before the function returns (D-02 ordering).
    - `getStoredPublicKeyHexForRetry` re-derives the pubkey via `PrivateKey.fromHex` from the stored key and never generates a new keypair.
  </acceptance_criteria>
  <done>utils/keypair.js generates a secp256k1 keypair, persists the private key to SecureStore before returning only the pubkey, and re-derives (never regenerates) the pubkey on retry — all with no private-key logging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device secure storage -> app memory | Private key lives in Keystore/Keychain-backed SecureStore; only ever read into memory on-device, never transmitted |
| npm registry -> app bundle | New dependencies (one [ASSUMED]) cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | private key in logs/exports | mitigate | keypair.js never logs/returns the private key; generateAndStoreKeypair returns only { pubKeyHex } |
| T-07-06 | Tampering (weak randomness) | RNG under Hermes | mitigate | react-native-get-random-values delegates to OS CSPRNG; never Math.random(); verified by on-device smoke test (Task 2) |
| T-07-07 | Tampering (insecure storage) | private key at rest | mitigate | expo-secure-store (Android Keystore / iOS Keychain), never AsyncStorage plaintext |
| T-07-SC | Tampering | npm install react-native-get-random-values | mitigate | [ASSUMED] package gated by blocking-human checkpoint (Task 1) before install; eciesjs + expo-secure-store already [OK] in RESEARCH.md audit |
</threat_model>

<verification>
- Blocking human checkpoint clears the [ASSUMED] polyfill package before install.
- On-device smoke test (human-check) confirms the RNG polyfill resolves the getRandomValues gap.
- grep confirms keypair.js exports, storage key, no private-key logging, no regenerate-on-retry.
</verification>

<success_criteria>
- KEY-01 satisfied: app generates a secp256k1 keypair on-device, stores the private key in expo-secure-store, never exports/transmits/logs it.
- The RNG polyfill is verified working on a real device/simulator (RESEARCH.md Open Question 1 closed).
- expo-secure-store is the SDK-53-resolved version; eciesjs is ^0.5.x.
</success_criteria>

<output>
Create `.planning/phases/07-student-keypair-two-phase-enrollment/07-03-SUMMARY.md` when done. Record the resolved expo-secure-store version and the on-device smoke-test result (pass + device used).
</output>
