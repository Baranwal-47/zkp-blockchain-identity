---
phase: 07-student-keypair-two-phase-enrollment
plan: 04
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - digital-app/screens/ClaimCredentialScreen.js
  - digital-app/App.js
  - digital-app/screens/LoginScreen.js
autonomous: false
requirements: [KEY-02]
must_haves:
  truths:
    - "On first login of an awaiting-keypair student, the app auto-redirects from LoginScreen into ClaimCredentialScreen with no confirmation tap (D-01)"
    - "ClaimCredentialScreen generates the keypair + POSTs only the pubkey to /students/:id/pubkey on mount, shown as a loading state"
    - "On success the screen auto-navigates to StudentProfile with the returned active student"
    - "On failure the screen shows an inline 'Try Again' button on the same screen (no logout, no navigate-away) and retry re-sends only the POST, never regenerating the keypair (D-02/D-03)"
    - "ClaimCredentialScreen is registered in the App.js navigation stack with headerLeft:null and gestureEnabled:false"
  artifacts:
    - path: "digital-app/screens/ClaimCredentialScreen.js"
      provides: "auto-triggered claim flow with loading/error states"
      contains: "generateAndStoreKeypair"
    - path: "digital-app/App.js"
      provides: "ClaimCredentialScreen Stack.Screen registration"
      contains: "ClaimCredentialScreen"
    - path: "digital-app/screens/LoginScreen.js"
      provides: "D-01 enrollmentPhase branch after login"
      contains: "enrollmentPhase"
  key_links:
    - from: "digital-app/screens/LoginScreen.js"
      to: "ClaimCredentialScreen"
      via: "navigate on enrollmentPhase === 'awaiting-keypair'"
      pattern: "awaiting-keypair"
    - from: "digital-app/screens/ClaimCredentialScreen.js"
      to: "digital-app/utils/keypair.js"
      via: "generateAndStoreKeypair / getStoredPublicKeyHexForRetry"
      pattern: "generateAndStoreKeypair|getStoredPublicKeyHexForRetry"
    - from: "digital-app/screens/ClaimCredentialScreen.js"
      to: "POST /api/students/:id/pubkey"
      via: "fetch on mount"
      pattern: "/pubkey"
---

<objective>
Build the student-facing claim flow: `ClaimCredentialScreen` (auto-triggered on mount, per the approved 07-UI-SPEC), register it in the navigation stack, and add the D-01 routing branch to `LoginScreen` so an `awaiting-keypair` student is redirected straight into the claim flow with no confirmation tap. The screen generates the keypair via `utils/keypair.js`, POSTs only the pubkey to the Phase 7 backend endpoint, shows loading/error states matching the existing visual language, and on success navigates to StudentProfile. This closes the client half of KEY-02 and wires the two-phase enrollment UX end-to-end.

Purpose: Closes the client half of KEY-02 (app sends only the public key to the backend) and implements D-01 (auto-redirect), D-02 (retry re-sends only the POST), D-03 (inline retry, no logout) per the 07-UI-SPEC copywriting + state-machine contract.
Output: New `ClaimCredentialScreen.js`, modified `App.js`, modified `LoginScreen.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-CONTEXT.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-RESEARCH.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-PATTERNS.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-UI-SPEC.md
@.planning/phases/07-student-keypair-two-phase-enrollment/07-03-SUMMARY.md

<interfaces>
<!-- Contracts from plan 07-03 and existing screens the executor must use directly. -->

From plan 07-03 (digital-app/utils/keypair.js):
  generateAndStoreKeypair(): Promise<{ pubKeyHex }>   // generates + stores priv, returns only pubkey
  getStoredPublicKeyHexForRetry(): Promise<string>    // re-derives pubkey from stored priv (retry path)

Backend endpoint (plan 07-02): POST `${ADMIN_BACKEND_URL}/api/students/:id/pubkey`
  body { pubKeyHex }; 200 -> { status:"success", student } with student.enrollmentPhase === "active";
  409 if already claimed; 400 if pubKeyHex missing/malformed.

LoginScreen.js handleLogin (lines 22-51): POSTs /api/students/login, then
  navigation.navigate('StudentProfile', { student: data.student }).
  D-01 hook: branch on data.student.enrollmentPhase BEFORE that navigate.

App.js Stack.Screen pattern (lines 75-84) + LoadingScreen registration (lines 100-107)
  for headerLeft:null / gestureEnabled:false precedent.

07-UI-SPEC copy (exact strings): header "Setting Up Your Credential" (App.js title per UI-SPEC
  interaction contract may read "Claim Your Credential" — use the UI-SPEC's interaction-contract
  header value); loading heading "Securing your credential…"; loading body "Generating your private
  key on this device. This stays on your phone and is never sent anywhere."; server-wait sub-state
  "Almost done — finalizing your credential…"; error heading "Couldn't Complete Setup"; error body
  "Something went wrong while securing your credential. Check your connection and try again."; retry
  CTA "Try Again". Never surface "ECIES"/"DEK"/"secp256k1" in user-facing copy.

07-UI-SPEC visuals: white card (borderRadius:16, padding:24, shadow recipe shadowColor:'#000',
  shadowOffset:{0,2}, shadowOpacity:0.07, shadowRadius:8, elevation:3) on '#f8fafc'; ActivityIndicator
  tint '#3b82f6'; retry button '#3b82f6' bg white text (matches loginButton); error color '#ef4444'.
  No KeyboardAvoidingView (no text inputs). Plain centered View, flex:1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClaimCredentialScreen.js (auto-triggered claim with loading/error states)</name>
  <read_first>
    - digital-app/screens/LoginScreen.js (the setLoading/try/fetch/response.ok/finally pattern lines 22-51 AND the full styles block lines 125-237 — copy the card/button/text styles)
    - digital-app/utils/keypair.js (generateAndStoreKeypair / getStoredPublicKeyHexForRetry signatures from plan 07-03)
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-UI-SPEC.md (Copywriting Contract + Screen-Specific Interaction Contract state machine — the exact copy strings and per-state visuals)
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-PATTERNS.md (ClaimCredentialScreen section lines 319-402 — the verbatim attemptClaim pattern, imports, useEffect-on-mount)
  </read_first>
  <action>Create `digital-app/screens/ClaimCredentialScreen.js` following the 07-PATTERNS.md ClaimCredentialScreen pattern and the 07-UI-SPEC state machine. Imports: `React, { useState, useEffect }` from 'react'; `{ View, Text, TouchableOpacity, StyleSheet, ActivityIndicator }` from 'react-native' (NO KeyboardAvoidingView/TextInput — there is no text input); `{ ADMIN_BACKEND_URL }` from '../environment'; `{ generateAndStoreKeypair, getStoredPublicKeyHexForRetry }` from '../utils/keypair'. Read `const { student } = route.params`. State: `status` ('loading' | 'error'), `errorMessage`, `hasGeneratedKey` (boolean). Define `attemptClaim`: set status 'loading'; `const pubKeyHex = hasGeneratedKey ? await getStoredPublicKeyHexForRetry() : (await generateAndStoreKeypair()).pubKeyHex;` then `setHasGeneratedKey(true)` (D-02: generate once, retry re-sends only the POST). Then `fetch(\`${ADMIN_BACKEND_URL}/api/students/${student.id}/pubkey\`, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ pubKeyHex }) })`; parse json; if `!response.ok` throw `new Error(data.message || 'Claim failed')`; on success `navigation.navigate('StudentProfile', { student: data.student })`. On catch: `setErrorMessage(error.message || <UI-SPEC error body>)` and `setStatus('error')` — do NOT Alert.alert, do NOT navigate away, do NOT log out (D-03). Fire `useEffect(() => { attemptClaim(); }, [])` (D-01: on mount, no confirmation tap). Render: loading state = centered white card with ActivityIndicator (color '#3b82f6') + heading "Securing your credential…" + the UI-SPEC loading body copy; error state = white card with error heading "Couldn't Complete Setup" (color '#ef4444'), the UI-SPEC error body, and a single "Try Again" TouchableOpacity ('#3b82f6' bg, white text, matching loginButton style) wired to `attemptClaim`. Use the exact UI-SPEC copy strings verbatim; never surface "ECIES"/"DEK"/"secp256k1". Copy the card/shadow/button/text styles from LoginScreen.js's StyleSheet (white card borderRadius:16 padding:24, shadow recipe per UI-SPEC) on a '#f8fafc' background, plain centered View flex:1.</action>
  <verify>
    <automated>cd digital-app && node -e "const s=require('fs').readFileSync('screens/ClaimCredentialScreen.js','utf8'); const need=['generateAndStoreKeypair','getStoredPublicKeyHexForRetry','/pubkey','useEffect','Try Again','Securing your credential','Couldn','t Complete Setup'.replace('t','')]; for(const n of ['generateAndStoreKeypair','getStoredPublicKeyHexForRetry','/pubkey','useEffect','Try Again','Securing your credential']){ if(!s.includes(n)){console.error('MISSING: '+n);process.exit(1);} } if(/Alert\.alert/.test(s)){console.error('USES Alert.alert (D-03 violation)');process.exit(1);} if(/ECIES|secp256k1|\\bDEK\\b/.test(s)){console.error('LEAKS CRYPTO JARGON IN COPY');process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `OK` (uses both keypair util fns, POSTs to /pubkey, fires on mount via useEffect, has the "Try Again" CTA and the exact loading heading, and does NOT use Alert.alert or leak crypto jargon).
    - On mount the screen calls `generateAndStoreKeypair` (first attempt) and on retry calls `getStoredPublicKeyHexForRetry` (never regenerating) — `hasGeneratedKey` gates this.
    - The error path sets inline error state + renders a "Try Again" button on the same screen; it does NOT navigate away or log out.
    - On a 200 it navigates to StudentProfile with the returned active student.
    - Copy strings match 07-UI-SPEC verbatim; no "ECIES"/"DEK"/"secp256k1" in user-facing text.
  </acceptance_criteria>
  <done>ClaimCredentialScreen auto-fires keygen+pubkey-POST on mount, shows the UI-SPEC loading/error states, retries POST-only, and navigates to StudentProfile on success.</done>
</task>

<task type="auto">
  <name>Task 2: Register ClaimCredentialScreen in App.js and add D-01 routing to LoginScreen</name>
  <read_first>
    - digital-app/App.js (the Stack.Screen registration block lines 75-84 and the LoadingScreen registration lines 100-107 for the headerLeft:null/gestureEnabled:false precedent; the screen import lines near line 7)
    - digital-app/screens/LoginScreen.js (handleLogin lines 22-51 — the exact navigation.navigate('StudentProfile', ...) call to branch before)
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-PATTERNS.md (App.js section lines 406-431; LoginScreen branch hook noted in code_context)
    - .planning/phases/07-student-keypair-two-phase-enrollment/07-UI-SPEC.md (Header registration note line 130 — title, headerLeft:null, gestureEnabled:false)
  </read_first>
  <action>In `digital-app/App.js`: add `import ClaimCredentialScreen from './screens/ClaimCredentialScreen';` alongside the other screen imports (~line 7). Add a `<Stack.Screen name="ClaimCredentialScreen" component={ClaimCredentialScreen} options={{ title: 'Claim Your Credential', headerLeft: null, gestureEnabled: false }} />` between the LoginScreen and StudentProfile registrations (logical order login -> claim -> profile), mirroring the LoadingScreen options that prevent back-navigation mid-flow (D-01/D-06 one-time gate). In `digital-app/screens/LoginScreen.js::handleLogin`, add the D-01 branch immediately BEFORE the existing `navigation.navigate('StudentProfile', { student: data.student })`: `if (data.student.enrollmentPhase === 'awaiting-keypair') { navigation.navigate('ClaimCredentialScreen', { student: data.student }); return; }` then fall through to the existing StudentProfile navigation for active students. Do not change the login fetch/error handling otherwise.</action>
  <verify>
    <automated>cd digital-app && node -e "const a=require('fs').readFileSync('App.js','utf8'); const l=require('fs').readFileSync('screens/LoginScreen.js','utf8'); if(!a.includes('ClaimCredentialScreen')){console.error('NOT REGISTERED IN App.js');process.exit(1);} if(!/headerLeft:\s*null/.test(a.split('ClaimCredentialScreen').slice(-2).join('ClaimCredentialScreen'))&&!/headerLeft:\s*null/.test(a)){console.error('NO headerLeft:null');process.exit(1);} if(!/gestureEnabled:\s*false/.test(a)){console.error('NO gestureEnabled:false');process.exit(1);} if(!l.includes('awaiting-keypair')||!l.includes('ClaimCredentialScreen')){console.error('NO D-01 BRANCH IN LoginScreen');process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'ClaimCredentialScreen' digital-app/App.js` shows both the import and a Stack.Screen registration.
    - The registration has `headerLeft: null` and `gestureEnabled: false` (no back-swipe mid-claim).
    - `grep -n "awaiting-keypair" digital-app/screens/LoginScreen.js` shows the D-01 branch that navigates to ClaimCredentialScreen before the StudentProfile navigate.
    - Active students still route straight to StudentProfile (the existing navigate is the fall-through).
    - The verify command prints `OK`.
  </acceptance_criteria>
  <done>ClaimCredentialScreen is registered (no back-nav) and an awaiting-keypair student is auto-redirected into it on login, while active students go straight to StudentProfile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end claim flow verification on device</name>
  <what-built>The complete two-phase enrollment UX: admin enrolls a student (Phase 6 + plan 07-01 -> awaiting-keypair), student logs in on the app, gets auto-redirected into ClaimCredentialScreen, the app generates a keypair on-device and POSTs only the pubkey, the backend (plan 07-02) wraps the DEK + pins the envelope + wipes the plaintext DEK + flips to active, and the app lands on StudentProfile.</what-built>
  <how-to-verify>
    1. Ensure the backend (plans 07-01 + 07-02) is running and an `awaiting-keypair` student exists (seed/re-seed per D-07 — DB can be freely wiped).
    2. In the app, log in as that student. Confirm: no confirmation tap is needed — you are auto-redirected into the claim screen showing "Securing your credential…".
    3. Confirm the flow completes and lands on StudentProfile with the student now `active`.
    4. In Mongo, confirm the student record now has `dek` absent/null, `dekEnvelopeCID` populated, `pubKey` set, and `enrollmentPhase: "active"`.
    5. Log out and back in as the now-active student: confirm you go straight to StudentProfile (NOT the claim screen) — the D-01 branch only fires for awaiting-keypair.
    6. (Error path) Temporarily stop the backend, trigger a claim, and confirm the screen shows "Couldn't Complete Setup" with a "Try Again" button (no logout, no crash). Restart backend, tap "Try Again", confirm it completes.
  </how-to-verify>
  <resume-signal>Type "approved" once the end-to-end claim succeeds, the Mongo record shows dek wiped + envelope set + active, an already-active student skips the claim screen, and the error/retry path works. Otherwise describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> admin-backend API | App sends only the pubkey over HTTP; never the private key (KEY-02) |
| navigation params -> screen | student object passed via route params (existing app pattern; no new auth surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | private key leaving device | mitigate | Screen only ever sends pubKeyHex; keypair.js (plan 07-03) keeps the private key in SecureStore; no key logging |
| T-07-08 | Tampering | regenerate-on-retry orphaning the stored key | mitigate | Retry path uses getStoredPublicKeyHexForRetry (re-derive, never new keypair) gated by hasGeneratedKey (D-02) |
| T-07-09 | Availability/UX | back-nav or logout mid-claim leaving inconsistent state | accept/mitigate | gestureEnabled:false + headerLeft:null block back-nav; error path stays on-screen with retry, never logs out (D-03); no server state changes until POST succeeds |
</threat_model>

<verification>
- grep/node static checks confirm screen behavior, navigation registration, and D-01 branch.
- Blocking human-verify checkpoint walks the full admin-enroll -> login -> claim -> active path on a device, confirms the Mongo record state transition, the already-active skip, and the error/retry path.
</verification>

<success_criteria>
- KEY-02 (client) satisfied: the app sends only the public key to POST /students/:id/pubkey.
- D-01/D-02/D-03 satisfied: auto-redirect with no tap, retry re-sends POST only, inline error with no logout.
- End-to-end two-phase enrollment verified on-device: awaiting-keypair -> claim -> active, with the plaintext DEK wiped server-side.
</success_criteria>

<output>
Create `.planning/phases/07-student-keypair-two-phase-enrollment/07-04-SUMMARY.md` when done.
</output>
