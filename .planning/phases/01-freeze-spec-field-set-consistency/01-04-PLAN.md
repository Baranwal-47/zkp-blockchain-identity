---
phase: 01-freeze-spec-field-set-consistency
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - privdId_admin/backend/scripts/reseed.js
autonomous: false
requirements: [SPEC-02]
must_haves:
  truths:
    - "Re-seeding a test student and recomputing the commitment yields the same Merkle root from the issuance path and the prover-side leaf computation."
    - "The parity gate is exercised for a student whose name > 31 bytes and a student whose email > 31 bytes (the 2-chunk path)."
    - "The re-seed set covers predicate edge cases: over-18 and under-18 DOBs, at least one postgrad (level ∈ {4,5,6}) and one undergrad, a spread of disciplines."
  decisions: [D-12]
  artifacts:
    - path: "privdId_admin/backend/scripts/reseed.js"
      provides: "Wipe + re-seed test students; root-equality assertion gate"
      contains: "assert.strictEqual"
  key_links:
    - from: "privdId_admin/backend/scripts/reseed.js"
      to: "privdId_admin/backend/utils/identityCommitment.js"
      via: "prover-side recomputation imports the same shared module as issuance"
      pattern: "computeMerkleRoot"
---

<objective>
Build the wipe-and-reseed script that is the central acceptance gate for Phase 1 (success criterion 3): re-seed a test student through the new issuance path, then independently recompute the Merkle root from the same attrs + persisted salts using the shared module (the prover-side recomputation), and assert the two roots are identical. This is the JS reference/test that proves both paths produce the same root — it does NOT touch the live ZKP backend prover endpoint (Phase 4). The re-seed set covers the predicate edge cases downstream phases need.

Purpose: Concretely prove the §1.4 drift is dead — issuance and the prover-side leaf computation converge on one root, including the multi-chunk string path.
Output: `privdId_admin/backend/scripts/reseed.js` (Claude's-discretion location chosen: backend/scripts/, ESM, resolving RESEARCH open question #3 — backend/scripts/ is cleanest for ESM and imports the shared module directly).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md
@.planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@privdId_admin/backend/utils/identityCommitment.js

<interfaces>
<!-- Contracts from plans 02 + 03. -->

From privdId_admin/backend/utils/identityCommitment.js:
  hashToField(str, maxChunks) -> Promise<decimal string>
  generateSalts(count=7) -> string[]
  computeMerkleRoot(attrs /*7 encoded leaf values, frozen order*/, salts) -> Promise<decimal string>
  CHUNK_COUNTS = { name: 4, rollNo: 2, email: 2 }

From privdId_admin/backend/constants/enumCodes.js:
  PROGRAMME_LEVEL, DISCIPLINE

Frozen leaf order (same as issuance buildStudentRecord):
  [ hashToField(name,4), hashToField(rollNo,2), String(dobInt),
    String(PROGRAMME_LEVEL[level]), String(DISCIPLINE[disc]), String(batch),
    hashToField(email,2) ]

Mongoose connect pattern (config/db.js): mongoose.connect(process.env.MONGO_URI)
Student model: privdId_admin/backend/models/Student.js (default export), now persists salts[] + merkleRoot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the wipe-and-reseed script with the root-equality acceptance gate</name>
  <files>privdId_admin/backend/scripts/reseed.js</files>
  <read_first>
    - .planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md (reseed.js section: ESM script structure, top-level async main, mongoose connect/disconnect, assert.strictEqual gate)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md (Validation Architecture: root-equality test pattern; D-12 re-seed coverage; the >31-byte email parity requirement)
    - privdId_admin/backend/utils/identityCommitment.js (the shared module)
    - privdId_admin/backend/constants/enumCodes.js (enum codes)
    - privdId_admin/backend/models/Student.js (schema with salts[]/merkleRoot)
  </read_first>
  <action>
    Create privdId_admin/backend/scripts/reseed.js as an ESM script (import, .js extensions; standalone, run with `node scripts/reseed.js` from backend/). Structure: import { hashToField, generateSalts, computeMerkleRoot, CHUNK_COUNTS } from "../utils/identityCommitment.js"; import { PROGRAMME_LEVEL, DISCIPLINE } from "../constants/enumCodes.js"; import Student from "../models/Student.js"; import mongoose from "mongoose"; import assert from "assert"; import dotenv from "dotenv"; dotenv.config(). Top-level async main() invoked at the bottom via main().catch(e => { console.error(e); process.exit(1); }).

    In main(): connect via mongoose.connect(process.env.MONGO_URI). Wipe with await Student.deleteMany({}) (per D-12 — no migration). Define a SEED array covering the D-12 edge cases: at least one over-18 DOB and one under-18 DOB (compute relative to 2026-06-16 so the under-18 case is unambiguous); at least one postgrad (programmeLevel ∈ {M.Tech, M.Des, PhD}) and one undergrad (B.Tech/B.Des/Dual); a spread of disciplines; include one student whose name is > 31 bytes (e.g. "Rajesh Kumar Sharma Gupta Verma Singh", 37 bytes → 2-chunk path) and one whose email is > 31 bytes (e.g. "utkarshbaranwal47@students.iiitdmj.ac.in", 40 bytes → 2-chunk path).

    For each seed student: derive dobInt from the YYYY-MM-DD dob (parseInt(dob.replace(/-/g,""),10)); salts = generateSalts(7); build the ISSUANCE attrs in the frozen leaf order [ await hashToField(name, CHUNK_COUNTS.name), await hashToField(rollNo, CHUNK_COUNTS.rollNo), String(dobInt), String(PROGRAMME_LEVEL[programmeLevel]), String(DISCIPLINE[discipline]), String(batch), await hashToField(email, CHUNK_COUNTS.email) ]; const merkleRoot = await computeMerkleRoot(attrs, salts); persist via Student.create({ name, email, rollNo, programme: `${programmeLevel} ${discipline}`, programmeLevel, discipline, batch, dob, dobInt, contactNo, salts, merkleRoot, ... }).

    PROVER-SIDE RECOMPUTATION + GATE: after each student is created, independently rebuild attrs from the persisted/known field values (re-running hashToField + enum lookups, NOT reusing the issuance attrs array variable) and recompute const recomputed = await computeMerkleRoot(reAttrs, createdStudent.salts); then assert.strictEqual(recomputed, createdStudent.merkleRoot, `ROOT MISMATCH for ${rollNo}: issuance and prover-side paths diverged`). This independent recomputation reading back the stored salts is what proves both paths converge (it stands in for the Phase-2 prover). The script MUST exercise the gate for the >31-byte name student and the >31-byte email student. Print a summary line per student (rollNo, merkleRoot prefix, PASS) and a final count. Disconnect with await mongoose.disconnect() in a finally block. Do NOT import or modify zkp-backend/server.js (Phase 4); the prover-side path here is this script's in-process recomputation.

    Note: this script requires a live MongoDB (process.env.MONGO_URI). If MONGO_URI is unset, fail fast with a clear message rather than hanging.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/privdId_admin/backend && node --check scripts/reseed.js && grep -q "assert.strictEqual" scripts/reseed.js && grep -q "deleteMany" scripts/reseed.js && grep -q "computeMerkleRoot" scripts/reseed.js && grep -q "Rajesh Kumar Sharma Gupta Verma Singh" scripts/reseed.js && grep -q "students.iiitdmj.ac.in" scripts/reseed.js</automated>
  </verify>
  <done>reseed.js parses cleanly; wipes (deleteMany) and re-seeds; the SEED set includes a >31-byte name and a >31-byte email plus postgrad/undergrad and over-18/under-18 cases; each student runs an independent prover-side recomputation and asserts root equality via assert.strictEqual against the stored merkleRoot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run reseed.js against live MongoDB and confirm the root-equality gate passes</name>
  <what-built>An ESM re-seed script that wipes test students, issues new 7-attribute salted-Merkle commitments, and asserts the prover-side recomputation reproduces each stored merkleRoot — the Phase-1 acceptance gate (success criterion 3).</what-built>
  <how-to-verify>
    1. Ensure MongoDB is running and `MONGO_URI` is set in privdId_admin/backend/.env (the script reads it via dotenv).
    2. From a real WSL shell (not a Windows path), run with a relative path:
       `cd privdId_admin/backend && node scripts/reseed.js`
    3. Expected: every seeded student prints PASS (no `ROOT MISMATCH` thrown), the run exits 0, and the final count matches the SEED array length. The assertion MUST pass specifically for the >31-byte name student and the >31-byte email student.
    4. Optional spot-check: `mongosh "$MONGO_URI" --eval "db.students.find({},{rollNo:1,merkleRoot:1,salts:1}).limit(2).pretty()"` shows non-null merkleRoot and a 7-element salts array per student.
  </how-to-verify>
  <resume-signal>Type "approved" if all students PASS and the script exits 0, or paste the ROOT MISMATCH / error output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issuance path ↔ prover-side recomputation | The whole phase succeeds only if these two independent computations of the root agree; this script is the gate that proves it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | issuance and prover-side leaf computation silently diverge (the §1.4 failure) | mitigate | assert.strictEqual root-equality gate run per student; both paths import the same shared module (D-08) |
| T-01-13 | Tampering | multi-chunk string path (>31 bytes) diverges while short strings pass | mitigate | Gate mandatorily exercised for a >31-byte name and a >31-byte email (RESEARCH parity requirement) |
| T-01-14 | Information disclosure | re-seed lacks predicate edge cases, hiding a Phase-2 predicate bug | mitigate | SEED set covers over-18/under-18 DOBs, postgrad/undergrad, discipline spread (D-12) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new dependencies (RESEARCH: circomlibjs@0.1.7 + Node built-ins only); no install task exists, so the supply-chain checkpoint is not triggered |
</threat_model>

<verification>
- reseed.js parses, wipes, and re-seeds with the required edge-case coverage.
- The root-equality gate (assert.strictEqual) passes for every student including the >31-byte name and >31-byte email cases (confirmed by the human-verify run).
- Stored merkleRoot is non-null and salts[] has 7 entries per student.
</verification>

<success_criteria>
- SPEC-02 verification gate satisfied: re-seeding a test student and recomputing the commitment yields the same root from the issuance path and the prover-side recomputation, including the multi-chunk string path.
</success_criteria>

<output>
Create `.planning/phases/01-freeze-spec-field-set-consistency/01-04-SUMMARY.md` when done.
</output>
