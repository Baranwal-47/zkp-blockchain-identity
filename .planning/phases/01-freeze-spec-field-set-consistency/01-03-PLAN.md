---
phase: 01-freeze-spec-field-set-consistency
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - privdId_admin/backend/models/Student.js
  - privdId_admin/backend/validators/studentValidator.js
  - privdId_admin/backend/services/studentService.js
  - privdId_admin/backend/services/credentialService.js
autonomous: true
requirements: [SPEC-02, SPEC-03]
must_haves:
  truths:
    - "The admin issuance hash consumes the identical 7-attribute list, order, and encoding the prover will use: programme split into programmeLevel+discipline, phone dropped for email, dob and batch as integers."
    - "Both hash-recompute sites (buildStudentRecord and updateStudent) use the shared commitment module — no flat-Poseidon(5) path survives."
    - "The credential issuer string reads 'PrivdID — IIITDM Jabalpur' with no VIT references in issuance output, version bumped to 2.0."
  artifacts:
    - path: "privdId_admin/backend/models/Student.js"
      provides: "programmeLevel, discipline, batch, dobInt, salts[], merkleRoot schema fields"
      contains: "merkleRoot"
    - path: "privdId_admin/backend/validators/studentValidator.js"
      provides: "programmeLevel/discipline enums, batch, email ≤62-byte cap"
      contains: "programmeLevel"
    - path: "privdId_admin/backend/services/studentService.js"
      provides: "buildStudentRecord + updateStudent using computeMerkleRoot"
      contains: "computeMerkleRoot"
    - path: "privdId_admin/backend/services/credentialService.js"
      provides: "IIITDM issuer string, merkleRoot anchoring, version 2.0"
      contains: "IIITDM Jabalpur"
  key_links:
    - from: "privdId_admin/backend/services/studentService.js"
      to: "privdId_admin/backend/utils/identityCommitment.js"
      via: "import + computeMerkleRoot in both buildStudentRecord and updateStudent"
      pattern: "computeMerkleRoot"
    - from: "privdId_admin/backend/services/studentService.js"
      to: "privdId_admin/backend/constants/enumCodes.js"
      via: "PROGRAMME_LEVEL / DISCIPLINE code lookup"
      pattern: "PROGRAMME_LEVEL"
    - from: "privdId_admin/backend/services/credentialService.js"
      to: "registry issueCredential"
      via: "ethers.zeroPadValue(toBeHex(BigInt(merkleRoot)),32)"
      pattern: "zeroPadValue"
---

<objective>
Refactor the admin issuance path from the old 5-field flat Poseidon hash (`[name, rollNo, dob, normalizePhone(contactNo), programme]`) to the new 7-attribute salted-Merkle commitment computed by the shared module. This is where SPEC-02 (byte-for-byte field-set parity) and SPEC-03 (branding) land: the Student schema gains the new fields, the validator enforces the new inputs + email cap, BOTH hash-recompute sites (create and update) call computeMerkleRoot, and the credential issuer string + version are corrected.

Purpose: Eliminate the §1.4 programme/branch mismatch at the source by making issuance commit exactly the spec's 7 attributes in the frozen order/encoding, and remove VIT branding.
Output: updated Student model, validator, studentService (both recompute sites), and credentialService.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md
@.planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@privdId_admin/backend/services/studentService.js
@privdId_admin/backend/services/credentialService.js
@privdId_admin/backend/models/Student.js
@privdId_admin/backend/validators/studentValidator.js

<interfaces>
<!-- Contracts produced by plans 01 + 02. Use directly. -->

From privdId_admin/backend/utils/identityCommitment.js (plan 02):
  hashToField(str, maxChunks) -> Promise<decimal string>
  generateSalts(count=7) -> string[]
  computeMerkleRoot(attrs /*7 encoded leaf values, frozen order*/, salts) -> Promise<decimal string>
  CHUNK_COUNTS = { name: 4, rollNo: 2, email: 2 }

From privdId_admin/backend/constants/enumCodes.js (plan 01):
  PROGRAMME_LEVEL = { "B.Tech":1,"B.Des":2,"Dual":3,"M.Tech":4,"M.Des":5,"PhD":6 }
  DISCIPLINE = { "CSE":1,"ECE":2,"ME":3,"SmartMfg":4,"Design":5,"NatSci":6 }

Frozen leaf order for the attrs array passed to computeMerkleRoot:
  [ hashToField(name,4), hashToField(rollNo,2), String(dobInt),
    String(PROGRAMME_LEVEL[programmeLevel]), String(DISCIPLINE[discipline]),
    String(batch), hashToField(email,2) ]

Current recompute sites (confirmed line numbers):
  studentService.js buildStudentRecord hash block ~L72-78
  studentService.js updateStudent allowedFields ~L171 + hash block ~L181-187
  credentialService.js issuer string L67, version L68, bytes32 conv L40
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Student schema and Joi validator for the new fields</name>
  <files>privdId_admin/backend/models/Student.js, privdId_admin/backend/validators/studentValidator.js</files>
  <read_first>
    - privdId_admin/backend/models/Student.js (current schema fields)
    - privdId_admin/backend/validators/studentValidator.js (current Joi studentSchema lines 3-10, validateStudentPayload lines 12-16)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md (Student.js + studentValidator.js sections with exact field shapes)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md (open questions #2 dob and #3 programme retention, #4 email cap)
  </read_first>
  <action>
    In Student.js, add these fields inside studentSchema (all required: false to stay lenient for the wipe-and-reseed in plan 04, per RESEARCH recommendation): programmeLevel { type: String, trim: true }, discipline { type: String, trim: true }, batch { type: Number, default: null }, dobInt { type: Number, default: null } (YYYYMMDD integer used for commitment), salts { type: [String], default: [] } (7 decimal strings, leaf-index order), merkleRoot { type: String, default: null } (decimal pubHash). Resolve RESEARCH open question #2: keep dob as the existing String display field AND add dobInt as the committed integer (do not change dob's type). Resolve open question #3: RETAIN programme as an optional legacy/display String (no longer committed); RETAIN contactNo as optional operational data (no longer committed). Keep the existing hashedData field present but it is no longer written by the new path. Do NOT touch the model export.

    In studentValidator.js, extend studentSchema: keep name min(2).max(120).required(); change email to Joi.string().trim().email().max(62).required() (the ≤62-byte cap enforces the 2-chunk email contract — RESEARCH open question #4); keep rollNo min(1).max(50).required(); change programme to optional (no longer committed); ADD programmeLevel = Joi.string().valid("B.Tech","B.Des","Dual","M.Tech","M.Des","PhD").required(); ADD discipline = Joi.string().valid("CSE","ECE","ME","SmartMfg","Design","NatSci").required(); ADD batch = Joi.number().integer().min(1990).max(2100).required(); change contactNo to optional; keep dob as Joi.string().trim().allow("").optional(). Keep validateStudentPayload (abortEarly:false, stripUnknown:true) unchanged. The valid() enum strings MUST match the enumCodes.js keys exactly.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/privdId_admin/backend && node --input-type=module -e "import('./validators/studentValidator.js').then(m=>{const bad=m.validateStudentPayload({name:'Al',email:'a@b.co',rollNo:'21BCS027',programmeLevel:'X.Tech',discipline:'CSE',batch:2022}); const okPayload=m.validateStudentPayload({name:'Alice Iyer',email:'alice@students.iiitdmj.ac.in',rollNo:'21BCS027',programmeLevel:'M.Tech',discipline:'CSE',batch:2022,dob:'2004-02-15'}); const longEmail=m.validateStudentPayload({name:'Alice Iyer',email:('x'.repeat(60)+'@students.iiitdmj.ac.in'),rollNo:'21BCS027',programmeLevel:'M.Tech',discipline:'CSE',batch:2022}); const ok = bad.error && !okPayload.error && longEmail.error; if(!ok)process.exit(1); console.log('validator ok');})" && grep -q "merkleRoot" /home/chetan/digital_id_app/privdId_admin/backend/models/Student.js && grep -q "dobInt" /home/chetan/digital_id_app/privdId_admin/backend/models/Student.js</automated>
  </verify>
  <done>Student.js has programmeLevel, discipline, batch, dobInt, salts[], merkleRoot; validator rejects an invalid programmeLevel and an email > 62 bytes, accepts a valid 7-field payload; dob stays String, programme/contactNo are optional.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor both hash-recompute sites to the shared commitment module</name>
  <files>privdId_admin/backend/services/studentService.js</files>
  <read_first>
    - privdId_admin/backend/services/studentService.js (imports L1-6, normalizeStudentInput L23-32, buildStudentRecord L66-85, createStudent issueCredentialOnChain call L102-116, updateStudent allowedFields L171 + hash block L181-187)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md (studentService.js REFACTOR section: exact import additions, normalizeStudentInput dobInt parse, buildStudentRecord replacement, updateStudent replacement, allowedFields extension, error-guard pattern)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md (Pitfall 3 updateStudent also recomputes; Code Examples target seven-attribute block)
    - privdId_admin/backend/utils/identityCommitment.js (the module from plan 02)
    - privdId_admin/backend/constants/enumCodes.js (the module from plan 01)
  </read_first>
  <action>
    Add imports (ESM, .js extensions): import { hashToField, generateSalts, computeMerkleRoot, CHUNK_COUNTS } from "../utils/identityCommitment.js"; import { PROGRAMME_LEVEL, DISCIPLINE } from "../constants/enumCodes.js". Keep the existing hashPoseidonFields import for any legacy caller but do not call it from the new path.

    Extend normalizeStudentInput: parse dob "YYYY-MM-DD" → dobInt integer via parseInt(dobStr.replace(/-/g,""),10) (null if empty); pass through programmeLevel, discipline, batch (Number), email (already present); RETAIN programme and contactNo as non-committed display fields. Keep name/email/rollNo trims as-is.

    Replace the buildStudentRecord hash block (currently L72-78, the hashPoseidonFields([name, rollNo, dob, normalizePhone(contactNo), programme]) call): add input guards mirroring the existing dob guard (if (!normalizedStudent.programmeLevel) throw new AppError("programmeLevel is required to issue a credential.", 400); same for discipline and batch; also guard dobInt). Compute salts = generateSalts(7); build attrs in the frozen leaf order = [ await hashToField(name, CHUNK_COUNTS.name), await hashToField(rollNo, CHUNK_COUNTS.rollNo), String(dobInt), String(PROGRAMME_LEVEL[programmeLevel]), String(DISCIPLINE[discipline]), String(batch), await hashToField(email, CHUNK_COUNTS.email) ]. If PROGRAMME_LEVEL[programmeLevel] or DISCIPLINE[discipline] is undefined, throw new AppError(`Unknown programmeLevel/discipline`, 400). const merkleRoot = await computeMerkleRoot(attrs, salts). Return { ...normalizedStudent, merkleRoot, salts, password: temporaryPassword } (drop hashedData from the return).

    Update createStudent: persist the new fields on Student.create (programmeLevel, discipline, batch, dobInt, salts, merkleRoot) and change the issueCredentialOnChain({...}) call (currently L104-109) to pass { rollNo: student.rollNo, email: student.email, merkleRoot: student.merkleRoot } (drop programme + hashedData). Keep the non-blocking try/catch anchoring pattern verbatim. Apply the same persistence to insertBulkStudents if it builds records via buildStudentRecord (it converges there).

    Update updateStudent (the SECOND recompute site — RESEARCH Pitfall 3): extend allowedFields (currently L171 ["name","programme","contactNo","dob"]) to ["name","programmeLevel","discipline","batch","contactNo","dob","programme"]; replace the hash block (L181-187, currently hashPoseidonFields([student.name, student.rollNo, student.dob, normalizePhone(student.contactNo), student.programme])) with the identical attrs/salts/computeMerkleRoot sequence as buildStudentRecord, re-parsing dobInt from student.dob and re-generating salts, then setting student.salts and student.merkleRoot. Update its issueCredentialOnChain call to pass { rollNo, email, merkleRoot } too. normalizePhone is no longer part of either commitment path; leave the function defined (it may be used elsewhere for display) but unused by commitment.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/privdId_admin/backend && node --check services/studentService.js && grep -c "computeMerkleRoot" services/studentService.js | grep -qv '^0$' && test "$(grep -c 'computeMerkleRoot' services/studentService.js)" -ge 2 && test "$(grep -vE '^\s*//' services/studentService.js | grep -c 'hashPoseidonFields(')" -eq 0 && grep -q "PROGRAMME_LEVEL\[" services/studentService.js</automated>
  </verify>
  <done>studentService.js parses+imports cleanly; computeMerkleRoot appears in both buildStudentRecord and updateStudent (≥2 call sites); no live (non-comment) hashPoseidonFields() call survives in the commitment path; enum codes are looked up; issueCredentialOnChain is passed merkleRoot.</done>
</task>

<task type="auto">
  <name>Task 3: Branding cleanup and merkleRoot anchoring in credentialService.js (SPEC-03)</name>
  <files>privdId_admin/backend/services/credentialService.js</files>
  <read_first>
    - privdId_admin/backend/services/credentialService.js (issueCredentialOnChain L60-77, anchorOnChain L34-46, bytes32 conv L40, issuer L67, version L68)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md (credentialService.js PATCH section)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-CONTEXT.md (D-11 exact issuer string + version bump)
  </read_first>
  <action>
    In issueCredentialOnChain (L60-77): change the credential object to { rollNo: student.rollNo, email: student.email, merkleRoot: student.merkleRoot, issuedAt: new Date().toISOString(), issuer: 'PrivdID — IIITDM Jabalpur', type: 'StudentIdentityCredential', version: '2.0' }. Drop the programme and hashedData fields. The issuer string is exactly 'PrivdID — IIITDM Jabalpur' (em-dash U+2014, matching D-11). Bump version '1.0' → '2.0'. Change the anchorOnChain call to pass student.merkleRoot instead of student.hashedData. In anchorOnChain (L34-46), rename the third parameter hashedData → merkleRoot; the bytes32 conversion stays identical: ethers.zeroPadValue(ethers.toBeHex(BigInt(merkleRoot)), 32). Scan the entire file (and any string it emits) for remaining "VIT" or "Bhopal" references and remove them. Do not change pinToIPFS, revokeCredentialOnChain, or the ESM __dirname block.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/privdId_admin/backend && node --check services/credentialService.js && grep -q "PrivdID — IIITDM Jabalpur" services/credentialService.js && test "$(grep -ciE 'VIT|Bhopal' services/credentialService.js)" -eq 0 && grep -q "version: '2.0'" services/credentialService.js && grep -q "zeroPadValue" services/credentialService.js</automated>
  </verify>
  <done>credentialService.js parses cleanly; issuer string is exactly 'PrivdID — IIITDM Jabalpur'; no VIT/Bhopal references remain; version is '2.0'; the credential is built from merkleRoot; the bytes32 zeroPadValue conversion is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form input → committed leaf set | The 7 committed attributes must match the prover's input list/order/encoding exactly or on-chain verification silently fails |
| issuance hash site duplication (create vs update) | Two recompute sites are a drift vector if only one is migrated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | updateStudent left on the old flat-Poseidon(5) path while create is migrated (RESEARCH Pitfall 3) | mitigate | Task 2 explicitly migrates BOTH sites to computeMerkleRoot; gate asserts ≥2 call sites and zero live hashPoseidonFields() calls |
| T-01-09 | Tampering | wrong enum code committed (string→code lookup miss) | mitigate | enum lookup throws AppError on undefined code; validator restricts inputs to the exact enum strings before they reach the service |
| T-01-10 | Repudiation | stale VIT issuer string in issued credential (SPEC-03) | mitigate | Issuer set to exact 'PrivdID — IIITDM Jabalpur'; gate greps for zero VIT/Bhopal references |
| T-01-11 | Information disclosure | email > 62 bytes silently truncated to wrong arity at issuance | mitigate | Joi email .max(62) cap rejects oversized emails at input (RESEARCH open question #4); gate asserts a 60+char email is rejected |
</threat_model>

<verification>
- Both buildStudentRecord and updateStudent call computeMerkleRoot; no live flat-Poseidon(5) path remains.
- Schema persists programmeLevel, discipline, batch, dobInt, salts[], merkleRoot.
- Validator enforces enum membership and the email ≤62-byte cap.
- Issuer string is 'PrivdID — IIITDM Jabalpur'; zero VIT references; version 2.0; bytes32 conversion intact.
</verification>

<success_criteria>
- SPEC-02 satisfied: admin issuance commits the identical 7-attribute list/order/encoding the prover will consume (programme split, phone→email, integer dob/batch).
- SPEC-03 satisfied: issuer string corrected, VIT references removed from issuance output.
</success_criteria>

<output>
Create `.planning/phases/01-freeze-spec-field-set-consistency/01-03-SUMMARY.md` when done.
</output>
