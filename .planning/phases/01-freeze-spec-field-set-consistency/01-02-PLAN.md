---
phase: 01-freeze-spec-field-set-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - privdId_admin/backend/utils/identityCommitment.js
autonomous: true
requirements: [SPEC-01, SPEC-02]
must_haves:
  truths:
    - "A single shared JS module computes hash-to-field string encoding, salt generation, salted Poseidon(2) leaves, and the depth-3 Merkle root."
    - "The module reproduces the verified RESEARCH test vectors exactly (parity oracle), including the >31-byte multi-chunk path."
  decisions: [D-01, D-02, D-08, D-09, D-10]
  artifacts:
    - path: "privdId_admin/backend/utils/identityCommitment.js"
      provides: "hashToField, generateSalt, generateSalts, computeLeaf, computeMerkleRoot, CHUNK_COUNTS"
      exports: ["hashToField", "generateSalt", "generateSalts", "computeLeaf", "computeMerkleRoot", "CHUNK_COUNTS"]
  key_links:
    - from: "privdId_admin/backend/utils/identityCommitment.js"
      to: "circomlibjs buildPoseidon"
      via: "Poseidon(2) leaf/node + Poseidon(maxChunks) string encoding"
      pattern: "buildPoseidon"
---

<objective>
Build the single shared `identityCommitment.js` module (per D-08): the structural mechanism that kills field-set drift. It exports the hash-to-field string encoder (chunked Poseidon), 31-byte salt generation, salted Poseidon(2) leaf hashing, and the depth-3 / 8-leaf Merkle root computation. Both the admin issuance path (plan 03) and the prover-side re-seed/parity script (plan 04) import this one module, so there is no second hand-written copy to fall out of sync — which is exactly the §1.4 failure mode.

Purpose: One module = one source of truth for the commitment, eliminating the programme/branch drift and the BN128 string-overflow bug at the root.
Output: `privdId_admin/backend/utils/identityCommitment.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md
@.planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@privdId_admin/backend/utils/poseidonHash.js

<interfaces>
<!-- Frozen contracts from the spec doc (plan 01) and RESEARCH. Use these exactly; no codebase exploration needed. -->

BN128 scalar field order:
  21888242871839275222246405745257275088548364400416034343698204186575808495617n

Frozen chunk counts (= Poseidon arities) — export as CHUNK_COUNTS:
  { name: 4, rollNo: 2, email: 2 }

Leaf layout (indices = Merkle positions):
  0:name(hashToField,4) 1:rollNo(hashToField,2) 2:dob(int) 3:programmeLevel(code)
  4:discipline(code) 5:batch(int) 6:email(hashToField,2) 7:zero-padding

Node combine order: Poseidon(2)(left, right), left = lower index. NOT symmetric.
leaf[7] = Poseidon(2)(0,0).

Singleton pattern (poseidonHash.js lines 16-22): cache buildPoseidon; output via poseidon.F.toString(result).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hashToField, salt generation, leaf + Merkle root in identityCommitment.js</name>
  <files>privdId_admin/backend/utils/identityCommitment.js</files>
  <read_first>
    - .planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md (Pattern 1 hashToField, Pattern 2 Merkle construction, Pattern 3 salt generation, and the verified test-vector list)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md (identityCommitment.js section: singleton init, F.toString convention, named-export style, timing instrumentation)
    - privdId_admin/backend/utils/poseidonHash.js (analog: buildPoseidon import, getPoseidon singleton, F.toString output)
  </read_first>
  <behavior>
    - hashToField("Utkarsh Baranwal", 4) === "2689494646062948360487866858549161268023147861439580363715484426041810573382"
    - hashToField("Rajesh Kumar Sharma Gupta Verma Singh", 4) === "8788477441821112447812609039840608362124692723989989797277498722759269778947"  (37 bytes, 2 chunks used)
    - hashToField("21BCS027", 2) === "15150160435819557810078120971221321758887516517285291325240673283662695955468"
    - hashToField("utkarshbaranwal47@students.iiitdmj.ac.in", 2) === "15157798813008110916508472488358427390626844432052365640772174362044533657556"  (40 bytes, 2 chunks)
    - computeLeaf with attr=0, salt=0 (the leaf[7] zero-pad) === Poseidon(2)(0,0) === "14744269619966411208460611736853059166543709924778005885397896789179099038553"
    - generateSalt() returns a decimal string whose BigInt value is < BN128 field order, on every call (test 1000 iterations)
    - computeMerkleRoot is deterministic: same attrs + same salts → identical root across repeated calls
  </behavior>
  <action>
    Create privdId_admin/backend/utils/identityCommitment.js as an ESM module (named exports only, no default; .js import extensions; camelCase filename). Mirror the poseidonHash.js singleton: cache the resolved buildPoseidon instance and return it; never re-await inside a tight loop.

    Declare const BN128_FIELD_ORDER = 21888242871839275222246405745257275088548364400416034343698204186575808495617n (from ffjavascript curves.js, verified).

    Export hashToField(str, maxChunks): UTF-8 encode String(str ?? ""); slice into ≤31-byte chunks; pack each chunk big-endian (for (const b of slice) val = (val << 8n) | BigInt(b)); zero-pad the chunk array at the END to maxChunks; result = poseidon(chunks.slice(0, maxChunks)); return poseidon.F.toString(result) (decimal). Do NOT use keccak256(value) mod p; do NOT reuse stringToBigInt for string commitment (it overflows BN128 for >31-byte strings).

    Export const CHUNK_COUNTS = { name: 4, rollNo: 2, email: 2 } (frozen arities; the Phase-2 circuit reads these verbatim).

    Export generateSalt(): BigInt("0x" + crypto.randomBytes(31).toString("hex")).toString() — 31 bytes = 248 bits, always < BN128 order; never use randomBytes(32) with % p (biased) or rejection sampling here. Export generateSalts(count = 7) returning an array of generateSalt() values.

    Export computeLeaf(encodedAttr, salt): poseidon([BigInt(encodedAttr), BigInt(salt)]) → F.toString (decimal). Used for both real leaves and the zero-pad leaf (encodedAttr=0, salt=0).

    Export computeMerkleRoot(attrs, salts): attrs is the 7 encoded leaf values (indices 0..6 in frozen order); build leaf[0..6] = computeLeaf(attrs[i], salts[i]); leaf[7] = computeLeaf(0, 0); then combine bottom-up with node = Poseidon(2)(left, right), left = lower index, for the 4 level-1 nodes, 2 level-2 nodes, and the root; return the root via F.toString (decimal). Wrap the full computation body in console.time('computeMerkleRoot') / console.timeEnd('computeMerkleRoot') (blueprint §10: every new crypto op prints elapsed time). Apply AppError-style throws only if you add input guards; the module itself need not import AppError unless validating arity (attrs.length === 7, salts.length >= 7).
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/privdId_admin/backend && node --input-type=module -e "import('./utils/identityCommitment.js').then(async m=>{const a=await m.hashToField('Utkarsh Baranwal',4); const b=await m.hashToField('utkarshbaranwal47@students.iiitdmj.ac.in',2); const z=await m.computeLeaf(0,0); const ORDER=21888242871839275222246405745257275088548364400416034343698204186575808495617n; let saltOk=true; for(let i=0;i<1000;i++){if(BigInt(m.generateSalt())>=ORDER){saltOk=false;break;}} const r1=await m.computeMerkleRoot(['1','2','3','4','5','6','7'],['11','22','33','44','55','66','77']); const r2=await m.computeMerkleRoot(['1','2','3','4','5','6','7'],['11','22','33','44','55','66','77']); const ok = a==='2689494646062948360487866858549161268023147861439580363715484426041810573382' && b==='15157798813008110916508472488358427390626844432052365640772174362044533657556' && z==='14744269619966411208460611736853059166543709924778005885397896789179099038553' && saltOk && r1===r2; if(!ok)process.exit(1); console.log('identityCommitment ok');});</automated>
  </verify>
  <done>identityCommitment.js exports hashToField, generateSalt, generateSalts, computeLeaf, computeMerkleRoot, CHUNK_COUNTS; the verification reproduces the verified name vector, the >31-byte email vector, and Poseidon(2)(0,0); generateSalt() is < BN128 order across 1000 samples; computeMerkleRoot is deterministic. Exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| string attribute input → field element | Untrusted-length strings must be reduced to a single in-field element without silent BN128 overflow |
| salt RNG → committed leaf | Salt must be a uniform field element to keep low-entropy leaves unguessable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | hashToField BN128 overflow (RESEARCH finding #2) | mitigate | Chunk into ≤31-byte big-endian pieces then Poseidon(maxChunks); never raw stringToBigInt; verified against the >31-byte email test vector in the gate |
| T-01-05 | Information disclosure | low-entropy leaves (dob/level/discipline/batch) brute-forceable when hidden | mitigate | Mandatory per-attribute salt on every leaf (D-10); 31-byte salt = 248 bits entropy |
| T-01-06 | Spoofing | salt not uniformly < BN128 order (biased / out-of-field) | mitigate | crypto.randomBytes(31) guarantees < order; reject randomBytes(32) % p; gate samples 1000 salts and asserts all < order |
| T-01-07 | Tampering | JS↔circom Poseidon constant/encoding drift | mitigate | circomlibjs@0.1.7 pinned, constants match vendored poseidon_constants.circom (RESEARCH verified); module reproduces frozen spec-doc vectors; arity table CHUNK_COUNTS exported for the circuit to mirror |
</threat_model>

<verification>
- The module reproduces every embedded RESEARCH parity vector, including the 2-chunk email path.
- generateSalt() is < BN128 order across a 1000-sample run.
- computeMerkleRoot is deterministic for fixed inputs.
- console.time('computeMerkleRoot') prints elapsed time on each call.
</verification>

<success_criteria>
- A single shared commitment module exists (D-08), exporting the full encoding + salt + Merkle surface used by both issuance and the parity script.
- The module's outputs match the frozen spec's parity oracle exactly — the structural precondition for SPEC-02 byte-for-byte issuance/prover equality.
</success_criteria>

<output>
Create `.planning/phases/01-freeze-spec-field-set-consistency/01-02-SUMMARY.md` when done.
</output>
