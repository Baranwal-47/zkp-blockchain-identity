---
context: phase
phase: 07-student-keypair-two-phase-enrollment
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-06-19T17:27:24.347Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Do NOT build a persistent Proof-ID store, ever. An earlier draft of Phase 8's context (D-01/D-02/D-03 in `08-CONTEXT.md`, now marked `[superseded]`) called for exactly that — a durable, lookup-able Proof-ID + Verification URL. The user caught that this contradicts the existing single-use/5-min-freshness nonce anti-replay design and explicitly rejected it. The final model (D-08–D-11 in `08-CONTEXT.md`) is a live, stateless, two-hop QR challenge/response with **zero storage** — the proof carries its own generation timestamp and simply stops verifying after 15 minutes. If a fresh agent re-derives "we need a database for Verify Proof," that is the anti-pattern repeating — re-read D-08–D-11 instead.

**Do not proceed past this until acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Persistent Proof-ID store re-proposed | See blocking constraint above. | blocking | Read `08-CONTEXT.md` D-08–D-11 before any Verify Proof design discussion; D-01–D-03 are struck through but kept visible as a "don't redo this" marker. |
| Stale STATE.md after context exhaustion | A prior session hit high context and left `.planning/STATE.md` half-edited (timestamp bumped, prose stale relative to git history). | advisory | Always cross-check STATE.md's prose against `git log` before trusting it as ground truth. |
| Docs left stale across a decision reversal | `.planning/ROADMAP.md` ("Target End-to-End UX" + Phase 9 note), `08-DISCUSSION-LOG.md`, and memory file `e3_target_ux.md` still describe the old Proof-ID/durable-link model — only `08-CONTEXT.md` was updated in the same session the decision reversed. | advisory | Sweep these three for consistency before they're read again for unrelated reasons; not currently blocking since `08-CONTEXT.md` is what `/gsd:plan-phase` actually consumes. |

<current_state>
Two threads in flight, by explicit user instruction to interleave them: (1) close out Phase 07 mechanically — no device needed except one deferred checkpoint — and (2) move straight into `/gsd:plan-phase 8`. The user explicitly said: **defer all human/device verification to the very end** — do not let Phase 07 Task 3 (or any Phase 8 equivalent, e.g. the new two-phone QR handshake) block planning or execution. Treat those checkpoints as the last step of the next session, batched together, not as gates along the way.

Phase 08's scope is now fully decided and written into `08-CONTEXT.md` (D-04 through D-11; D-01–D-03 superseded). The big mid-session change: Verify Proof dropped its persistent Proof-ID/database model entirely in favor of a live, stateless, two-hop QR challenge/response (see blocking constraint above) — this makes Verify Proof small enough to fold back into Phase 8 itself, no Phase 8.1.
</current_state>

<completed_work>

Plan 07-04 (ClaimCredentialScreen):
- Task 1: Created `digital-app/screens/ClaimCredentialScreen.js`. Done (`cea4204`).
- Task 2: Registered in `App.js` + D-01 routing in `LoginScreen.js`. Done (`fce0964`).
- Task 3: Human-verify device checkpoint — explicitly deferred to end-of-session, not abandoned.

Plan 07-03 (mobile crypto foundation): code-complete; RNG smoke test reportedly passed (memory obs #375) but cleanup never happened — `TEMPORARY` probe still in `digital-app/App.js` (~line 65), `07-03-SUMMARY.md` still says PARTIAL.

Phase 08 context-gathering: `08-CONTEXT.md` fully reconciled this session. Final decisions:
- D-04: legacy screens (`HomeScreen.js`, `StudentProfileScreen.js`, `IdentityForm.js`, old `ShowProof.js`) deleted now, no transitional state.
- D-05: "Blockchain Status: Verified" = real on-chain revocation check.
- D-07: attribute checkboxes map to real E1 Merkle per-attribute selective disclosure (not app-layer filtering).
- D-08: proof freshness window is 15 minutes (raised from 5), embedded as a public-signal timestamp, checked statelessly at `/verify` time — no DB, no expiry job.
- D-09: Verify Proof = two-hop QR handshake. Outgoing: verifier calls `/session/nonce` (backend-issued, never invented client-side) + requested fields → QR. Returning: prover's generated proof → QR. Both hops support scan-or-manual-entry; UI must label which hop is active ("Step 1 of 2" / "Step 2 of 2").
- D-10: consent is folded into the existing attribute-checklist screen (pre-checked-but-editable fields from an incoming request; tapping "Generate Proof" is the consent act) — no separate consent screen.
- D-11: Verify Proof folds back into Phase 8 — no Phase 8.1.

08-CONTEXT.md is committed-ready but not yet committed (see uncommitted_files in HANDOFF.json).

</completed_work>

<remaining_work>

**Phase 07 (mechanical, no device needed):**
- Remove the `TEMPORARY` RNG probe from `digital-app/App.js` (~line 65).
- Update `07-03-SUMMARY.md` self-check from PARTIAL to PASS (smoke test already passed per memory).
- Write `07-04-SUMMARY.md` — note Task 3 (device checkpoint) is deferred to end-of-session human verification, not skipped.
- Update `.planning/STATE.md` to reflect Phase 07's real status once the above is done.

**Phase 07 (deferred to the very end, do not block on this):**
- Task 3 device checkpoint: enroll → login → auto-redirect into ClaimCredentialScreen → claim completes → `active` → Mongo shows `dek` wiped/`dekEnvelopeCID` set/`pubKey` set → already-active student skips claim screen → error/retry path works. Full steps: `07-04-PLAN.md` lines 144-156.

**Phase 08:**
- Commit `08-CONTEXT.md`.
- Run `/gsd:plan-phase 8` — context is ready, no further discussion needed.
- (Non-blocking cleanup, anytime): reconcile `.planning/ROADMAP.md`'s "Target End-to-End UX" section + Phase 9 note, `08-DISCUSSION-LOG.md`, and memory file `e3_target_ux.md` to match D-08–D-11 (they still describe the old Proof-ID model).
- At the very end of the next session: run the human-verify checkpoint for Phase 8's Verify Proof flow too (needs two devices/phones for the two-hop QR handshake) — batch with Phase 07's Task 3.

</remaining_work>

<decisions_made>

- Verify Proof drops all persistent storage — no Proof-ID, no Verification URL, no database. `/verify` becomes a pure stateless check (crypto validity + on-chain revocation + 15-min freshness via embedded timestamp).
- Freshness window raised 5 → 15 minutes to fit a real two-phone QR round trip.
- Verify Proof is a two-hop QR exchange (challenge out, proof back), both hops scan-or-manual.
- Consent lives inside the attribute-checklist screen, not a separate screen.
- Verify Proof folds back into Phase 8; no Phase 8.1.
- All human/device verification (Phase 07 Task 3 + Phase 8's new two-device QR checkpoint) is explicitly deferred to the end of the next session, batched together, per direct user instruction — do not gate planning or execution on it.

</decisions_made>

<blockers>
- None blocking. Phase 07 Task 3 and the future Phase 8 device checkpoint are deferred, not blocking, per user instruction.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-daily-access-flow/08-CONTEXT.md` — D-08 through D-11 (current/correct), D-01–D-03 (superseded, kept visible as a guardrail) — required before `/gsd:plan-phase 8`.
2. `.planning/phases/07-student-keypair-two-phase-enrollment/07-04-PLAN.md` (Task 3 section, lines 144-156) — exact device-checkpoint steps, for when verification is finally run.
3. `.planning/phases/07-student-keypair-two-phase-enrollment/07-03-SUMMARY.md` — stale PARTIAL self-check, needs flipping to PASS during cleanup.

## Infrastructure State
- Expo dev server / admin backend / Mongo / zkp-backend state not verified this session — re-check before relying on any of them.

<context>
This session's work was almost entirely a decision-correction pass on Phase 8's Verify Proof design (triggered by the user spotting the nonce/persistence contradiction), not new exploration. `08-CONTEXT.md` is now the single source of truth for Phase 8 and is ready for planning. Phase 07's remaining work is pure cleanup plus one deferred device checkpoint.
</context>

<next_action>
1. Phase 07 mechanical cleanup (remove RNG probe, fix 07-03-SUMMARY.md, write 07-04-SUMMARY.md, update STATE.md) — no device needed.
2. Commit `08-CONTEXT.md`.
3. Run `/gsd:plan-phase 8`.
4. At the very end: run both deferred device checkpoints (Phase 07 Task 3 + Phase 8 Verify Proof two-device QR test) together.
</next_action>
