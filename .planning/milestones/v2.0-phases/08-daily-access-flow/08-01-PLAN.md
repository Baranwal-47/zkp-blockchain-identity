---
phase: 08-daily-access-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - privdId_admin/backend/controllers/studentController.js
  - privdId_admin/backend/routes/studentRoutes.js
  - zkp-backend/lib/nonceStore.js
autonomous: true
requirements: [ACCESS-01]
must_haves:
  truths:
    - "GET /credential/:rollNo/blobs returns ciphertextCID + dekEnvelopeCID for an active student"
    - "The same endpoint returns 403 for a student whose enrollmentPhase is not 'active'"
    - "The same endpoint returns 404 for an unknown rollNo"
    - "A nonce session issued by /session/nonce stays valid for 15 minutes, not 5"
  artifacts:
    - path: "privdId_admin/backend/controllers/studentController.js"
      provides: "getCredentialBlobs handler (rollNo lookup, active-phase gate, blob presence gate)"
      contains: "getCredentialBlobs"
    - path: "privdId_admin/backend/routes/studentRoutes.js"
      provides: "GET /credential/:rollNo/blobs route registered BEFORE /:id"
      contains: "credential/:rollNo/blobs"
    - path: "zkp-backend/lib/nonceStore.js"
      provides: "TTL_MS raised to 15 minutes (D-08)"
      contains: "15 * 60 * 1000"
  key_links:
    - from: "routes/studentRoutes.js"
      to: "studentController.js getCredentialBlobs"
      via: "router.get registration"
      pattern: "credential/:rollNo/blobs.*getCredentialBlobs"
    - from: "getCredentialBlobs"
      to: "Student model"
      via: "Student.findOne({ rollNo })"
      pattern: "findOne\\(\\{\\s*rollNo"
---

<objective>
Build the ACCESS-01 backend surface for daily access: a new admin-backend route that returns both IPFS CIDs (ciphertext + DEK envelope) for an active student keyed by rollNo, plus the D-08 freshness-window change in the zkp-backend nonce store (5 → 15 minutes).

Purpose: The mobile daily-access flow (Plans 08-02..08-05) needs a way to look up a student's `ciphertextCID` and `dekEnvelopeCID` by rollNo — no such lookup exists today (only `findById` / `findOne({email})`). D-08 raises the proof-freshness window so a real two-phone QR round trip in Verify Proof (Plan 08-05) has enough slack.
Output: `getCredentialBlobs` controller + route registration + a one-constant TTL change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-daily-access-flow/08-RESEARCH.md
@.planning/phases/08-daily-access-flow/08-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly, no exploration needed. -->

From privdId_admin/backend/middleware/asyncHandler.js (full file):
  export function asyncHandler(handler) {
    return (req, res, next) => Promise.resolve(handler(req, res, next)).catch(next);
  }

From privdId_admin/backend/utils/appError.js:
  export default class AppError extends Error // constructor(message, statusCode = 500, details = null)
  NOTE: studentController.js imports it as a NAMED import `{ AppError }` per 08-PATTERNS.md — confirm the existing import line in the file and match it exactly; do not change the import style.

Existing analog getStudentById (studentController.js ~lines 240-245):
  export const getStudentById = asyncHandler(async (req, res) => {
    const { id } = req.params;
    const student = await Student.findById(id);
    if (!student) throw new AppError("Student not found.", 404);
    res.json({ status: "success", student: sanitizeStudent(student) });
  });

Student schema fields consumed (privdId_admin/backend/models/Student.js): rollNo, ciphertextCID, dekEnvelopeCID, enrollmentPhase ("awaiting-keypair" | "active").

zkp-backend/lib/nonceStore.js line 30 (current): const TTL_MS = 5 * 60 * 1000;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getCredentialBlobs controller + route (ACCESS-01)</name>
  <files>privdId_admin/backend/controllers/studentController.js, privdId_admin/backend/routes/studentRoutes.js</files>
  <read_first>
    - privdId_admin/backend/controllers/studentController.js (read getStudentById ~240-245 and claimPubkey ~279-298 for the exact asyncHandler/AppError import + handler style; confirm whether AppError is a named or default import in THIS file and match it)
    - privdId_admin/backend/routes/studentRoutes.js (full file — confirm current route order; the "/:id" GET registration line is the insertion anchor)
    - privdId_admin/backend/models/Student.js (confirm field names rollNo, ciphertextCID, dekEnvelopeCID, enrollmentPhase exist exactly)
    - .planning/phases/08-daily-access-flow/08-RESEARCH.md (Pattern 1 + Pitfall 5)
  </read_first>
  <action>
    In studentController.js, export a new `getCredentialBlobs` handler wrapped in `asyncHandler(async (req, res) => {...})`, following getStudentById's exact convention (no try/catch, throw AppError directly). Read `rollNo` from `req.params`. Look up `Student.findOne({ rollNo })`. Throw `AppError("Student not found.", 404)` if no student; throw `AppError("Student has not completed enrollment.", 403)` if `student.enrollmentPhase !== "active"`; throw `AppError("Credential blobs are not available for this student.", 404)` if `!student.ciphertextCID || !student.dekEnvelopeCID`. On success respond `res.json({ status: "success", ciphertextCID: student.ciphertextCID, dekEnvelopeCID: student.dekEnvelopeCID })`. Do NOT return the full sanitized student object — return only the two CID fields. In studentRoutes.js, add `getCredentialBlobs` to the existing named import from studentController.js, and register `router.get("/credential/:rollNo/blobs", getCredentialBlobs)` STRICTLY BEFORE the existing `router.get("/:id", getStudentById)` line (Pitfall 5: Express matches in registration order; if registered after `/:id`, the path `/credential/X/blobs` is swallowed by `getStudentById` with `id="credential"`). Add a one-line comment noting the ordering requirement.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('privdId_admin/backend/routes/studentRoutes.js','utf8'); const blobIdx=s.indexOf('credential/:rollNo/blobs'); const idIdx=s.indexOf('\"/:id\"'); if(blobIdx===-1) throw new Error('blobs route missing'); if(idIdx!==-1 && blobIdx>idIdx) throw new Error('blobs route registered AFTER /:id — Pitfall 5'); const c=require('fs').readFileSync('privdId_admin/backend/controllers/studentController.js','utf8'); if(!/getCredentialBlobs/.test(c)) throw new Error('controller missing'); if(!/findOne\(\{\s*rollNo/.test(c)) throw new Error('rollNo lookup missing'); if(!/enrollmentPhase\s*!==\s*\"active\"/.test(c)) throw new Error('active gate missing'); console.log('OK')"</automated>
  </verify>
  <done>getCredentialBlobs exported and wrapped in asyncHandler; route registered before /:id; rollNo lookup, active-phase 403 gate, and missing-blob 404 gate all present; response returns only the two CID fields.</done>
</task>

<task type="auto">
  <name>Task 2: Raise nonce TTL to 15 minutes (D-08)</name>
  <files>zkp-backend/lib/nonceStore.js</files>
  <read_first>
    - zkp-backend/lib/nonceStore.js (full file — confirm line 30 `const TTL_MS = 5 * 60 * 1000;` and that this is the ONLY freshness constant; RESEARCH.md confirms there is no second one)
    - .planning/phases/08-daily-access-flow/08-RESEARCH.md (D-08 row + Open Question 2 — TTL_MS is the SOLE mechanism; the circuit carries no separate "generatedAt" signal; this is a narrow justified exception to "zkp-backend untouched")
  </read_first>
  <action>
    Change the single `TTL_MS` constant from `5 * 60 * 1000` to `15 * 60 * 1000` per D-08 (15-minute proof-freshness window for the two-phone QR round trip in Verify Proof). Update any adjacent comment that says "5 minutes" / "5 min" to "15 minutes". Make NO other change to nonceStore.js — do not touch validateAndConsume, the Map, or the single-use `used` flag. Do not add an embedded proof timestamp (the circuit is frozen per CLAUDE.md ground rule 1; per RESEARCH.md Open Question 2 the nonce-session TTL is the entire freshness mechanism and CONTEXT.md D-08's "proof carries its own timestamp" framing is satisfied by this server-tracked expiry).
  </action>
  <verify>
    <automated>grep -v '^\s*//' zkp-backend/lib/nonceStore.js | grep -q "15 \* 60 \* 1000" && ! (grep -v '^\s*//' zkp-backend/lib/nonceStore.js | grep -q "TTL_MS = 5 \* 60") && echo "OK"</automated>
  </verify>
  <done>TTL_MS is `15 * 60 * 1000`; no `5 * 60` TTL assignment remains in non-comment code; validateAndConsume/Map/used-flag logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify zkp-backend nonce test suite still passes after TTL change</name>
  <files>zkp-backend/test/nonceStore.test.js</files>
  <read_first>
    - zkp-backend/test/nonceStore.test.js (confirm it exists and whether any assertion hardcodes the 5-minute value — if it does, that assertion must be updated to 15 minutes, NOT the production constant reverted)
    - zkp-backend/package.json (test script is `mocha test/**/*.test.js --exit`)
  </read_first>
  <action>
    Run the existing zkp-backend mocha suite to confirm the TTL change did not break nonce-store tests. If `nonceStore.test.js` contains an assertion that hardcodes `5 * 60 * 1000` or "5 minutes" of expiry, update that assertion to 15 minutes to match the new production constant (the test must follow the D-08 decision, not pin the old value). If no test references the TTL value, no test edit is needed — the run just confirms green. Do not weaken or skip any test to make it pass.
  </action>
  <verify>
    <automated>cd zkp-backend && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>`cd zkp-backend && npm test` exits 0 with the full mocha suite green; any TTL-pinned assertion updated to 15 minutes rather than reverting the production constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → admin-backend GET /credential/:rollNo/blobs | Untrusted rollNo path param crosses into a Mongo query |
| verifier/prover → zkp-backend /session/nonce | Nonce session lifetime (now 15 min) governs anti-replay freshness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | GET /credential/:rollNo/blobs (no auth, mirrors existing unauthenticated getStudentById) | accept | Inherits project-wide accepted gap HARD-01 (auth middleware on student CRUD deferred to v3+). Disclosed values are only two IPFS CIDs — the DEK envelope remains ECIES-locked to the student's private key, so this is CID-disclosure, not credential-disclosure. Documented, not fixed in this phase (no scope creep). |
| T-08-02 | Tampering | rollNo path param → Student.findOne | mitigate | rollNo is used only as an exact-match `findOne({ rollNo })` filter (string equality, not a regex or `$where`), so no NoSQL operator-injection surface. Param is a plain string; no `$`-prefixed object reaches the query. |
| T-08-03 | Spoofing / replay | Raising nonce TTL 5→15 min widens the replay window | accept | Nonce remains single-use (`validateAndConsume` `used` flag unchanged); a longer-but-still-single-use window is the explicit D-08 tradeoff for a real two-phone round trip. No weakening of single-use semantics. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — backend-only edits to existing files. Per RESEARCH.md Package Legitimacy Audit: no new packages introduced. |
</threat_model>

<verification>
- `cd zkp-backend && npm test` is green.
- Manual smoke (execute-phase, requires running admin-backend + a seeded active student): `curl http://localhost:<port>/api/students/credential/<activeRollNo>/blobs` returns `{status:"success", ciphertextCID, dekEnvelopeCID}`; an awaiting-keypair student returns 403; an unknown rollNo returns 404; `curl .../credential/credential/blobs`-style probe does NOT hit getStudentById.
</verification>

<success_criteria>
- ACCESS-01 endpoint returns both CIDs for active students, 403 for non-active, 404 for unknown/missing-blob.
- Route is registered before `/:id` (no shadowing).
- nonce TTL is 15 minutes; zkp-backend suite green.
</success_criteria>

<output>
Create `.planning/phases/08-daily-access-flow/08-01-SUMMARY.md` when done.
</output>
