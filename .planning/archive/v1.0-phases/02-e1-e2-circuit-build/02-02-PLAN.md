---
phase: 02-e1-e2-circuit-build
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - zk-proofs/test/circuitParity.test.js
autonomous: true
requirements: [REPL-02]
must_haves:
  truths:
    - "The compiled circuit's pubHash (witness signal [0]) equals identityCommitment.js computeMerkleRoot for the same attr[]/salt[] inputs across all IDENTITY_SPEC.md section 9 vectors"
    - "The two mandatory multi-chunk vectors (37-byte name, 40-byte email) are exercised as part of the parity gate (D-14)"
    - "A proof/witness valid for nonce A is rejected when verified against nonce B (REPL-02)"
    - "The circuit is declared frozen only after the parity gate and the nonce-rejection test both pass"
  artifacts:
    - path: "zk-proofs/test/circuitParity.test.js"
      provides: "Witness-level parity gate (section 9 vectors) + nonce-rejection test, the freeze precondition"
      contains: "describe"
      min_lines: 60
  key_links:
    - from: "zk-proofs/test/circuitParity.test.js"
      to: "privdId_admin/backend/utils/identityCommitment.js"
      via: "dynamic import() of the ESM oracle computeMerkleRoot/computeLeaf"
      pattern: "import\\("
    - from: "zk-proofs/test/circuitParity.test.js"
      to: "zk-proofs/build/identity_js/identity.wasm"
      via: "snarkjs.wtns.calculate against the compiled wasm"
      pattern: "wtns"
---

<objective>
Build the witness-level parity gate that is this phase's freeze precondition (D-14), plus the nonce-rejection test (REPL-02). The parity test generates a witness for the compiled circuit (plan 02-01) using the exact attr/salt inputs behind each `IDENTITY_SPEC.md` section 9 vector and asserts the circuit's computed `pubHash` matches `identityCommitment.js::computeMerkleRoot` byte-for-byte. The nonce-rejection test confirms a witness/proof valid for nonce A fails against nonce B.

Purpose: DESIGN-ONCE means any encoding/ordering mismatch caught after this phase costs a full Groth16 trusted-setup redo (Phase 3 is irreversible). A witness-level parity check is cheap now and the spec's vectors already exist — there is no reason to skip it (D-14, user risk-acceptance call). The circuit is frozen only after this gate is green.

Output: `zk-proofs/test/circuitParity.test.js` — passing the section 9 parity vectors (including both multi-chunk cases) and the nonce-rejection assertion. After this plan passes, the circuit is frozen for Phase 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-e1-e2-circuit-build/02-CONTEXT.md
@.planning/phases/02-e1-e2-circuit-build/02-RESEARCH.md
@.planning/phases/02-e1-e2-circuit-build/02-PATTERNS.md
@.planning/phases/02-e1-e2-circuit-build/02-01-SUMMARY.md

<interfaces>
<!-- The JS oracle and the section 9 vectors the test asserts against. -->

JS oracle (privdId_admin/backend/utils/identityCommitment.js — ESM, "type":"module"):
  export async function computeMerkleRoot(attrs, salts) -> Promise<string>   // decimal-string root
    attrs: 7 encoded leaf values (indices 0..6, frozen order); salts: >=7 decimal-string salts
  export async function computeLeaf(encodedAttr, salt) -> Promise<string>
  export async function hashToField(str, maxChunks) -> Promise<string>
  export const CHUNK_COUNTS = { name: 4, rollNo: 2, email: 2 }
NOTE: zk-proofs is CommonJS (package.json has no "type" field). identityCommitment.js is ESM in a
different package. Import it via dynamic `await import("../../privdId_admin/backend/utils/identityCommitment.js")`
inside an async test, OR duplicate the minimal oracle locally. Dynamic import is the lower-duplication option (PATTERNS).

IDENTITY_SPEC.md section 9 frozen vectors (the parity assertions):
  Poseidon(2)(1,2)            = 7853200120776062878684798364095072458815029376092732009249414926327459813530
  Poseidon(2)(0,0)            = 14744269619966411208579211824598458697587494354926760081771325075741142829156
  hashToField("Utkarsh Baranwal", 4)                              = 2689494646062948360487866858549161268023147861439580363715484426041810573382
  hashToField("Rajesh Kumar Sharma Gupta Verma", 4)              = 15807365457822395816033615442310068061117268680265938810672994222315960047410
  hashToField("21BCS027", 2)                                      = 15150160435819557810078120971221321758887516517285291325240673283662695955468
  hashToField("21bcs027@iiitdmj.ac.in", 2)                        = 6744441775314583329532040559385253235651674879202368422786321712697490882813
  hashToField("Rajesh Kumar Sharma Gupta Verma Singh", 4) [37 bytes, MULTI-CHUNK] = 8788477441821112447812609039840608362124692723989989797277498722759269778947
  hashToField("utkarshbaranwal47@students.iiitdmj.ac.in", 2) [40 bytes, MULTI-CHUNK] = 15157798813008110916508472488358427390626844432052365640772174362044533657556

snarkjs JS API (already a dependency, zero new deps — RESEARCH section 3 decision):
  const snarkjs = require("snarkjs");
  await snarkjs.wtns.calculate(input, "build/identity_js/identity.wasm", "/tmp/witness.wtns");
  // export witness to extract pubHash (signal [0]): snarkjs.wtns.exportJson(...) or read symbol map
  await snarkjs.groth16.verify(vKey, publicSignals, proof) -> boolean  // for nonce-rejection (needs zkey -> see action)

Existing test-file shape (zk-proofs/test/Registry.js): CommonJS require, mocha describe/it/before,
run via `npx hardhat test`. Use assert.strictEqual or chai expect(...).to.equal(...) per vector.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Witness-level parity gate against IDENTITY_SPEC section 9 vectors (D-14)</name>
  <read_first>
    - zk-proofs/test/Registry.js (this package's mocha test-file shape: require, describe/it/before, run via npx hardhat test)
    - zk-proofs/build/identity_js/identity.wasm (the compiled circuit from plan 02-01 — the witness source; confirm it exists)
    - zk-proofs/build/identity.sym (symbol map — used to locate the pubHash output signal index in the witness)
    - privdId_admin/backend/utils/identityCommitment.js (computeMerkleRoot/computeLeaf/hashToField — the oracle imported via dynamic import())
    - docs/current/research/IDENTITY_SPEC.md section 9 (the frozen vectors to assert against) and section 4 (leaf/node order)
    - .planning/phases/02-e1-e2-circuit-build/02-RESEARCH.md section 3 (witness-level parity test approach, snarkjs JS API, zero-new-deps decision)
    - .planning/phases/02-e1-e2-circuit-build/02-01-SUMMARY.md (the verified public-signal order and pubHash signal index from plan 02-01)
  </read_first>
  <action>
    Create `zk-proofs/test/circuitParity.test.js` as a CommonJS mocha test (matching Registry.js shape) run via `npx hardhat test test/circuitParity.test.js`. The test asserts the compiled circuit's `pubHash` equals the JS oracle's `computeMerkleRoot` for the same attr/salt inputs — this is the D-14 freeze gate, NOT a spot check.

    Build the oracle bridge: inside an async `before()` hook, `await import("../../privdId_admin/backend/utils/identityCommitment.js")` to get `computeMerkleRoot`, `computeLeaf`, `hashToField` from the ESM oracle (dynamic import works from CommonJS — PATTERNS confirms this as the lower-duplication option; if the cross-package import fails to resolve, fall back to duplicating only computeLeaf/computeMerkleRoot logic locally and document why in the SUMMARY).

    Construct the attr[7] inputs from the section 9 vectors: attr[0]=name hashToField result, attr[1]=rollNo hashToField result, attr[2]=a valid YYYYMMDD dob integer, attr[3]=a programmeLevel code, attr[4]=a discipline code, attr[5]=a batch year, attr[6]=email hashToField result. Use FIXED reproducible salts (e.g. salt[i] = String(i+1) or any fixed decimal strings) so leaf values are deterministic across runs. Cover at least these cases, each as its own `it()`:
      (a) single-chunk name+email vector set (Utkarsh Baranwal / 21bcs027@iiitdmj.ac.in)
      (b) MANDATORY 37-byte name multi-chunk vector ("Rajesh Kumar Sharma Gupta Verma Singh", maxChunks=4)
      (c) MANDATORY 40-byte email multi-chunk vector ("utkarshbaranwal47@students.iiitdmj.ac.in", maxChunks=2)
    For each case: compute the JS oracle root via `await computeMerkleRoot(attrs, salts)`; generate the circuit witness via `snarkjs.wtns.calculate(input, "build/identity_js/identity.wasm", tmpWtnsPath)` where `input` provides attr/salt/nonce/currentDateInt/revealedValue/revealMask (use revealMask all-1 with revealedValue=attr, or all-0 with revealedValue=0, a valid combination per CIRC-03); export the witness to JSON via `snarkjs.wtns.exportJson` and extract the `pubHash` value at its witness index (the index after the constant-1 wire; resolve it from `build/identity.sym` per the public-signal order recorded in 02-01-SUMMARY — pubHash is public signal [0]). Assert `assert.strictEqual(circuitPubHash.toString(), jsRoot)`.

    Also add a primitive sanity `it()` asserting the zero-pad leaf: a witness where attr[7]/salt[7] path is exercised must produce a tree using leaf7 = Poseidon(2)(0,0) = 14744269619966411208579211824598458697587494354926760081771325075741142829156 — assert the JS oracle and circuit agree (this is implicitly covered by the full-root parity but call it out as a named assertion for traceability to IDENTITY_SPEC section 9).
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/zk-proofs && npx hardhat test test/circuitParity.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `zk-proofs/test/circuitParity.test.js` exists and runs via `npx hardhat test test/circuitParity.test.js`
    - The test has separate `it()` cases for the single-chunk set, the 37-byte name multi-chunk vector, and the 40-byte email multi-chunk vector (the two multi-chunk cases are present and pass — D-14 mandatory)
    - Each case computes the circuit witness via `snarkjs.wtns.calculate` against `build/identity_js/identity.wasm` and the JS root via `computeMerkleRoot`, then asserts they are equal with `assert.strictEqual` (or chai `.to.equal`)
    - The test passes (mocha reports all parity cases green); a deliberate mismatch (e.g. swapping two attrs) would fail it
    - The oracle is obtained via dynamic `import(...)` of identityCommitment.js OR a documented local duplication of computeLeaf/computeMerkleRoot
  </acceptance_criteria>
  <done>The witness-level parity gate passes for all section 9 vectors including both multi-chunk cases, proving the circuit's Merkle math matches the JS oracle byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 2: Nonce-rejection test (REPL-02) and freeze sign-off</name>
  <read_first>
    - zk-proofs/test/circuitParity.test.js (the test file from Task 1 — extend it)
    - zk-proofs/build/identity_js/identity.wasm (compiled circuit)
    - .planning/phases/02-e1-e2-circuit-build/02-RESEARCH.md section 1 nonce-binding (REPL-02 is a property of Groth16 public-input binding — verified by an integration test, not extra circuit code) and section 4 risk 8 (ptau missing)
    - docs/CLAUDE_CODE_BLUEPRINT.md section 3 (nonce-binding rationale)
  </read_first>
  <action>
    Add a nonce-rejection `it()` to `circuitParity.test.js` proving REPL-02 (a proof for nonce A is rejected against nonce B). REPL-02 is a property of Groth16's public-input binding, automatically true once `nonce` participates in a constraint (REPL-01, plan 02-01) — so the test is integration-style, not new circuit code.

    Because full Groth16 proving needs a zkey and the trusted-setup ptau is NOT available this phase (it is a Phase-3 prerequisite, RESEARCH section 4 risk 8), verify REPL-02 at the WITNESS level rather than the full-proof level, which needs no zkey: generate a valid witness with nonce=A and assert witness generation succeeds and the witness records nonce=A at its public-signal index [1]; then assert that the public-signal vector extracted from the witness binds nonce=A (i.e. the nonce value in publicSignals is A, not free) so that a verifier checking against publicSignals stating nonce=B would necessarily reject. Concretely: extract the nonce public signal (index [1] per the 02-01 verified order) from the nonce-A witness and `assert.notStrictEqual(witnessNonce, B)` plus `assert.strictEqual(witnessNonce, A)`. Add a comment that the full groth16.verify(nonce-A-proof, publicSignals-with-nonce-B) === false assertion is deferred to Phase 4 once the zkey exists (it is the same binding property; the witness-level check is the strongest test possible without the ptau this phase). If the executor finds a local dev ptau can be generated cheaply for a small circuit (powersOfTau new + a single contribution) WITHOUT committing it as the real ceremony, it MAY optionally do a full groth16.verify nonce-swap assertion — but this is optional and must NOT be mistaken for or replace the real Phase-3 ceremony.

    Freeze sign-off: once Task 1 parity gate and this nonce test both pass, the circuit is frozen. Record in the SUMMARY: parity gate green (with the two multi-chunk vectors named), nonce-binding confirmed, and an explicit "CIRCUIT FROZEN — do not edit identity.circom; any change forces a Phase-3 trusted-setup redo" line. Also restate the missing `pot12_final.ptau` as the carried-forward Phase-3 prerequisite.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/zk-proofs && npx hardhat test test/circuitParity.test.js 2>&1 | grep -E "passing|failing" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `circuitParity.test.js` contains a nonce-rejection `it()` that generates a witness for nonce A and asserts the witness's public nonce signal (index [1]) equals A and does not equal a different value B
    - The full test file passes via `npx hardhat test test/circuitParity.test.js` with zero failures
    - The test includes a comment that the full groth16.verify nonce-swap assertion is deferred to Phase 4 (zkey-dependent), with the witness-level binding check being the strongest no-ptau check
    - The SUMMARY records the freeze sign-off line "CIRCUIT FROZEN" and restates the missing pot12_final.ptau Phase-3 prerequisite
  </acceptance_criteria>
  <done>REPL-02 is verified at the witness level, the full parity+nonce suite passes, and the circuit is signed off as frozen for Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled circuit -> Phase 3 trusted setup | The frozen r1cs/wasm is consumed irreversibly by the ceremony; an undetected encoding/ordering bug survives into the deployed verifier |
| circuit witness -> verifier (nonce binding) | A captured proof could be replayed against a different verifier challenge if nonce were not bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | chunking/ordering bug survives into irreversible Phase-3 setup | mitigate | Witness-level parity gate against all section 9 vectors incl. both mandatory multi-chunk cases (D-14) blocks freeze until circuit root == JS oracle root |
| T-02-07 | Spoofing / Replay | captured proof replayed against a different nonce | mitigate | Witness-level nonce-binding check (REPL-02); full groth16.verify nonce-swap deferred to Phase 4 where the zkey exists |
| T-02-08 | Repudiation | circuit silently edited after freeze, invalidating the ceremony | mitigate | Explicit "CIRCUIT FROZEN" sign-off recorded in SUMMARY after both tests pass |
| T-02-SC | Tampering | npm test-tooling installs | accept | No new packages: snarkjs/chai/mocha/hardhat already present; dynamic import of existing oracle, zero new deps (RESEARCH section 3) |
</threat_model>

<verification>
- `npx hardhat test test/circuitParity.test.js` passes with zero failures
- The two mandatory multi-chunk parity vectors (37-byte name, 40-byte email) are present and green
- The nonce-rejection assertion passes (witness binds nonce A, not B)
- The SUMMARY records the freeze sign-off and the carried-forward missing ptau
</verification>

<success_criteria>
- The circuit's witness pubHash matches identityCommitment.js computeMerkleRoot across all section 9 vectors (D-14 gate green)
- REPL-02 nonce binding is verified at the witness level
- The circuit is frozen with an explicit recorded sign-off, ready for Phase 3's trusted setup
</success_criteria>

<output>
Create `.planning/phases/02-e1-e2-circuit-build/02-02-SUMMARY.md` when done. Record: parity gate result (naming the two multi-chunk vectors), nonce-binding confirmation, the CIRCUIT FROZEN sign-off line, and the missing pot12_final.ptau Phase-3 prerequisite.
</output>
