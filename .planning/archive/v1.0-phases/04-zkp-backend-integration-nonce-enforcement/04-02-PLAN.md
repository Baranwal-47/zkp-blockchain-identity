---
phase: 04-zkp-backend-integration-nonce-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - zkp-backend/server.js
  - zkp-backend/lib/nonceStore.js
  - zkp-backend/test/generateProof.test.js
  - zkp-backend/test/nonceStore.test.js
autonomous: true
requirements: [BACK-01, REPL-03]
must_haves:
  truths:
    - "POST /generate-proof accepts {attrs, salts, reveal, nonce, currentDateInt} and returns {proof, publicSignals} with publicSignals.length === 19 in frozen §3 order"
    - "POST /session/nonce issues a random field element < BN128 order with a sessionId and 5-minute TTL"
    - "The old stringToBigInt 5-field input path is fully removed from server.js"
  artifacts:
    - path: "zkp-backend/lib/nonceStore.js"
      provides: "In-memory Map nonce store: issueNonce + validateAndConsume (TTL + one-time-use)"
      exports: ["issueNonce", "validateAndConsume"]
    - path: "zkp-backend/server.js"
      provides: "Rewritten /generate-proof + new POST /session/nonce route"
      contains: "buildWitnessInput"
  key_links:
    - from: "zkp-backend/server.js"
      to: "zkp-backend/lib/witnessBuilder.js"
      via: "buildWitnessInput in /generate-proof"
      pattern: "buildWitnessInput"
    - from: "zkp-backend/server.js"
      to: "zkp-backend/lib/nonceStore.js"
      via: "issueNonce in /session/nonce"
      pattern: "issueNonce"
---

<objective>
Rewrite `POST /generate-proof` to consume the new 7-attribute input shape via the plan-01 witness library and return the 19-signal publicSignals array, and add `POST /session/nonce` backed by an in-memory nonce store with 5-minute TTL and one-time-use semantics. Delete the obsolete stringToBigInt 5-field path entirely.

Purpose: Make the prover speak the frozen circuit contract (BACK-01) and stand up the nonce-issue half of the session lifecycle (REPL-03 issue side).
Output: New `/session/nonce` route, rewritten `/generate-proof`, `lib/nonceStore.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-zkp-backend-integration-nonce-enforcement/04-RESEARCH.md
@.planning/phases/04-zkp-backend-integration-nonce-enforcement/04-01-SUMMARY.md

<interfaces>
<!-- From plan 04-01 (CommonJS): -->
From zkp-backend/lib/witnessBuilder.js:
  async function buildWitnessInput({ attrs, salts, reveal, nonce, currentDateInt }): Promise<input>
    // input = { attr[7], salt[7], nonce, currentDateInt, isOver18, isPostgrad, revealedValue[7], revealMask[7] }
From zkp-backend/lib/encoding.js:
  function generateSalts(count = 7): string[]

<!-- Frozen public-signal order (RESEARCH Pitfall 1; component main public declaration):
  publicSignals[0]    = pubHash (Merkle root)
  publicSignals[1]    = nonce
  publicSignals[2]    = currentDateInt
  publicSignals[3]    = isOver18
  publicSignals[4]    = isPostgrad
  publicSignals[5..11]= revealedValue[0..6]
  publicSignals[12..18]=revealMask[0..6]
  nPublic = 19 (verified in verification_key.json) -->

<!-- Nonce store contract to implement (RESEARCH Pattern 5): -->
  issueNonce(): { nonce: string, sessionId: string, expiresAt: number }   // expiresAt in ms (Date.now()+300000)
  validateAndConsume(sessionId, presentedNonce): { ok: boolean, reason?: string }
    // reasons: "unknown_session" | "nonce_already_used" | "nonce_expired" | "nonce_mismatch"
  BN128_FIELD_ORDER = 21888242871839275222246405745257275088548364400416034343698204186575808495617n
  Nonce gen: BigInt('0x'+crypto.randomBytes(31).toString('hex')).toString()  // 248 bits < field order
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-memory nonce store with TTL + one-time-use</name>
  <files>zkp-backend/lib/nonceStore.js, zkp-backend/test/nonceStore.test.js</files>
  <read_first>
    - .planning/phases/04-zkp-backend-integration-nonce-enforcement/04-RESEARCH.md Pattern 5 (full nonceStore.js example), Pitfall 4 (ms vs seconds), Pitfall 5 (unbounded growth — document, do not over-engineer)
    - privdId_admin/backend/utils/identityCommitment.js generateSalt (lines 111-115) — the randomBytes(31) field-element technique to mirror for nonce generation
    - decisions 2 and 3 in the phase planning context (200-OK {valid:false,reason} shape; in-memory Map acceptable; consumed on first successful verification)
  </read_first>
  <behavior>
    - issueNonce() returns {nonce, sessionId, expiresAt}; nonce parsed as BigInt is > 0 and < BN128_FIELD_ORDER; expiresAt === issuedAt + 300000 (5 min in ms)
    - validateAndConsume(unknownSessionId, anyNonce) → {ok:false, reason:"unknown_session"}
    - validateAndConsume(sid, wrongNonce) → {ok:false, reason:"nonce_mismatch"}
    - validateAndConsume(sid, correctNonce) → {ok:true}; a second call with the same → {ok:false, reason:"nonce_already_used"}
    - an entry whose expiresAt is in the past → {ok:false, reason:"nonce_expired"} (test by injecting an expired entry or mocking Date.now)
  </behavior>
  <action>
    Create `zkp-backend/lib/nonceStore.js` (CommonJS) implementing the RESEARCH Pattern 5 store: a module-level `Map` keyed by sessionId holding `{nonce, issuedAt, expiresAt, used}`. `issueNonce()` uses `crypto.randomUUID()` for sessionId and `crypto.randomBytes(31)` → BigInt → decimal string for the nonce (248 bits, always < BN128 order — cite the same justification as generateSalt). TTL_MS = 5*60*1000. `validateAndConsume(sessionId, presentedNonce)` checks in order: unknown_session → nonce_already_used → nonce_expired (Date.now() > expiresAt, server clock only, never trust client) → nonce_mismatch, then sets used=true and returns {ok:true}. Add a top-of-file comment noting unbounded-growth is an accepted prototype limitation (RESEARCH Pitfall 5) and a v2 sweep is deferred. Hardcode BN128_FIELD_ORDER for the test's range assertion (or export it).

    Create `zkp-backend/test/nonceStore.test.js` covering all behavior cases above. For the expiry case, either inject a crafted entry or stub Date.now.
  </action>
  <verify>
    <automated>cd zkp-backend && npm test -- --grep "nonceStore" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - issueNonce() nonce satisfies 0 < BigInt(nonce) < 21888242871839275222246405745257275088548364400416034343698204186575808495617
    - All four reason strings (unknown_session, nonce_already_used, nonce_expired, nonce_mismatch) are returned by the matching scenarios
    - Second validateAndConsume of a successful (sessionId, nonce) returns nonce_already_used (one-time-use proven)
  </acceptance_criteria>
  <done>nonceStore.js exports issueNonce/validateAndConsume; all lifecycle behavior tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /generate-proof + add POST /session/nonce</name>
  <files>zkp-backend/server.js, zkp-backend/test/generateProof.test.js</files>
  <read_first>
    - zkp-backend/server.js (ENTIRE FILE — you are deleting stringToBigInt lines 23-30, rewriting /generate-proof lines 57-84, adding the nonce route; keep the wasm/zkey/vkey loading and the logging middleware)
    - zkp-backend/lib/witnessBuilder.js + zkp-backend/lib/nonceStore.js + zkp-backend/lib/encoding.js (plan 01 + Task 1 of this plan)
    - zk-proofs/test/circuitParity.test.js buildInput (the witness shape buildWitnessInput must match)
    - .planning/phases/04-zkp-backend-integration-nonce-enforcement/04-RESEARCH.md Pattern 2, Pitfall 1 (publicSignals order), Pitfall 2 (string normalization / clear 400 on malformed body)
  </read_first>
  <action>
    DELETE `stringToBigInt` (server.js lines 23-30) and its three call sites — do NOT keep it as a fallback (RESEARCH State of the Art "Deprecated").

    Rewrite `POST /generate-proof` to: validate the request body has `attrs` (object with name, rollNo, dob, programmeLevel, discipline, batch, email), `reveal` (boolean map), `nonce` (decimal string), `currentDateInt` (string "YYYYMMDD"), and optional `salts` (string[7]; if absent call `generateSalts(7)` and include the generated salts in the response so the caller can reuse them — note salts must match issuance per RESEARCH Assumption A2). Return a clear `400 {error: "..."}` on a malformed body (not an opaque snarkjs 500 — Pitfall 2). Call `await buildWitnessInput({attrs, salts, reveal, nonce, currentDateInt})`, then `snarkjs.groth16.fullProve(input, wasmPath, zkeyPath)`. Keep the `console.time('ProofGeneration')` instrumentation (ground rule #5). Respond `{ proof, publicSignals, salts }`. Do NOT manually reorder publicSignals — snarkjs emits them in frozen circuit order automatically (Pitfall 1).

    Add `POST /session/nonce`: call `issueNonce()` and respond `{ nonce, sessionId, expiresAt }`. Document expiresAt unit as epoch milliseconds in a comment (Pitfall 4).

    Require the lib modules at the top of server.js (CommonJS require).

    Create `zkp-backend/test/generateProof.test.js`: a unit/integration-style test that builds a request body for the Utkarsh Baranwal section-9 vector with FIXED_SALTS, invokes the witness+fullProve path (either via the exported handler or supertest against the app — supertest install acceptable as a devDependency), and asserts the response `publicSignals.length === 19` and `publicSignals[0]` equals the oracle computeMerkleRoot for those attrs/salts (publicSignals[0] = pubHash). Also assert `POST /session/nonce` returns a nonce, sessionId, and expiresAt with expiresAt - Date.now() within (0, 305000].
  </action>
  <verify>
    <automated>cd zkp-backend && npm test -- --grep "generate-proof|generateProof|session/nonce|nonce route" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c stringToBigInt zkp-backend/server.js` returns 0
    - /generate-proof response publicSignals.length === 19
    - publicSignals[0] === computeMerkleRoot(encoded attrs, salts) for the section-9 vector
    - POST /session/nonce returns {nonce, sessionId, expiresAt} with a field-element nonce and ~5-min future expiresAt
    - A malformed /generate-proof body (missing currentDateInt) returns HTTP 400, not 500
  </acceptance_criteria>
  <done>/generate-proof produces a 19-signal proof from the new input shape; /session/nonce issues TTL'd nonces; stringToBigInt removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → POST /generate-proof | Untrusted attrs/salts/reveal/nonce/currentDateInt; validated then fed to witness builder |
| caller → POST /session/nonce | Unauthenticated challenge request; issues an ephemeral nonce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | /session/nonce unbounded Map growth | accept | Prototype scope, low traffic; documented in nonceStore.js as a v2 sweep (RESEARCH Pitfall 5) |
| T-04-04 | Information Disclosure | /generate-proof opaque error leakage | mitigate | Validate body shape → clear 400 before snarkjs; avoid leaking internal stack traces beyond the existing 500 message (Pitfall 2) |
| T-04-05 | Spoofing | predicate self-report | mitigate | isOver18/isPostgrad never read from request body; derived in buildWitnessInput (plan 01, T-04-01) |
| T-04-SC | Tampering | npm install (supertest devDependency) | mitigate | supertest is a mainstream, high-download HTTP test library; no [ASSUMED]/[SUS]/[SLOP] packages introduced |
</threat_model>

<verification>
- `cd zkp-backend && npm test` passes the nonceStore and generateProof suites
- A live `node server.js` + `curl -X POST localhost:3001/session/nonce` returns a nonce/sessionId/expiresAt JSON
- publicSignals from /generate-proof has length 19 with publicSignals[0] = pubHash
</verification>

<success_criteria>
- POST /generate-proof accepts the new input shape and returns 19 publicSignals in frozen order (BACK-01)
- POST /session/nonce issues a random field-element nonce with sessionId + 5-min TTL (REPL-03 issue side)
- stringToBigInt is gone from server.js
</success_criteria>

<output>
Create `.planning/phases/04-zkp-backend-integration-nonce-enforcement/04-02-SUMMARY.md` when done
</output>
