---
phase: 05-benchmarking-metrics
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - zkp-backend/bench.js
  - zkp-backend/package.json
  - docs/improvement/PERFORMANCE_METRICS_E1E2.md
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "bench.js loops each of the six measured ops N=20, drops the warm-up run, and reports mean ± σ over n=19 in seconds"
    - "The QR payload size is measured as the byte length of JSON.stringify({proof, publicSignals}) from a real 19-signal proof"
    - "PERFORMANCE_METRICS_E1E2.md records constraint count (7891, cited from Phase 3), proof-gen, off-chain verify, on-chain verify, nonce issue, nonce check timings, and QR payload size"
    - "Any free research number already at hand (proof size in bytes, public-signal count=19, end-to-end nonce->proof->verify latency) is also recorded"
  artifacts:
    - path: "zkp-backend/bench.js"
      provides: "Benchmark driver for all six ops + QR size, N=20/n=19 mean±σ table in seconds"
      contains: "n=19"
    - path: "docs/improvement/PERFORMANCE_METRICS_E1E2.md"
      provides: "Research-deliverable metrics doc for the E1+E2 circuit (new standalone file, NOT docs/current/research/)"
      contains: "7891"
  key_links:
    - from: "zkp-backend/bench.js"
      to: "zkp-backend/lib/witnessBuilder.js + lib/nonceStore.js + lib/encoding.js + server endpoints"
      via: "drives the six measured ops"
      pattern: "buildWitnessInput|issueNonce|validateAndConsume|generateSalts"
    - from: "zkp-backend/package.json"
      to: "zkp-backend/bench.js"
      via: "npm run bench script"
      pattern: "bench"
---

<objective>
Write `zkp-backend/bench.js` that drives all six measured E1+E2 operations N=20 times each (dropping the first warm-up run), computes mean and sample standard deviation over the remaining n=19, and prints a results table in seconds (blueprint §10.3 format, e.g. `proof_gen: mean 1.42 s ± 0.08 s (n=19)`). Also measure the QR payload size synthetically from a real generated proof. Then record all results — plus any free research-relevant numbers already at hand — in the new standalone deliverable `docs/improvement/PERFORMANCE_METRICS_E1E2.md`.

Purpose: PERF-01 requires statistically rigorous timings (mean ± σ, n≥19); PERF-02 requires the metrics recorded for the new E1+E2 circuit. This doc feeds the team's research paper, so capture every real number already produced as a side effect rather than narrowly six rows (D-18 widened scope).

Output: `zkp-backend/bench.js`, a `bench` script in `package.json`, and `docs/improvement/PERFORMANCE_METRICS_E1E2.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-benchmarking-metrics/05-CONTEXT.md
@.planning/phases/05-benchmarking-metrics/05-01-PLAN.md

<interfaces>
<!-- zkp-backend self-contained building blocks bench.js drives (all CommonJS). -->

From zkp-backend/lib/encoding.js:
  module.exports = { hashToField, CHUNK_COUNTS, generateSalts, generateSalt, BN128_FIELD_ORDER };
  - generateSalts(7) -> string[7] of decimal-string salts.

From zkp-backend/lib/witnessBuilder.js:
  module.exports = { buildWitnessInput, ATTR_KEYS };
  - buildWitnessInput({ attrs, salts, reveal, nonce, currentDateInt }) -> Promise<circuit input object>.

From zkp-backend/lib/nonceStore.js:
  module.exports = { issueNonce, validateAndConsume, BN128_FIELD_ORDER, _store };
  - issueNonce() -> { nonce, sessionId, expiresAt }.
  - validateAndConsume(sessionId, presentedNonce) -> { ok, reason? }.

From zkp-backend/utils/timing.js (created in Plan 01):
  module.exports = { timed };   // async timed(label, fn) -> { out, seconds }

Proof generation primitive (mirrors server.js POST /generate-proof body):
  const { proof, publicSignals } = await snarkjs.groth16.fullProve(input, wasmPath, zkeyPath);
  wasmPath = path.join(__dirname, 'identity.wasm'); zkeyPath = path.join(__dirname, 'identity_final.zkey');
  vKey = JSON.parse(fs.readFileSync(path.join(__dirname, 'verification_key.json')));

Off-chain verify: await snarkjs.groth16.verify(vKey, publicSignals, proof).
On-chain verify: verifierContract.verifyProof(pA, pB, pC, publicSignals) — pB inner arrays swapped per server.js:178-181; needs VERIFIER_ADDRESS + BLOCKCHAIN_RPC_URL from .env and an ethers JsonRpcProvider.

Live fixture (registered on Sepolia this session under rollNo 22BCSD01 — verifyFlow.test.js:22-31):
  attrs = { name:"Utkarsh Baranwal", rollNo:"22BCSD01", dob:"20041014", programmeLevel:"B.Tech", discipline:"CSE", batch:"2022", email:"22bcsd01@iiitdmj.ac.in" }
  FIXED_SALTS = ["1","2","3","4","5","6","7"]; CURRENT_DATE_INT = "20260617"; reveal = {}.

Frozen public-signal layout (blueprint §3): length 19 = [0] merkleRoot, [1] nonce, [2] currentDateInt, [3] isOver18, [4] isPostgrad, [5..11] revealedValue[7], [12..18] revealMask[7].

Frozen constraint count (Phase 3, DO NOT recompute — D-18): 7891 total (3770 non-linear + 4121 linear), source: .planning/phases/03-trusted-setup-redeploy/CEREMONY_LOG.md and docs/current/research/PERFORMANCE_METRICS.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bench.js driving all six ops at N=20 / n=19 plus QR payload size</name>
  <files>zkp-backend/bench.js</files>
  <read_first>
    - .planning/phases/05-benchmarking-metrics/05-CONTEXT.md (D-16 bench location+loop, D-17 full on-chain treatment, D-19 synthetic QR size, Claude's-Discretion items on nonce direct-call vs HTTP and proof reuse)
    - docs/CLAUDE_CODE_BLUEPRINT.md §10.3 (N=20 / drop warm-up / mean±σ format, example line `proof_gen: mean 1.42 s ± 0.08 s (n=19)`)
    - zkp-backend/server.js (lines 27-30 artifact paths, 49-57 provider/verifier wiring, 72-204 the six op bodies to mirror — especially pA/pB swap at 175-183)
    - zkp-backend/lib/witnessBuilder.js, zkp-backend/lib/nonceStore.js, zkp-backend/lib/encoding.js (the building blocks bench.js calls directly)
    - zkp-backend/test/verifyFlow.test.js (lines 21-51 — the live registered fixture attrs, FIXED_SALTS, CURRENT_DATE_INT to reuse verbatim)
    - zkp-backend/utils/timing.js (the timed() helper from Plan 01)
  </read_first>
  <action>
    Create `zkp-backend/bench.js` (CommonJS, lives in zkp-backend/ per D-16) that benchmarks the six measured ops and computes the QR payload size. Use `require('dotenv').config()` and the same artifact paths and ethers provider/verifier wiring as `server.js` (wasm/zkey/vkey from __dirname, VERIFIER_ADDRESS + BLOCKCHAIN_RPC_URL from env via a requireEnv guard). Reuse the live registered fixture (attrs/FIXED_SALTS/CURRENT_DATE_INT) from verifyFlow.test.js verbatim so the on-chain op hits the credential already registered on Sepolia under rollNo 22BCSD01.

    Implement a generic `runLoop(label, fn, N=20)` that calls fn() N times, records each elapsed-seconds value (via the timed() helper from Plan 01, or performance.now() deltas — either is fine as long as it prints/collects seconds), DROPS the first (warm-up) sample, then computes `mean` and SAMPLE standard deviation (divide by n-1 where n=19, not population stddev) over the remaining 19. Print one line per op in the §10.3 format: `<label>: mean <m> s ± <sd> s (n=19)` with seconds to a sensible precision (3 decimals).

    The six ops to loop (per blueprint §10.2 / D-15 list):
    1. `MerkleRoot` — recompute the salted depth-3 root for the fixture using zkp-backend's own building blocks (build the witness input via buildWitnessInput and compute the root the same way the witness path does; do NOT cross-import privdId_admin). Mirror identityCommitment.js's Poseidon(2) leaf/level/root math if a standalone root helper is not exported.
    2. `ProofGeneration` — `snarkjs.groth16.fullProve(input, wasmPath, zkeyPath)` where input comes from buildWitnessInput with a fresh issued nonce per iteration.
    3. `OffChainVerification` — `snarkjs.groth16.verify(vKey, publicSignals, proof)`.
    4. `OnChainVerification` — `verifierContract.verifyProof(pA, pB, pC, publicSignals)` with the pB inner-array swap from server.js:178-181. Per D-17 give this the FULL N=20/n=19 live-Sepolia treatment — no reduced sample size. Per the discretion note it is fine to reuse ONE funded proof across all 20 calls (verify-onchain is a stateless 0-gas view call).
    5. `NonceIssue` — direct call to `issueNonce()` (sub-ms op; direct-call avoids HTTP overhead skewing it per the discretion note — label honestly as a direct-function measurement).
    6. `NonceCheck` — direct call to `validateAndConsume(sessionId, nonce)`; issue a fresh session each iteration so the one-time-use `used` flag does not turn iterations 2..20 into `nonce_already_used` no-ops.

    QR payload size (D-19): generate ONE real proof against the 19-signal shape, compute `Buffer.byteLength(JSON.stringify({ proof, publicSignals }), 'utf8')`, and print it as `qr_payload_bytes: <N> bytes`. Also print `public_signal_count: <publicSignals.length>` (expect 19) and `proof_size_bytes: <Buffer.byteLength(JSON.stringify(proof))>` as free side-effect numbers. Capture an `end_to_end_latency` measurement = elapsed seconds for one full issueNonce -> buildWitnessInput -> fullProve -> groth16.verify chain.

    Collect every printed number into an in-memory results object and `console.log` a final summary block (console-table or plain lines — Claude's discretion) so the executor can transcribe figures into the doc in Task 2. Wrap the whole run in an async main() with a top-level catch that prints the error and `process.exit(1)`, and call `process.exit(0)` on success so the ethers provider does not hang the process.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/zkp-backend && timeout 300 node bench.js 2>&1 | tee /tmp/bench_out.txt | grep -E "mean .* s ± .* s \(n=19\)|qr_payload_bytes|public_signal_count" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `node bench.js` runs to completion (exit 0) and prints six `<label>: mean <m> s ± <sd> s (n=19)` lines, one per op (MerkleRoot, ProofGeneration, OffChainVerification, OnChainVerification, NonceIssue, NonceCheck).
    - Output contains `public_signal_count: 19` and a non-zero `qr_payload_bytes:` line.
    - stddev is sample (n-1) based: bench.js source divides the squared-deviation sum by 18 (n-1 for n=19), not 19.
    - The NonceCheck loop issues a fresh session per iteration (grep bench.js for issueNonce inside the NonceCheck loop body) so no iteration returns reason `nonce_already_used`.
  </acceptance_criteria>
  <done>bench.js runs end to end, drives all six ops with the warm-up-dropped N=20/n=19 mean±σ methodology, measures QR payload bytes and public-signal count from a real proof, and prints a transcribable summary.</done>
</task>

<task type="auto">
  <name>Task 2: Wire npm run bench and write PERFORMANCE_METRICS_E1E2.md</name>
  <files>zkp-backend/package.json, docs/improvement/PERFORMANCE_METRICS_E1E2.md</files>
  <read_first>
    - .planning/phases/05-benchmarking-metrics/05-CONTEXT.md (D-18 exact path docs/improvement/PERFORMANCE_METRICS_E1E2.md + widened-scope free numbers + 7891 cite rule)
    - /tmp/bench_out.txt (the captured bench.js output from Task 1 — the real numbers to record)
    - zkp-backend/package.json (the scripts block to extend)
    - docs/current/research/PERFORMANCE_METRICS.md (the existing historical doc — read to mirror its table style, but DO NOT edit it; new doc is standalone in docs/improvement/)
    - .planning/phases/03-trusted-setup-redeploy/CEREMONY_LOG.md (source citation for the 7891 constraint count)
  </read_first>
  <action>
    Add a `"bench": "node bench.js"` entry to the `scripts` block in `zkp-backend/package.json` (keep the existing `test` script intact).

    Create `docs/improvement/PERFORMANCE_METRICS_E1E2.md` (exact path per D-18 — NOT docs/current/research/). Record the milestone's measured results from the Task 1 bench run:
    - A "Methodology" note: N=20 runs per op, first run dropped as warm-up, mean ± sample σ over n=19; on-chain verify is a live Sepolia 0-gas view call; QR size measured synthetically from a real 19-signal proof.
    - A timing table with one row per measured op (MerkleRoot, ProofGeneration, OffChainVerification, OnChainVerification, NonceIssue, NonceCheck) showing mean ± σ (n=19) in seconds, transcribed from /tmp/bench_out.txt.
    - The static facts: circuit constraint count = 7891 total (3770 non-linear + 4121 linear), CITED from Phase 3 (`.planning/phases/03-trusted-setup-redeploy/CEREMONY_LOG.md`, verified via `snarkjs zkey verify`) — DO NOT recompute, just cite the frozen figure with its source per D-18.
    - QR payload size in bytes (`JSON.stringify({proof, publicSignals})`).
    - The free research-relevant numbers also produced this phase (D-18 widened scope): proof size in bytes, public-signal count (19), and the end-to-end nonce->proof->verify latency. Note explicitly that on-chain `verifyProof` is a 0-gas view call and that no verifier gas figure is recorded because Phase 3 did not capture one (do NOT fabricate a gas number).
    - A footer dating the run (2026-06-18) and naming the source script (`zkp-backend/bench.js`) and the live Sepolia fixture (rollNo 22BCSD01).

    Do not touch `docs/current/research/PERFORMANCE_METRICS.md` — it stays as the historical record.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app && test -f docs/improvement/PERFORMANCE_METRICS_E1E2.md && grep -c "7891" docs/improvement/PERFORMANCE_METRICS_E1E2.md && grep -qE "bench" zkp-backend/package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/improvement/PERFORMANCE_METRICS_E1E2.md` exists (this exact path, not docs/current/research/) and `docs/current/research/PERFORMANCE_METRICS.md` is unchanged (no diff).
    - The new doc contains `7891` cited to the Phase 3 ceremony log source (not recomputed), a six-row timing table with mean ± σ (n=19) values, a QR-payload-bytes figure, public-signal-count 19, proof size in bytes, and an end-to-end latency figure.
    - The doc explicitly states on-chain verifyProof is a 0-gas view call and that no verifier gas number is recorded (no fabricated gas figure).
    - `zkp-backend/package.json` has a `bench` script (`node bench.js`) and still has the original `test` script; `node -e "require('/home/chetan/digital_id_app/zkp-backend/package.json')"` parses cleanly.
  </acceptance_criteria>
  <done>npm run bench is wired, and PERFORMANCE_METRICS_E1E2.md at the corrected docs/improvement/ path records all six op timings (mean±σ n=19), the cited 7891 constraint count, QR payload size, and the free side-effect numbers — without fabricating any unrecorded figure or editing the historical doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench.js → Sepolia RPC | bench.js reads VERIFIER_ADDRESS + BLOCKCHAIN_RPC_URL and makes read-only verifyProof view calls. No tx, no gas, no secrets written. The RPC endpoint is the only external surface. |
| bench.js → local artifacts | reads identity.wasm / identity_final.zkey / verification_key.json from disk to generate proofs. Trusted local files produced by Phase 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information disclosure | metrics doc or bench output leaks PII (the fixture attrs include a real name/email) | mitigate | bench.js logs only timing numbers, byte sizes, and counts — never attribute values, salts, proofs, or the nonce. The doc records aggregate numbers only. Acceptance criteria check the printed lines are metric lines, not attribute dumps. |
| T-05-04 | Tampering / DoS | a failed Sepolia view call mid-loop hangs or crashes bench.js | accept | bench.js wraps main() in a try/catch with process.exit(1) on error; on-chain loop is read-only (no funds at risk). A transient RPC failure is a re-run, not a state-corruption risk. |
| T-05-05 | Spoofing | fabricating a verifier gas number Phase 3 never recorded | mitigate | Task 2 explicitly forbids inventing a gas figure; the doc states verifyProof is a 0-gas view call and no gas number is on record. Acceptance criteria assert no fabricated gas value. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — bench.js uses only already-installed snarkjs, ethers, dotenv (in package.json). If any install is attempted, stop and require RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
- `cd zkp-backend && npm run bench` → exit 0, prints six `mean ... s ± ... s (n=19)` lines + qr_payload_bytes + public_signal_count: 19.
- `test -f docs/improvement/PERFORMANCE_METRICS_E1E2.md` → present; `docs/current/research/PERFORMANCE_METRICS.md` → unchanged.
- `grep 7891 docs/improvement/PERFORMANCE_METRICS_E1E2.md` → present and cited to Phase 3, not recomputed.
- bench.js sample stddev divides by n-1 (18 for n=19).
</verification>

<success_criteria>
`bench.js` produces statistically rigorous timings (mean ± sample σ over n=19, warm-up dropped) for all six measured ops plus a synthetic QR payload size, wired as `npm run bench`, and `docs/improvement/PERFORMANCE_METRICS_E1E2.md` records those results — the six op timings, the cited 7891 constraint count, QR payload size, public-signal count (19), proof size in bytes, and end-to-end latency — without fabricating an unrecorded gas figure or modifying the historical `docs/current/research/PERFORMANCE_METRICS.md`.
</success_criteria>

<output>
Create `.planning/phases/05-benchmarking-metrics/05-02-SUMMARY.md` when done.
</output>
