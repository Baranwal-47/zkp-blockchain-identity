---
phase: 01-freeze-spec-field-set-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/current/research/IDENTITY_SPEC.md
  - privdId_admin/backend/constants/enumCodes.js
autonomous: true
requirements: [SPEC-01]
must_haves:
  truths:
    - "The frozen spec documents all 7 leaf attributes in fixed order with per-attribute type/encoding and the public-signal layout, leaf 7 = zero-padding."
    - "A single canonical enum-code module maps programmeLevel and discipline strings to frozen integer codes, with the isPostgrad set {4,5,6}."
  decisions: [D-01, D-02, D-03, D-06, D-07, D-09]
  artifacts:
    - path: "docs/current/research/IDENTITY_SPEC.md"
      provides: "Frozen 7-attribute identity commitment spec (SPEC-01)"
      contains: "Poseidon"
      min_lines: 60
    - path: "privdId_admin/backend/constants/enumCodes.js"
      provides: "Canonical programmeLevel + discipline + isPostgrad code maps"
      exports: ["PROGRAMME_LEVEL", "DISCIPLINE", "POSTGRAD_CODES"]
  key_links:
    - from: "docs/current/research/IDENTITY_SPEC.md"
      to: "privdId_admin/backend/constants/enumCodes.js"
      via: "spec references the enum module as the single source of truth for codes"
      pattern: "enumCodes"
---

<objective>
Freeze the 7-attribute identity commitment spec as a written single-source-of-truth document and create the canonical enum-code module both the admin backend and the Phase-2 circom circuit will reference. This is the foundation artifact for SPEC-01: every downstream task (the commitment module, the issuance refactor, the Phase-2 circuit) reads its leaf order, encodings, chunking arities, enum codes, and Merkle topology from here.

Purpose: A spec change discovered after the circuit is frozen (Phase 2/3) forces a full trusted-setup redo. Freezing it now, on paper, with the verified RESEARCH test vectors as the parity oracle, is the design-once guardrail.
Output: `docs/current/research/IDENTITY_SPEC.md` (frozen spec) and `privdId_admin/backend/constants/enumCodes.js` (frozen codes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-freeze-spec-field-set-consistency/01-CONTEXT.md
@.planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md
@.planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the frozen IDENTITY_SPEC.md spec document (SPEC-01)</name>
  <files>docs/current/research/IDENTITY_SPEC.md</files>
  <read_first>
    - .planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md (Primary Research Deliverable section: chunking scheme, frozen chunk counts, verified test vectors; Pattern 2 Merkle topology; BN128 field order)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-CONTEXT.md (decisions D-01 through D-12)
    - docs/CLAUDE_CODE_BLUEPRINT.md §3 (frozen 7-attribute spec, public-signal layout)
  </read_first>
  <action>
    Create a new markdown spec doc at docs/current/research/IDENTITY_SPEC.md (Claude's-discretion location chosen: a dedicated doc beside the other current-state research, per RESEARCH file-layout recommendation, rather than inlining into the blueprint). The doc is the frozen single source of truth and MUST contain, verbatim and unambiguous:

    1. Leaf layout table, fixed indices = Merkle leaf positions (per D-09): 0:name, 1:rollNo, 2:dob (YYYYMMDD integer), 3:programmeLevel (integer code), 4:discipline (integer code), 5:batch (4-digit year integer), 6:email, 7:zero-padding. For each leaf give: attribute, type, encoding, and source (admin form field).

    2. String encoding rule (per D-01/D-02): hash-to-field = UTF-8 bytes split into ≤31-byte chunks, each chunk packed big-endian (left-most byte most-significant) into a BigInt, zero-padded at the END to a per-attribute fixed maxChunks, then Poseidon(maxChunks)(chunk_0..chunk_{maxChunks-1}), result via poseidon.F.toString() (decimal). State explicitly: avoid keccak256(value) mod p. State the frozen chunk counts (= Poseidon arities): name=4, rollNo=2, email=2. State the email ≤62-byte validator cap and its rationale (keeps email arity = rollNo arity = 2).

    3. Integer encoding rule (per D-03): dob committed as YYYYMMDD integer, batch as 4-digit year integer, both fed directly to the leaf hasher (no string encoding).

    4. Leaf and node hashing (per D-09): leaf_i = Poseidon(2)(encodedAttr_i, salt_i); internal node = Poseidon(2)(left_child, right_child) where left has the lower leaf index (state explicitly Poseidon(2)(left,right) NOT (right,left) — Poseidon is not symmetric); root = merkleRoot = pubHash. Include the depth-3 / 8-leaf tree topology diagram. State leaf[7] = Poseidon(2)(0,0) (the zero-padding leaf is the hash of (0,0), a non-zero field element 14744269619966411208460611736853059166543709924778005885397896789179099038553, NOT bare 0).

    5. Salt rule (per D-10): one salt per committed attribute (leaves 0..6), 31 random bytes -> BigInt -> decimal string (always < BN128 order), stored in Student.salts[] in leaf-index order; leaf[7] uses salt 0.

    6. Public-signal layout: pubHash (the Merkle root, decimal string) is public signal [0]; bytes32 form = ethers.zeroPadValue(ethers.toBeHex(BigInt(root)), 32). Note the full disclosure/predicate/nonce public-signal ordering is frozen in Phase 2 — this doc fixes pubHash as signal [0] only.

    7. BN128 scalar field order, stated exactly: 21888242871839275222246405745257275088548364400416034343698204186575808495617.

    8. Frozen enum codes (per D-06), referencing enumCodes.js as the executable source of truth: programmeLevel { B.Tech:1, B.Des:2, Dual:3, M.Tech:4, M.Des:5, PhD:6 }; discipline { CSE:1, ECE:2, ME:3, SmartMfg:4, Design:5, NatSci:6 }; isPostgrad = programmeLevel ∈ {4,5,6}. State append-only (per D-07).

    9. A "Cross-path parity oracle" section embedding the verified RESEARCH test vectors verbatim, including at minimum: Poseidon(2)(1,2), hashToField("Utkarsh Baranwal",4), hashToField("Rajesh Kumar Sharma Gupta Verma Singh",4) (37-byte / 2-chunk), hashToField("21BCS027",2), the >31-byte email vector hashToField("utkarshbaranwal47@students.iiitdmj.ac.in",2) with both chunk values, and Poseidon(2)(0,0). Label these as the mandatory parity check the re-seed script (plan 04) must reproduce.

    10. A "Phase-2 forward contract" note: the circom circuit MUST mirror leaf order, chunk arities, node-combine order, enum codes, and the isPostgrad set {4,5,6} verbatim. Add the one-line confirmation note from RESEARCH open question #1: Dual (code 3) is EXCLUDED from isPostgrad — confirm with users before the Phase-2 circuit is frozen, as it cannot change after trusted setup.

    Use directive prose and tables; do not paste a JS implementation of the commitment module into the spec (the implementation lives in plan 02). Code-shaped content is limited to the enum-code tables, the topology diagram, and the labeled test vectors.
  </action>
  <verify>
    <automated>test -f docs/current/research/IDENTITY_SPEC.md && grep -q "21888242871839275222246405745257275088548364400416034343698204186575808495617" docs/current/research/IDENTITY_SPEC.md && grep -q "utkarshbaranwal47@students.iiitdmj.ac.in" docs/current/research/IDENTITY_SPEC.md && grep -Eq "name=4|name: 4|name \| 4" docs/current/research/IDENTITY_SPEC.md && grep -q "Poseidon(2)(left" docs/current/research/IDENTITY_SPEC.md</automated>
  </verify>
  <done>IDENTITY_SPEC.md exists, documents all 7 leaf attributes + zero-padding leaf in fixed order with types/encodings, frozen chunk arities (name=4, rollNo=2, email=2), Merkle node-combine order (left,right), enum codes, isPostgrad set {4,5,6}, BN128 field order, pubHash public-signal layout, and the verified parity test vectors including the >31-byte email vector and Poseidon(2)(0,0).</done>
</task>

<task type="auto">
  <name>Task 2: Create the frozen enumCodes.js canonical mapping module</name>
  <files>privdId_admin/backend/constants/enumCodes.js</files>
  <read_first>
    - .planning/phases/01-freeze-spec-field-set-consistency/01-PATTERNS.md (enumCodes.js section: ESM named-export style, camelCase file naming, constants/ directory)
    - .planning/phases/01-freeze-spec-field-set-consistency/01-RESEARCH.md (Pattern 4: Canonical enum code module)
    - privdId_admin/backend/validators/studentValidator.js (analog: ESM named exports, inline enum strings)
  </read_first>
  <action>
    Create a new ESM module at privdId_admin/backend/constants/enumCodes.js (new constants/ directory, camelCase filename per CONVENTIONS.md). Pure constant declarations, no runtime logic, named exports only (no default export), consistent with the ESM style in studentValidator.js. Export exactly (per D-06):
    - PROGRAMME_LEVEL = { "B.Tech":1, "B.Des":2, "Dual":3, "M.Tech":4, "M.Des":5, "PhD":6 }
    - DISCIPLINE = { "CSE":1, "ECE":2, "ME":3, "SmartMfg":4, "Design":5, "NatSci":6 }
    - POSTGRAD_CODES = new Set([4, 5, 6])  // M.Tech, M.Des, PhD
    Add a top-of-file comment block stating: FROZEN — append-only (per D-07); changing an existing mapping invalidates all prior commitments and forces a Phase-2 circuit rebuild + trusted-setup redo; the Phase-2 circuit must hardcode these same integer values in its set-membership check; Dual (code 3) is EXCLUDED from isPostgrad (confirm before Phase 2 freeze). Do not add new programmes/disciplines unless appending the next free integer.
  </action>
  <verify>
    <automated>cd /home/chetan/digital_id_app/privdId_admin/backend && node --input-type=module -e "import('./constants/enumCodes.js').then(m=>{const ok=m.PROGRAMME_LEVEL['M.Tech']===4 && m.PROGRAMME_LEVEL['B.Tech']===1 && m.DISCIPLINE['CSE']===1 && m.DISCIPLINE['NatSci']===6 && m.POSTGRAD_CODES.has(4) && m.POSTGRAD_CODES.has(5) && m.POSTGRAD_CODES.has(6) && !m.POSTGRAD_CODES.has(3); if(!ok)process.exit(1); console.log('enumCodes ok');})"</automated>
  </verify>
  <done>enumCodes.js exports PROGRAMME_LEVEL, DISCIPLINE, POSTGRAD_CODES with the exact frozen integer codes; isPostgrad set is {4,5,6} and excludes Dual (3); the node import check exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec doc → all downstream implementers | The frozen spec is the contract every later artifact (commitment module, circuit) copies verbatim; ambiguity here propagates as silent verification failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | enum-code drift between this module and the Phase-2 circuit | mitigate | Single canonical enumCodes.js + spec doc both state the codes and the append-only rule (D-07); spec includes a Phase-2 forward-contract note requiring verbatim mirroring |
| T-01-02 | Information disclosure | isPostgrad set membership chosen wrong (Dual mis-classified) | mitigate | Spec and module both flag Dual(3) excluded from {4,5,6} as a confirm-before-freeze item (RESEARCH open question #1); a wrong set leaks/denies a predicate but is caught before the circuit is frozen, not after |
| T-01-03 | Tampering | spec omits node-combine order, breaking Merkle root reproduction | mitigate | Spec states Poseidon(2)(left,right) with left = lower index explicitly, plus the topology diagram |
</threat_model>

<verification>
- IDENTITY_SPEC.md documents all 7 attributes + zero-pad leaf, encodings, arities, node order, enum codes, BN128 order, pubHash layout, and embeds the verified parity vectors.
- enumCodes.js imports cleanly under ESM and returns the exact frozen codes; isPostgrad excludes Dual.
- Both artifacts agree on the enum codes and the isPostgrad set.
</verification>

<success_criteria>
- SPEC-01 satisfied: the 7-attribute spec is frozen and documented with fixed leaf order, per-attribute types/encodings, and the pubHash public-signal layout, leaf 7 reserved as zero-padding.
- The canonical enum module exists and is importable by the admin backend (and later the Phase-2 circuit tooling).
</success_criteria>

<output>
Create `.planning/phases/01-freeze-spec-field-set-consistency/01-01-SUMMARY.md` when done.
</output>
